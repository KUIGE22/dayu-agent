# Aggregate Fix RT-01 Dual Re-Review

- **Review 类型**：dual re-review（只读复核，不修改 production/tests/plan/implementation/adjudication）
- **日期**：2026-08-08 20:15:07 CST
- **Output file**：`docs/reviews/code-review-20260808-201507.md`
- **Review 基线**：`a0c469a`（HEAD，v5.8 ACCEPTED）
- **改动范围**：
  - `dayu/cli/commands/research_template.py`（+2/-2）
  - `tests/cli/test_research_template_command.py`（+34 lines）
- **参考契约**：v5.8 `AGG-RT-CTRL-01`
- **初审 artifacts**：
  - `docs/reviews/code-review-20260808-195939.md`（DeepSeek，PASS，0/0/0）
  - `docs/reviews/code-review-20260808-115535.md`（MiMo，PASS，0/0/0）
- **Controller 裁决**：`docs/reviews/aggregate-fix-rt-exit-code-review-adjudication-20260808-codex.md`（accepted findings=0，open 0/0/0）
- **Reviewer**：Codex（dual re-review gate）

---

## Re-Review 结论

**Verdict**：PASS。无 material findings。

**Open Findings**：High=0，Medium=0，Low=0。

**初审裁决复核**：Controller 裁决（accepted findings=0，open H/M/L=0/0/0，无需 fix）**正确**。代码仍满足 v5.8 `AGG-RT-CTRL-01` 契约。

---

## 1. 实际代码 diff 复核

### 1.1 生产代码变更

`dayu/cli/commands/research_template.py` 仅有两处变更：

**变更 1 — Docstring Returns**（line 602）：

```diff
-        校验结果成功输出时返回 0。
+        校验成功时返回 0，校验失败时返回 1。
```

**变更 2 — Return 语句**（line 614）：

```diff
-    return 0
+    return 0 if result.get("ok") is True else 1
```

复核确认：
- `validate_monitoring_source_map_payload` 返回 `dict[str, object]`，`ok` 键由 `not errors` 赋值，恒为 `bool`。`is True` identity check 正确。
- `.get("ok")` 在 `ok` 缺失时返回 `None`，`None is True` → `False`，安全回退到 `1`。
- JSON stdout（`print(json.dumps(...))`）、dispatch mapping、异常传播、owner/DAG 均未改变。
- 其他 5 个 `_run_validate_*` sibling 中，`_run_validate_research_workbook`（line 497）使用相同无 `isinstance` guard 的模式；其余 4 个使用 `isinstance(validation, dict) and` guard，是 pre-existing 设计差异，非本修复引入。

**结论**：diff 与初审描述完全一致，无遗漏或误报。

### 1.2 测试代码变更

`tests/cli/test_research_template_command.py` 新增一个测试函数（+34 lines）：

```python
@pytest.mark.unit
def test_run_validate_source_map_command_returns_nonzero_for_template_mismatch(
    tmp_path: Path,
    capsys: pytest.CaptureFixture[str],
) -> None:
    rules_path = write_monitoring_rules_payload("financial", workspace_root=tmp_path)
    source_map_path = write_monitoring_source_map_payload("consumer", workspace_root=tmp_path)
    args = DayuCliArguments(
        research_template_action="validate-source-map",
        rules=str(rules_path),
        source_map=str(source_map_path),
    )
    result = run_research_template_command(args)
    assert result == 1
    payload = json.loads(capsys.readouterr().out)
    assert payload["ok"] is False
    assert payload["errors"]
```

复核确认：
- 使用 `financial` rules + `consumer` source-map 构造 template mismatch，精确匹配 v5.8 契约。
- 通过真实 `run_research_template_command` → dispatch → `_run_validate_source_map` 完整链路。
- 断言退出码 `1`、`ok is False`、`errors` 非空，覆盖 failure 分支。
- 已有 `test_run_validate_source_map_command_outputs_validation_json`（consumer/consumer，退出码 0，`ok is True`）未修改，覆盖 success 分支。
- 包含完整中文 docstring，使用 `tmp_path` 隔离。

**结论**：测试变更与初审描述完全一致，覆盖充分。

---

## 2. 初审 Finding 复核

| 初审 | Verdict | Findings | 复核 |
|---|---|---|---|
| DeepSeek（code-review-20260808-195939.md） | PASS | 0 | ✅ 正确 — 8 个维度审查均无缺陷 |
| MiMo（code-review-20260808-115535.md） | PASS | 0 | ✅ 正确 — 9 个维度审查均无缺陷 |

两路初审结论一致，均未提出 finding 或 observation。复核实际代码 diff 后确认两路审查覆盖完整、分析准确，不存在遗漏的 material issue。

---

## 3. Controller 裁决复核

Controller 裁决：
- accepted findings = 0 ✅
- rejected findings = 0 ✅
- deferred findings = 0 ✅
- needs-more-evidence findings = 0 ✅
- open High/Medium/Low = 0/0/0 ✅
- 无需 code/test/README/plan fix ✅

**复核结论**：Controller 裁决正确。两路初审均 PASS 且无 findings，不存在需要接受、拒绝或延期的审查项。

---

## 4. v5.8 契约逐项复核

| v5.8 AGG-RT-CTRL-01 要求 | 实现状态 |
|---|---|
| 只改 `_run_validate_source_map` 的 Return | ✅ `return 0 if result.get("ok") is True else 1` |
| 同步中文 docstring Returns | ✅ "校验成功时返回 0，校验失败时返回 1" |
| JSON stdout 不变 | ✅ `print(json.dumps(result, ...))` 未改 |
| dispatch/action key/owner/DAG 不变 | ✅ |
| 异常集合与传播不变 | ✅ |
| 测试用 `financial` rules + `consumer` source-map | ✅ |
| 测试经真实 `run_research_template_command` | ✅ |
| 断言返回 1、`ok is False`、errors 非空 | ✅ |
| valid 回归用 consumer/consumer、断言返回 0、`ok is True` | ✅ 已有测试保留 |
| 不新增 Slice 14、不重构其他 runner | ✅ `git diff HEAD --stat` 确认仅 2 文件 |

**结论**：代码仍完全满足 v5.8 契约。

---

## 5. 现场验证

- Focused 测试：`3 passed, 213 deselected` ✅
- `git diff HEAD --stat`：`2 files changed, 36 insertions(+), 2 deletions(-)` ✅
- 变更文件仅 `research_template.py` 和 `test_research_template_command.py`，无 README/plan/CI 变更 ✅

---

## 6. 补充说明

- 本 re-review 是只读复核，未修改 production code、tests、README、plan、implementation artifact 或 adjudication artifact。
- 初审 artifacts（code-review-20260808-195939.md、code-review-20260808-115535.md）和 adjudication artifact 保持原样。
- 未发现初审遗漏的 material issue，未发现 v5.8 契约违反。

---

*本 artifact 由 Codex dual re-review gate 产出。状态：FINAL。*
