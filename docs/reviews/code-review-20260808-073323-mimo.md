# Slice 8 Code Review — Challenger 与配置回滚迁移

- **审查人**: mimo
- **日期**: 2026-08-08
- **基线**: `fcf4602` (HEAD)
- **范围**: workspace uncommitted changes
- **Verdict**: **PASS**

## 审查维度与结论

### 1. AST 等价（14/14 PASS）

对 14 个迁移函数逐一剥离 docstring 后做 AST dump 比较，全部14/14 PASS。

验证方法：Python `ast.dump()` 对 HEAD 与当前版本的 `FunctionDef` 节点做精确比较，body 首节点 `Expr(Constant(str))` 被剥离。

### 2. write.py 本地定义计数

- HEAD：16 个顶层函数定义
- 当前：2 个顶层函数定义（`_materialize_research_after_write`、`run_write_command`）
- 旧定义残留：0 ✓

### 3. write.py 真实 global lookup（10+1）

`run_write_command` 中对迁移函数的 `Name` 引用：

| 类别 | 计数 | 详情 |
|---|---:|---|
| Challenger | 10 | `_assert_challenger_run_output_boundaries`、`_build_auto_bootstrap_args`、`_build_challenger_run_plan_from_args`、`_build_challenger_write_config`、`_needs_auto_research_bootstrap`、`_persist_challenger_run_authorization_after_preflight`、`_preflight_champion_and_challenger`、`_run_champion_challenger_experiment`、`_verify_and_consume_challenger_run_approval_before_host`、`_verify_challenger_preflight_approval_before_host` |
| Rollback | 1 | `_run_write_model_configuration_rollback` |

三个内部 helper（`_challenger_preflight_cli_args`、`_load_current_challenger_proposal`、`_manifest_has_usable_company_facets`）无 surviving `write.py` caller，不建立兼容绑定。✓

### 4. 直接 owner vs dispatch owner 与 identity 测试

`test_write_challenger_and_rollback_imports_preserve_identity` 包含 11 个 `is` 断言（10 Challenger + 1 rollback），全部指向真实 owner 模块中的同一函数对象。✓

测试 monkeypatch 路径已从 `dayu.cli.commands.write.<symbol>` 迁移到 `dayu.cli.commands._write_challenger.<symbol>` 或 `dayu.cli.commands._write_execution.<symbol>`，对应函数的实际定义位置。✓

### 5. 退出码、异常分类与 I/O

- `_run_write_model_configuration_rollback`：退出码映射（0/4/6/2）与 HEAD 完全一致。✓
- 异常捕获层级（`BlockedError` → 4、`ReceiptError` → 6、`File/OS/Type/Value` → 2）不变。✓
- `format_write_model_configuration_operator_rollback_receipt_report` 输出到 stdout 不变。✓
- 所有 Challenger 函数的退出码语义不变。✓

### 6. 模块常量与单一真源

- `_CHALLENGER_COMPARISON_FILE`：从 `write.py` 迁移到 `_write_challenger.py`，`write.py` 中已删除。✓
- `MODULE`：两个新 owner 均从 `_write_config_helpers.MODULE` import，不本地复制。✓

### 7. 依赖方向

- `_write_challenger.py` → `write.py` 反向 import：0 ✓
- `_write_config_rollback.py` → `write.py` 反向 import：0 ✓
- `_write_config_rollback.py` → `_write_snapshot_builder` → `_write_config_application`：单向链，无环。✓
- `write.py` 旧独占依赖（`build_snapshot_builder`、`WorkspaceConfig`、`ExecutionOptions`、`WriteRunConfig`、所有 `challenger_*`/`rollback_*` service imports）已全部移除。✓

### 8. `_manifest_has_usable_company_facets` function-local import

HEAD 的 `from dayu.cli.research_template_routing import load_company_facets_from_manifest` 作为 function-local import 按 exact AST 保留。✓

这不是 owner/compat lazy import，是既有 domain local import，按 Controller 裁决保留。

### 9. Any 语义使用

| 文件 | Any 引用数 |
|---|---:|
| HEAD `write.py` | 7 |
| 当前 `write.py` | 1 |
| `_write_challenger.py` | 6 |
| `_write_config_rollback.py` | 0 |
| **合计** | **7** |

7→7，未新增签名或逃逸。`_write_challenger.py` 的 `typing.Any` import 是承载既有签名的必要 ownership import，不属于类型扩散。✓

### 10. `__all__` 与私有模块

两个新 owner 均为私有模块（`_` 前缀），无 `__all__`，符合预期。✓

### 11. Docstring 覆盖

| 模块 | 概览 docstring | 函数 docstring | Args/Returns/Raises |
|---|---|---|---|
| `_write_challenger.py` | ✓ | 14/14 | 14/14 |
| `_write_config_rollback.py` | ✓ | 1/1 | 1/1 |

全部使用中文，符合编码硬约束。✓

### 12. 测试验证

```
659 passed in 12.31s
```

pyright：`0 errors, 0 warnings, 0 informations`。✓

Ruff F/I：仅保留 HEAD 既有 `I001`（`test_cli_running_config.py`），无新增 finding。✓

`git diff --check`：PASS。✓

### 13. 覆盖率

实现文档记录：

| 文件 | statements | covered | 精确覆盖率 |
|---|---:|---:|---:|
| `_write_challenger.py` | 200 | 166 | 83.0% |
| `_write_config_rollback.py` | 34 | 31 | 91.2% |
| `write.py` | 189 | 157 | 83.1% |

三份文件均满足 `>=80%` 门槛。✓

### 14. README

实现文档记录 README diff 为 0。本 Slice 只迁移 CLI 私有实现，命令、参数、退出码与用户调用方式不变，无需更新 README。✓

## Findings

### L-1: 实现文档 Challenger 函数计数表述不精确

**严重性**: Low

**位置**: `docs/reviews/slice-8-write-challenger-rollback-implementation-20260808-codex.md` 第 37 行

**证据**: 文档写"按 accepted v5.0 顺序 exact 迁移 13 个 Challenger 函数"，但 `_write_challenger.py` 实际包含 14 个顶层函数定义。三个内部 helper（`_challenger_preflight_cli_args`、`_load_current_challenger_proposal`、`_manifest_has_usable_company_facets`）在文档第 75-77 行被单独列出，但第 37 行的"13"似乎未将 `_manifest_has_usable_company_facets` 计入。实际应为"exact 迁移 14 个 Challenger 相关函数（含 3 个内部 helper）"或"exact 迁移 10 个外部函数 + 4 个内部/helper 函数"。

**影响**: 仅文档准确性，不影响代码行为。identity 测试（10+1）与 global lookup 计数（10+1）均正确覆盖了外部绑定。

**最小修复**: 将第 37 行改为"exact 迁移 14 个 Challenger 相关函数（10 个外部函数 + 4 个内部 helper）"，或明确说明 13 是指外部函数计数。

## Open Findings

| # | 严重性 | 描述 | 状态 |
|---|---|---|---|
| L-1 | Low | 实现文档 Challenger 函数计数"13"不精确，实际为 14 | Open |
