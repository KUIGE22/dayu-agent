# Slice 9 Code Review Targeted Re-Review（MiMo）

- **日期**: 2026-08-08
- **基线**: `89fa4bc gateflow: accept cli write architecture plan v5.1`
- **分支**: `codex/dual-model-research-mvp`
- **Gate**: Slice 9 code review targeted re-review after fix
- **输入 artifact**:
  - 初审：`docs/reviews/code-review-20260808-091001-mimo.md`（PASS，open 0/0/1）
  - DeepSeek 初审：`docs/reviews/code-review-20260808-091000-deepseek.md`（PASS，5 findings）
  - Controller 裁决：`docs/reviews/slice-9-code-review-adjudication-20260808-codex.md`
  - Fix 记录：`docs/reviews/slice-9-research-template-modules-fix-20260808-codex.md`
  - 更新后 implementation：`docs/reviews/slice-9-research-template-modules-implementation-20260808-codex.md`
- **Reviewer**: MiMo
- **Verdict**: **PASS**
- **open High/Medium/Low**: `0/0/0`

## 1. 验证方法

读取裁决与 fix 文档后，对当前 workspace diff 执行以下 targeted 验证：

1. MiMo L-01：83 函数 docstring 具体化审计
2. DS 2/3/5：三个异常 docstring 精度修复核验
3. DS 1：RuntimeError 存量证据核验
4. DS 4：私有 helper 为 accepted owner 证据核验
5. 结构回归：83+2+40 AST、DAG、identity、__all__、import smoke
6. 静态门禁：323+812 tests、pyright 0、Ruff delta 0

## 2. 逐项裁决

### MiMo L-01（Low）— 83 个迁移函数 docstring 系统性泛化

- **初审状态**: OPEN
- **裁决**: `ACCEPTED / FIXED / CLOSED`
- **核验**:
  - 83 函数逐名 AST `ast.get_docstring` 审计：泛化模板短语（"处理研究模板数据"、"构建研究模板数据"等）残留 **0**。
  - 抽查 `_normalize_template_name` → "去除模板名两端空白、转换为小写并拒绝路径分隔符" ✓
  - 抽查 `_sha256_file` → "以 1 MiB 分块读取文件并计算 SHA-256 十六进制摘要" ✓
  - 抽查 `recommend_research_templates` → "根据公司主特征与约束特征为打包模板评分并稳定排序" ✓
  - 抽查 `validate_research_template_bundle_descriptor` → "校验 Bundle schema、产物存在性、指纹、工作簿、报告和监控一致性" ✓
  - 抽查 `build_monitoring_execution_plan` → "从健康 Bundle 的规则与 source-map 构建禁用自动执行的 dry-run 监控计划" ✓
  - 抽查 `materialize_research_template_bundle` → "在单一回滚边界内物化模板、工作簿、报告、监控文件、指南、检查单和 Bundle" ✓
  - 83 函数全部包含 Args / Returns / Raises 三段，参数说明遗漏 0。
- **行为保护**: stripped-docstring AST 与基线差异 0（见 §3）。

### DS 1（High）— RuntimeError 逃逸 runner 异常处理

- **初审状态**: DeepSeek OPEN (High)
- **裁决**: `REJECTED / CLOSED (PRE-EXISTING / OUT-OF-SCOPE)`
- **核验**:
  - `git show 89fa4bc:dayu/cli/commands/research_template.py` 第 2522、2641、2801 行均有 `raise RuntimeError(...)` — 基线存量。
  - 同文件第 263 行 `except (FileNotFoundError, FileExistsError, ValueError)` — 基线存量。
  - 当前 `research_template.py:181` 的 except 元组与基线 stripped-docstring AST 等价。
  - Slice 9 是 accepted v5.1 exact owner migration，Controller 裁决禁止改变存量异常捕获边界。
- **结论**: 非 Slice 9 引入的 defect；如需修复，需独立语义 work unit。

### DS 2（Medium）— `list_research_templates` 异常 docstring 失真

- **初审状态**: DeepSeek OPEN (Medium)
- **裁决**: `ACCEPTED / FIXED / CLOSED`
- **核验**:
  - 当前 `_research_template_core.py` `list_research_templates` docstring Raises 段：
    ```
    Raises:
        FileNotFoundError: 当打包研究模板目录不存在时，由 ``_resolve_template_dir`` 传播。
        OSError: 当模板目录无法遍历或模板标题文件无法读取时。
    ```
  - 泛化短语 "本函数不显式抛出异常" 残留：**否** ✓
  - 仅改 docstring，未增加 catch 或改变空列表语义。

### DS 3（Medium）— `build_research_template_usage_guide` 异常 docstring 失真

- **初审状态**: DeepSeek OPEN (Medium)
- **裁决**: `ACCEPTED / FIXED / CLOSED`
- **核验**:
  - 当前 `_research_template_core.py` `build_research_template_usage_guide` docstring Raises 段：
    ```
    Raises:
        ValueError: 当模板名无效时。
        FileNotFoundError: 当模板目录或指定模板文件不存在时，由路径解析传播。
        OSError: 当模板标题文件无法读取时，由底层文件操作传播。
    ```
  - 泛化短语残留：**否** ✓
  - 仅改 docstring，底层异常仍原样传播。

### DS 4（Low）— `_load_company_facets_from_manifest` 被视为透明 wrapper

- **初审状态**: DeepSeek OPEN (Low)
- **裁决**: `REJECTED / CLOSED (PRE-EXISTING / ACCEPTED-PLAN OWNERSHIP / NON-COMPAT)`
- **核验**:
  - `git show 89fa4bc:dayu/cli/commands/research_template.py` 第 3883 行定义 `def _load_company_facets_from_manifest(path: Path) -> CompanyFacetProfile:` — 基线存量函数。
  - 当前该函数在 `_research_template_helpers.py` 中，是 accepted v5.1 S9-CTRL 逐名列入的 exact 83 个迁移 owner 之一。
  - stripped-docstring AST 与基线同名函数精确等价（已由 §3 统一验证）。
  - 不是为保留旧 import path 新增的 compatibility wrapper。

### DS 5（Low）— `write._materialize_research_after_write` 异常 docstring 失真

- **初审状态**: DeepSeek OPEN (Low)
- **裁决**: `ACCEPTED / FIXED / CLOSED`
- **核验**:
  - 当前 `write.py` `_materialize_research_after_write` docstring Raises 段：
    ```
    Raises:
        OSError: 当 write manifest、模板资产或物化产物无法读写时由底层物化流程传播。
        ValueError: 当 manifest 未完成、模板选择无效或物化验证失败时由底层物化流程传播。
        FileNotFoundError: 当 manifest 或选中的模板资产不存在时由底层物化流程传播。
        FileExistsError: 当目标产物已存在且未启用覆盖时由底层物化流程传播。
        RuntimeError: 当物化失败且快照回滚也失败时由底层物化流程传播。
    ```
  - 泛化短语 "本函数不显式抛出异常" 残留：**否** ✓
  - 仅改 docstring，function-local import 时序和异常传播语义不变。

## 3. 结构回归验证

### 83+2+40 stripped-docstring AST

| 组件 | 计划值 | 实测值 | 与基线 AST 差异 | 状态 |
|---|---|---|---|---|
| 5 owner FunctionDef | 22/26/12/11/12 | 22/26/12/11/12 | 0 | PASS |
| 5 owner ClassDef | 2/0/0/0/0 | 2/0/0/0/0 | 0 | PASS |
| main FunctionDef | 40 | 40 | 0 | PASS |
| main ClassDef | 0 | 0 | 0 | PASS |

85 个函数/类（83 + 2 dataclass）逐名与基线 stripped-docstring AST 比对：**全部匹配**。
40 个 retained entry + runners 逐名与基线 stripped-docstring AST 比对：**全部匹配**。

### DAG

```text
helpers → (none)
core → helpers
bundle → core, helpers
monitoring → bundle, core, helpers
materialize → bundle, core, helpers, monitoring
main → all 5 owners
```

- 逐边验证：与计划一致 ✓
- 零 reverse edge ✓
- 零 cycle ✓
- 五个 owner 均不 import main ✓

### Functional bindings

- main 从 5 owner 导入 exact 45 个功能绑定
- 分布 `6 / 18 / 6 / 9 / 6` ✓
- identity 回归测试 `test_research_template_functional_bindings_use_real_owners` 存在 ✓
- write lazy-materialize caller 回归测试 `test_write_materialize_helper_uses_real_materialize_owner` 存在 ✓

### __all__

- baseline 68 → current 55，删除 exact 13，无新增 ✓
- 五个 private owner `__all__` 均为 0 ✓

### write.py lazy import

- 旧路径 `dayu.cli.commands.research_template.materialize_research_bundle_from_write_manifest`：非测试代码 exact 0 ✓
- 新路径 `dayu.cli.commands._research_template_materialize.materialize_research_bundle_from_write_manifest`：exact 1 ✓

## 4. 静态门禁

| 检查项 | 结果 | 状态 |
|---|---|---|
| 323 research-template tests | PASS | ✓ |
| 812 write tests | PASS | ✓ |
| pyright | 0 errors, 0 warnings, 0 informations | ✓ |
| Ruff F/I | All checks passed | ✓ |
| Ruff full-rule positive delta | {} | ✓ |
| 七文件 coverage | 均 >= 80%（最差 81.41%） | ✓ |
| git diff --check | PASS | ✓ |
| README diff | 0 | ✓ |

## 5. Finding 汇总

| Finding | 来源 | 初审状态 | 裁决 | 最终状态 |
|---|---|---|---|---|
| MiMo L-01：docstring 系统性泛化 | MiMo | OPEN | FIXED | **CLOSED** |
| DS 1：RuntimeError 逃逸 | DeepSeek | OPEN (High) | REJECTED (pre-existing) | **CLOSED** |
| DS 2：list_research_templates docstring | DeepSeek | OPEN (Medium) | FIXED | **CLOSED** |
| DS 3：build_research_template_usage_guide docstring | DeepSeek | OPEN (Medium) | FIXED | **CLOSED** |
| DS 4：_load_company_facets_from_manifest wrapper | DeepSeek | OPEN (Low) | REJECTED (accepted-plan owner) | **CLOSED** |
| DS 5：write._materialize_research_after_write docstring | DeepSeek | OPEN (Low) | FIXED | **CLOSED** |

## 6. 结论

**Verdict: PASS**。open High/Medium/Low = **0/0/0**。

MiMo L-01 与 DS 2/3/5 的 docstring 修复已验证：83 函数全部具有具体的功能描述、参数说明与异常声明，泛化模板残留为 0，stripped-docstring AST 与基线差异为 0。DS 1 的 RuntimeError 为 HEAD 存量语义，DS 4 的私有 helper 为 accepted plan 锁定的 owner 迁移，均非 Slice 9 引入。结构门禁（exact counts、DAG、identity、__all__、lazy import）与静态门禁（323+812 tests、pyright 0、Ruff delta 0、七文件 coverage >= 80%）全部无回归。Controller open H/M/L=0/0/0 裁决得到独立复核确认。
