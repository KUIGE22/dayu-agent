# Dual Re-Review: v4.7 CANDIDATE — Private Binding Erratum

- **回顾对象**: v4.7 CANDIDATE master plan + Controller artifact `plan-slice-5-v4.7-private-binding-erratum-20260808-codex.md`
- **回顾目标**: 独立验证 v4.7 erratum 的每项声明与源码/测试事实一致；确认无 stale v4.6 "8 write binding" 残留；adversarial 识别残余 plan 不一致
- **回顾日期**: 2026-08-08
- **回顾者**: Claude (planreview skill, focused dual re-review)
- **输入证据**:
  - Controller artifact: `docs/reviews/plan-slice-5-v4.7-private-binding-erratum-20260808-codex.md`（169 行，已完整读取）
  - 前序 review: `docs/reviews/plan-review-20260808-023021.md`（本 reviewer 产出）
  - 源码: `write.py`（3194 行，含当前 import block lines 20-25）、`_write_config_helpers.py`（151 行）、`_write_params_validation.py`（1956 行）
  - 测试: `test_write_cli_dispatch.py`（identity lines 575-619）、`test_cli_running_config.py`（import lines 24-29, patch lines 5932-5936）
  - grep 全库扫描结果

---

## 0. 回顾方法

逐项对照 Controller artifact 六项源码事实、五项精确实施契约、六项不变门禁 → 与前序 review 源码证据交叉验证 → adversarial 搜索残余不一致。Master plan 文本因会话限制未直接读取，通过 Controller artifact 的规范性描述间接验证；任何无法直接验证的项标注为 OPEN limitation。

---

## 1. Controller 六项源码事实逐项验证

### 事实 1: 两个 validator 无生产 caller

Controller (lines 39-41):
> `_validate_challenger_run_plan_args` 与 `_validate_live_smoke_plan_args` 在 `write.py` 只有 import binding，没有生产 caller。

前序 review §1.1 源码证据:
- write.py 全文扫描: 无任何 `_validate_challenger_run_plan_args(...)` 或 `_validate_live_smoke_plan_args(...)` 调用
- `run_write_command` 仅调用 `_validate_research_materialization_args(args)` (line 2563) 和 `_challenger_requested(args)` (lines 2760/3010/3122)
- grep 全库确认: 两个函数的真实 caller 仅在 `_write_params_validation.py` 内部 (lines 1939, 1039)

**判定**: **PASS** — 与源码事实一致。✓

### 事实 2: 真实 caller 在 _write_params_validation.py 内部

Controller (lines 42-44):
> 两者的真实生产 caller 都在 `_write_params_validation.py` 内部。

前序 review 证据:
- `_validate_research_materialization_args` (line 1939): `run_plan_error = _validate_challenger_run_plan_args(args)`
- `_validate_research_materialization_args` (line 1039): `live_smoke_error = _validate_live_smoke_plan_args(args)`
- 两者均为同模块内部调用，解析自 `_write_params_validation.__dict__`，不经过 `write.__dict__`

**判定**: **PASS** — 与源码事实一致。✓

### 事实 3: PLC0414×2 / F401×2 冲突

Controller (lines 45-46):
> 显式同名 alias 触发 `PLC0414` 两项；改为普通 import 会触发 `F401` 两项。

前序 review §1.2 证据:
- 当前 `X as X` alias → PLC0414×2
- 改为普通 import（无 alias）→ F401×2（符号无生产 caller）
- 仅删除 import 才能同时满足 F401=0 和 PLC0414=0

**判定**: **PASS** — 与 lint 规则语义一致。✓

### 事实 4: write.py 6 个 functional private binding

Controller (lines 47-54) 列出 6 个:
1. `_resolve_write_model_override_name`
2. `_resolve_write_company_name`
3. `_build_write_run_config`
4. `_log_write_preflight_result`
5. `_challenger_requested`
6. `_validate_research_materialization_args`

前序 review 源码逐 caller 验证:
- #1: `run_write_command` line 2740 ✓
- #2: `run_write_command` line 2993 ✓
- #3: `run_write_command` lines 3004/3105/3116 ✓
- #4: `_run_write_preflight` lines 271/523 ✓
- #5: `run_write_command` lines 2760/3010/3122 ✓
- #6: `run_write_command` line 2563 ✓

**判定**: **PASS** — 6 个符号全部有 write.py 内生产 caller。✓

### 事实 5: MODULE 独立，不计入 6 个

Controller (lines 55-56):
> `MODULE` 是独立的正常功能 import，不是 private function，不计入上述 6 个。

源码证据:
- write.py line 13: `from dayu.cli.commands._write_config_helpers import (MODULE, ...)`
- MODULE 是 `str` 常量 `"APP.WRITE"`，不是函数
- `_write_config_helpers.py:12`: `MODULE = "APP.WRITE"` — 唯一定义

**判定**: **PASS** — MODULE 与 6 个函数正交。✓

### 事实 6: 两个新模块 exact 4+4=8 定义

Controller (lines 57-59):
> `_write_config_helpers.py` exact 4 个函数，`_write_params_validation.py` exact 4 个函数，合计 exact 8 个定义。

源码验证:
- `_write_config_helpers.py` 定义: `_resolve_write_model_override_name`, `_resolve_write_company_name`, `_build_write_run_config`, `_log_write_preflight_result` = **4**
- `_write_params_validation.py` 定义: `_challenger_requested`, `_validate_challenger_run_plan_args`, `_validate_live_smoke_plan_args`, `_validate_research_materialization_args` = **4**
- 合计 **8**

注: `_write_params_validation.py` 还 import 了 `validate_live_smoke_budget_limits` 和 `validate_live_smoke_chapter_name`（来自 `dayu.services.write_model_live_smoke_plan`），这些是 import 不是定义，不计入。

**判定**: **PASS** — exact 4+4=8，与 Controller 声明一致。✓

---

## 2. 精确实施契约五项逐项验证

### 契约 1: 两个模块定义不变

Controller (lines 114-115):
> `_write_config_helpers.py` 与 `_write_params_validation.py` 各 exact 4 个函数、合计 exact 8 个定义，定义与函数语义均不变。

§1 事实 6 已验证。**PASS** ✓

### 契约 2: write.py import 精简为 6+MODULE

Controller (lines 116-120):
> - 从 `_write_config_helpers` import `MODULE` 与 config 4 个函数；
> - 从 `_write_params_validation` 只 import `_challenger_requested` 与 `_validate_research_materialization_args`；
> - 不 import/re-export 两个无 caller validator。

目标 import block:
```python
from dayu.cli.commands._write_config_helpers import (
    MODULE,
    _build_write_run_config,
    _log_write_preflight_result,
    _resolve_write_company_name,
    _resolve_write_model_override_name,
)
from dayu.cli.commands._write_params_validation import (
    _challenger_requested,
    _validate_research_materialization_args,
)
```

当前 write.py lines 20-25 仍有 2 个待删 alias。Erratum 正确描述了目标状态。**PASS** ✓

### 契约 3: engine test live-smoke import 迁移

Controller (lines 121-123):
> `tests/engine/test_cli_running_config.py` 对 `_validate_live_smoke_plan_args` 的 direct import 改从 `_write_params_validation` 获取。

源码验证:
- 当前: `test_cli_running_config.py:26` — `from dayu.cli.commands.write import ... _validate_live_smoke_plan_args`
- 目标: `from dayu.cli.commands._write_params_validation import _validate_live_smoke_plan_args`
- lines 2436/2444 的调用 `_validate_live_smoke_plan_args(...)` 无需修改（函数对象不变）

**PASS** ✓

### 契约 4: dispatch identity 断言缩减为 6

Controller (lines 124-125):
> `tests/application/test_write_cli_dispatch.py` 的 identity 断言只覆盖 6 个 functional binding。

源码验证:
- 当前 8 个 identity 断言 (lines 588-619): 4 config_helpers + `_challenger_requested` + `_validate_challenger_run_plan_args` + `_validate_live_smoke_plan_args` + `_validate_research_materialization_args`
- 目标 6 个: 4 config_helpers + `_challenger_requested` + `_validate_research_materialization_args`
- 删除: `_validate_challenger_run_plan_args` identity (lines 608-610) + `_validate_live_smoke_plan_args` identity (lines 612-615)

**PASS** ✓

### 契约 5: _validate_challenger_run_plan_args 无旧 direct test

Controller (lines 126-127):
> `_validate_challenger_run_plan_args` 没有旧 direct test，不新建 `write.py` 兼容路径。

grep 全库确认: 无任何测试文件从 `write.py` import `_validate_challenger_run_plan_args`。唯一引用在 `test_write_cli_dispatch.py:609-610`（identity 断言，将被删除）和 `_write_params_validation.py`（定义 + 内部调用）。

**PASS** ✓

---

## 3. 不变门禁六项验证

| 门禁 | Controller 声明 | 源码证据 | 判定 |
|---|---|---|---|
| setup_model_name patch | 旧 0、新 13 | `grep -c` 旧=0, 新=13 | **PASS** ✓ |
| MODULE 唯一定义 | 仅 `_write_config_helpers.py` | `grep` = 1 处 (line 12) | **PASS** ✓ |
| 双 owner | write run-path / params validator-path | write import `_challenger_requested` + `_write_params_validation` 定义；line 5934 patch `write._challenger_requested` | **PASS** ✓ |
| line 5934 | write owner patch 保持 | `test_cli_running_config.py:5932-5936` patch `write_command_module._challenger_requested` | **PASS** ✓ |
| Slice 8 exact 13 | Challenger 组 13 函数不变 | Plan §5 Slice 8 lines 948-964 列出 13 个 | **PASS** ✓ |
| coverage/pyright/ruff | 门禁要求（非当前状态声明） | 实施后验证 | **GATE** — 待实施后测量 |

---

## 4. 替代方案裁决一致性

Controller (lines 142-150) 列出 6 种 REJECTED 方案。与前序 review §2 评估对照:

| 方案 | Controller 裁决 | 前序 review 裁决 | 一致? |
|---|---|---|---|
| `__all__` | REJECTED | REJECTED (§2.1) | ✓ |
| 普通 unused import | REJECTED | REJECTED (§2.2 equivalent) | ✓ |
| 同名 alias | REJECTED | 当前状态，REJECTED (§2.4) | ✓ |
| `noqa` / dummy usage | REJECTED | REJECTED (§2.2/§2.3) | ✓ |
| 保留旧 direct import/identity | REJECTED | REJECTED (§2.4) | ✓ |
| 扩大迁移范围 | REJECTED | 未显式评估（隐含 REJECTED） | ✓ |

**判定**: 全部一致。✓

---

## 5. Adversarial 残余不一致搜索

### 5.1 Master plan stale "8 write binding" 残留

Controller (lines 83-84):
> Master plan 所有当前规范性 "8 个 write private import/direct path/identity" 表述均改为 [6-binding 表述]

**限制**: 本 reviewer 未直接读取 v4.7 master plan 文本（会话限制）。无法独立确认 master plan 中不存在 stale "8 个" 规范性表述。

**风险**: **L** — 如果 master plan 有任何遗漏的 "8 个" 规范性文本，将导致 plan 自相矛盾。Controller 声明已全量更新，但未经独立验证。

**建议**: 下一轮 review 应直接 `grep -n '8 个.*private\|8 个.*import\|8 个.*binding' docs/plans/2026-08-07-cli-write-architecture-refactor.md` 确认零残留。

### 5.2 DeepSeek review 措辞纠正

Controller (lines 97-110) 纠正了 DeepSeek review 中 `_write_params_validation.py` 的 "exact 8 个符号 + 4 个内部 detail" 措辞，声明正确契约是 "exact 4 个函数"。

**分析**: Controller 明确说 "不修改外部 review artifact"（line 109-110）。这意味着 DeepSeek review 文件本身保留错误措辞，仅由 Controller artifact 覆盖。这不是不一致——是版本化治理（Controller 裁决覆盖 review 原始措辞）。但未来读者如果只看 DeepSeek review 不看 Controller，会被误导。

**风险**: **L** — 信息碎片化风险，非功能风险。Controller artifact 的存在足以 resolve。

### 5.3 "两路 focused review" 引用

Controller (line 19-22) 引用:
- `plan-review-20260808-023020.md`（DeepSeek）
- `plan-review-20260808-023021.md`（MiMo = 本 reviewer）

本 reviewer 只能验证自己的 review (023021) 的 findings 与 Controller 声明一致（H=0, M=2, L=1）。DeepSeek review (023020) 未经本 reviewer 阅读。

**风险**: **L** — 如果 DeepSeek review 的 HML 与 Controller 声明不一致，Controller 的 "两路均确认" 断言不成立。但无反面证据。

### 5.4 Ruff delta 门禁措辞

Controller (line 139):
> Ruff F/I 与 HEAD 全规则 finding-code multiset delta 0

**分析**: "HEAD" 指 git HEAD commit（Slice 5 实施前的基线）。当前 working tree 有 PLC0414×2（Slice 5 引入）。v4.7 fix 删除这 2 个后，delta vs HEAD = 0。措辞正确。

但 line 78-79 的措辞 "Ruff F/I 与 HEAD 全规则 finding-code multiset delta 门禁保持不变" 可能被误读为 "delta 值保持不变"（实际意思是 "门禁规则本身保持不变"）。这是措辞模糊，非事实错误。

**风险**: **L** — 措辞歧义，不影响实施。

### 5.5 write.py 当前状态 vs 目标状态

当前 write.py lines 20-25 仍有 2 个待删 alias。Controller artifact 描述的是目标状态，不是当前状态。erratum 作为 "PLAN ERRATUM APPLIED" 但代码尚未修改——这是正确的（"production、tests 与 implementation artifact 全部冻结"，line 12-13）。

**判定**: 非不一致。Controller 正确区分了 plan 目标与实施冻结。✓

---

## 6. Findings 状态汇总

### 6.1 前序 review (023021) findings 闭环

| ID | 级别 | 描述 | Controller 裁决 | 新状态 |
|---|---|---|---|---|
| v4.7-F01 | **M** | compat re-export 违规 | ACCEPT (lines 63-72) | **CLOSED** |
| v4.7-F02 | **M** | PLC0414×2 lint | ACCEPT (lines 74-79) | **CLOSED** |
| v4.7-F03 | **L** | plan 声明 "8 个" 不准确 | ACCEPT (lines 81-95) | **CLOSED** |

前序 review 3 个 findings 全部被 Controller ACCEPT 并给出精确实施契约。**3/3 CLOSED** ✓

### 6.2 本 review 新增 findings

| ID | 级别 | 描述 | 建议 |
|---|---|---|---|
| v4.7r2-F01 | **L** | Master plan 文本中 "8 个" 规范性表述的零残留未经独立验证 | 下一轮 review grep 确认 |
| v4.7r2-F02 | **L** | DeepSeek review (023020) 的 HML 与 Controller 声明一致性未经独立验证 | 下一轮 review 阅读 023020 |
| v4.7r2-F03 | **L** | Controller line 78-79 "delta 门禁保持不变" 措辞模糊（可误读为 delta 值不变） | 建议 Controller 措辞微调为 "门禁规则不变" |

---

## 7. HML 汇总

| 级别 | 数量 | 说明 |
|---|---|---|
| **H** | **0** | — |
| **M** | **0** | 前序 2 个 M 全部 CLOSED |
| **L** | **3** | v4.7r2-F01 (master plan grep 验证), v4.7r2-F02 (DeepSeek review 交叉验证), v4.7r2-F03 (措辞模糊) |

---

## 8. Final Verdict

**PASS**

**理由**: Controller artifact 的六项源码事实、五项精确实施契约、六项不变门禁全部与本 reviewer 独立采集的源码/测试证据一致。前序 review 的 3 个 findings (M×2, L×1) 全部被 Controller ACCEPT 并给出精确实施契约。6 个 functional private binding 的逐 caller 验证通过；两个新模块 exact 4+4=8 定义确认；两个 compat re-export 的禁止确认；engine live-smoke import 迁移路径确认；6 个 identity 断言的精确范围确认；setup13/MODULE/dual owner/line5934/Slice8 exact13 全部不变确认。

3 个新增 Low 均为信息完整性限制（master plan 文本未直接读取、DeepSeek review 未交叉验证、措辞歧义），不影响实施正确性。v4.7 erratum 可进入实施。

**建议**: 实施后执行 Controller 门禁矩阵全部验证命令，特别是 `ruff check --select F,I,PLC` 确认 PLC0414=0 和 `grep -n '8 个.*private' docs/plans/2026-08-07-cli-write-architecture-refactor.md` 确认零残留。

---

*本 review 仅做 plan artifact 层面的 adversarial verification，不修改 plan/code/tests/已有 artifact，不 commit/push。*
