# Plan Review — v5.0 CANDIDATE Final Independent Re-Review

- **审查人**: MiMo
- **日期**: 2026-08-08 06:10:04
- **基线**: clean HEAD `b55f794`（Slice 6 accepted commit，`write.py` 3096 行）
- **计划版本**: v5.0 CANDIDATE（Controller 已修订初审 M1/L2 findings）
- **初审**: `docs/reviews/plan-review-20260808-061002-mimo.md`（PASS with M1/L2）
- **审查范围**: 逐项验证 7 项指定核查点；以当前 plan 文件与 HEAD `b55f794` 源码为准
- **修改**: 无（read-only 审查）

---

## 1. Verdict

**PASS** — plan v5.0 CANDIDATE 全部 7 项核查点通过，初审 M1/L2 已正确修订。

Open findings: **High=0, Medium=0, Low=1**。

---

## 2. 逐项 Closure

### Item 1: Manual Recovery exact 14 runners + gate check = exact 15

**结论: ✅ PASS**

源码 `write.py` 定义：

| # | 函数 | 行号 |
|---|---|---|
| 1 | `_run_write_model_configuration_manual_recovery_evidence` | 579 |
| 2 | `_run_write_model_configuration_manual_recovery_plan` | 644 |
| 3 | `_run_write_model_configuration_manual_recovery_approval` | 718 |
| 4 | `_run_write_model_configuration_manual_recovery_application` | 791 |
| 5 | `_run_write_model_configuration_manual_recovery_verification` | 896 |
| 6 | `_run_write_model_configuration_manual_recovery_clearance` | 980 |
| 7 | `_run_write_model_configuration_manual_recovery_clearance_revocation` | 1085 |
| 8 | `_run_write_model_configuration_manual_recovery_restart` | 1176 |
| 9 | `_run_write_model_configuration_manual_recovery_gate_check` | 1260 |
| 10 | `_run_write_model_configuration_manual_recovery_gate_verification` | 1336 |
| 11 | `_run_write_model_configuration_manual_recovery_gate_revalidation` | 1440 |
| 12 | `_run_write_model_configuration_manual_recovery_audit_timeline` | 1553 |
| 13 | `_run_write_model_configuration_manual_recovery_incident_dossier` | 1646 |
| 14 | `_run_write_model_configuration_manual_recovery_incident_dossier_revalidation` | 1771 |
| 15 | `_check_write_model_configuration_manual_recovery_gate` | 1888 |

计数 = 14 个 `_run_*` runner + 1 个 `_check_*` gate check = **15**。

plan §5 Slice 7 写 "exact 15 definitions"，§2.1 写 "exact 14 manual recovery runners + Phase C gate check（exact 15 definitions）"。**与源码一致**。✅

初审 M1（"12 个 runner" → "14 个 runner"）已修订。✅

### Item 2: `_write_execution.py` exact 2; write.py 删除旧 17 定义 + import exact 17

**结论: ✅ PASS**

**`_write_execution.py` exact 2 定义**:

| 函数 | 行号 |
|---|---|
| `_run_write_stage` | 197 |
| `_run_write_preflight` | 242 |

plan §5 Slice 7 item 1 写 "exact 2 definitions"。✅

**write.py 删除旧 17 定义**: 2 execution + 14 manual runner + 1 gate check = 17。plan §5 Slice 7 item 3 写 "删除...旧定义，旧定义 AST 合计必须为 0"。✅

**write.py 从真实 owner import exact 17 functional symbols**:

| 来源模块 | 导入符号 | 数量 |
|---|---|---|
| `_write_execution` | `_run_write_stage`, `_run_write_preflight` | 2 |
| `_write_manual_recovery` | 14 个 `_run_write_model_configuration_manual_recovery_*` + `_check_write_model_configuration_manual_recovery_gate` | 15 |
| **合计** | | **17** |

plan §5 Slice 7 item 3 写 "从两个新 owner 正常顶层 import exact 17 个 functional symbols"。✅

**禁止兼容 re-export/glue**: plan 明确 "不是 compatibility re-export"、"禁止 compatibility wrapper/re-export 或 DI/glue seam"。✅

### Item 3: HEAD b55f794 真实范围/owner 一致

**结论: ✅ PASS**

| 验证项 | plan 声明 | 源码实际 | 一致 |
|---|---|---|---|
| write.py 行数 | 3096（§1.1） | 3096 | ✅ |
| 手动恢复 runner 数 | 14（§1.2） | 14 | ✅ |
| Phase C gate | 1（§1.2） | 1 | ✅ |
| Challenger 函数数 | 13（§1.2） | 13 | ✅ |
| Phase A dispatch 条目 | 14（§1.3） | 14 | ✅ |
| Phase B dispatch 条目 | 2（§1.3） | 2 | ✅ |
| `build_snapshot_builder` call in write.py | 4（Slice 6 后） | 4（lines 526, 843, 932, 1032） | ✅ |
| 已迁出模块 | config_helpers(4+MODULE), params_validation(4), config_application(2), snapshot_builder(2) | 同 | ✅ |

### Item 4: Snapshot factory 随 owner 迁移; labels 为 default/verification/clearance; import DAG 单向无环

**结论: ✅ PASS**

**三处 snapshot factory 随 owner 迁移**:

| Runner | 当前行号 | `run_label` | 迁入模块 |
|---|---|---|---|
| `_run_write_model_configuration_manual_recovery_application` | 843 | 默认（`"configuration-application"`） | `_write_manual_recovery` |
| `_run_write_model_configuration_manual_recovery_verification` | 932 | `"configuration-manual-recovery-verification"` | `_write_manual_recovery` |
| `_run_write_model_configuration_manual_recovery_clearance` | 1032 | `"configuration-manual-recovery-clearance"` | `_write_manual_recovery` |

plan §5 Slice 7 item 2 写 "label 精确保持：application 默认、verification 为 `configuration-manual-recovery-verification`、clearance 为 `configuration-manual-recovery-clearance`"。✅

**Slice 7 后 write.py 剩余 `build_snapshot_builder` call**: 仅 `_run_write_model_configuration_rollback`（line 526，默认 label）= exact 1。plan §5 Slice 7 item 3 写 "`write.py` 只为 Slice 8 rollback runner 保留 `build_snapshot_builder` 默认-label import/call exact 1"。✅

**Import DAG 验证**:

```
_write_manual_recovery.py → _write_snapshot_builder.py（build_snapshot_builder import）
_write_snapshot_builder.py → _write_config_application.py（_build_fresh_application_routing_snapshot import）
_write_config_application.py → ✗ 不 import _write_snapshot_builder 或 _write_manual_recovery
_write_snapshot_builder.py → ✗ 不 import _write_manual_recovery 或 write.py
_write_manual_recovery.py → ✗ 不 import write.py 或 _write_config_application
```

源码验证：
- `_write_snapshot_builder.py:9` — `from dayu.cli.commands._write_config_application import (...)` ✅
- `_write_config_application.py` — 无 `_write_snapshot_builder` import ✅
- 无循环路径 ✅

plan §5 Slice 7 item 2 写 "CLI 内部依赖只能为 `_write_manual_recovery → _write_snapshot_builder → _write_config_application`"。✅

### Item 5: Direct runner/dependency patches 迁真实 owner; dispatch characterization 继续 patch write functional binding

**结论: ✅ PASS**

plan §5 Slice 7 item 4 测试 ownership 规则：

| 规则 | 验证 |
|---|---|
| 直接调用 execution runner 的测试迁到 `_write_execution` | plan 明确 ✅ |
| 直接调用 manual runner / Phase C gate 的测试迁到 `_write_manual_recovery` | plan 明确 ✅ |
| 验证 `run_write_command` dispatch 短路顺序的 characterization 继续 patch `write.<symbol>` | plan 明确 "继续 patch `write.<symbol>` 正常功能 binding" ✅ |
| 禁止 blanket 把全部旧 patch path 迁走 | plan 明确 "禁止 blanket 把全部旧 patch path 迁走" ✅ |
| 禁止为测试新增 compatibility seam | plan 明确 "禁止为测试新增 compatibility seam" ✅ |
| identity 回归：write.py 17 个 binding 与新 owner 函数对象相同 | plan 明确 "exact 17 个 functional bindings 分别与两个新 owner 中的函数对象相同" ✅ |

### Item 6: Coverage≥80 / pyright=0 / Ruff no-new / AST/import/identity/README gates

**结论: ✅ PASS**

plan §5 Slice 7 item 5 门禁：

| 门禁 | plan 要求 | 验证 |
|---|---|---|
| `_write_execution.py` 定义 exact 2 | ✅ | |
| `_write_manual_recovery.py` 定义 exact 15 | ✅ | |
| write.py 旧定义 exact 0 | ✅ | |
| 两个新 owner 合计 exact 17 | ✅ | |
| write.py 从它们 import exact 17 | ✅ | |
| manual `build_snapshot_builder` import exact 1 | ✅ | |
| manual `build_snapshot_builder` call exact 3 | ✅ | |
| manual labels: default / verification / clearance | ✅ | |
| write.py factory call 剩余 exact 1（rollback 默认） | ✅ | |
| 三个相关模块 nested `_snapshot_builder` 定义为 0 | ✅ | |
| manual → snapshot import exact 1 | ✅ | |
| snapshot → config-application 既有 import exact 1 | ✅ | |
| manual → write、snapshot → manual、config-application → manual 均为 0 | ✅ | |
| 逐文件 coverage ≥ 80% | plan 要求 ✅ | |
| pyright 0 errors | plan 要求 ✅ | |
| Ruff no-new | plan 要求 ✅ | |
| README 同步 | plan 要求按触发规则 ✅ | |

### Item 7: Phase A adapter/dispatch/Protocol、Slice 8 exact 13 等其余语义无意外改变

**结论: ✅ PASS**

| 验证项 | 状态 |
|---|---|
| Phase A 14 adapter（§11e）与源码 14 个 runner 一一对应 | ✅ |
| Phase A adapter 9/10/14 惰性 `_build_execution_options` 与源码一致 | ✅（lines 2581, 2593, 2641） |
| Phase B 2 adapter 读 `config_ctx.execution_options` 不重算 | ✅（§11e 明确） |
| Phase 常量表（§11d）14 + 2 条目 | ✅ |
| `WriteDispatchArguments` Protocol（§11a）11 bool + 3 path + 5 other = 19 fields | ✅ |
| `_WriteCommandContext` / `_WriteConfigurationContext`（§11b）frozen dataclass | ✅ |
| Slice 8 exact 13 Challenger 函数 | ✅（源码 13 个定义） |
| Slice 8 `_write_config_rollback.py` 接收 `_run_write_model_configuration_rollback` | ✅ |
| Phase C-H 顺序逻辑不变 | ✅ |
| `print_report` 副作用顺序不变（步骤 1 先于步骤 3） | ✅（§1.4） |
| `_challenger_requested` 双 owner 契约不变 | ✅（§1.8） |

---

## 3. 初审 Findings 修订验证

| 初审 Finding | 严重度 | v5.0 修订状态 | 验证 |
|---|---|---|---|
| M1: Slice 7 描述 "12 个 runner" 应为 "14" | Medium | ✅ 已修订 | §5 Slice 7 现写 "exact 15 definitions" + 逐名列出 14 个 runner；§1.2 写 "手动恢复 14 runner"；§2.1 写 "exact 14 manual recovery runners + Phase C gate check（exact 15 definitions）" |
| L2: §1.2 "手动恢复 12 runner" 与 §1.3 矛盾 | Low | ✅ 已修订 | §1.2 现写 "手动恢复 14 runner" |
| L3: Slice 7 描述未逐名列出 14 个 runner | Low | ✅ 已修订 | §5 Slice 7 item 2 现逐名列出全部 14 个 runner + 1 个 gate check |

---

## 4. 新发现

### Finding 1 — LOW: §1.3 行号引用指向旧 5262 行版本，非当前 3096 行 HEAD

**位置**: plan §1.3 run_write_command phase 边界

**问题**: §1.3 引用的行号（如 Phase A 起点 "行 4636"、Phase B 起点 "行 4809"、Phase C "行 4823" 等）指向旧 5262 行版本的 write.py，非当前 HEAD `b55f794` 的 3096 行版本。当前实际行号约为 Phase A 起点 ~2470、Phase B 起点 ~2643、Phase C ~2658。审计命令 `awk -F: '$1 >= 4637 && $1 <= 4822'` 在当前文件上不会匹配任何行。

**影响**: 无实际影响。plan 明确声明 "边界描述以函数调用锚点和首末 predicate 字段为准，行号仅作辅助证据"，且标注 "HEAD 5821014 取证" 表明这些是历史证据。函数名和 predicate 字段锚点全部正确，不影响 Slice 7 实现。

**建议**: 可选——在 §1.3 注明行号来自 Slice 0-6 前的 HEAD 5821014，当前 HEAD `b55f794` 行号已变化但结构锚点不变。或在实现后改用结构测试验证 phase 表条目数（plan 已提及此意图）。

---

## 5. 最终判定

**PASS** — plan v5.0 CANDIDATE 全部 7 项核查点通过，初审 3 项 findings 全部正确修订。1 项新 Low finding 不阻塞实施。

Open findings: **High=0, Medium=0, Low=1**（§1.3 历史行号引用，不影响实现）。

---

## 6. 完整 Closure 矩阵

| 核查项 | 结果 | 证据 |
|---|---|---|
| 1. manual recovery exact 14 + gate = 15 | ✅ | 源码 14 `_run_*` + 1 `_check_*` = 15; plan "exact 15 definitions" |
| 2. execution exact 2; write.py 删旧 17 + import 17 | ✅ | 源码 2 execution; plan "exact 2" + "exact 17 functional symbols" |
| 3. HEAD b55f794 范围/owner 一致 | ✅ | 3096 行、14 runner、1 gate、13 challenger、4 factory call 全部匹配 |
| 4. snapshot factory labels + import DAG | ✅ | default/verification/clearance; manual→snapshot→config-app 单向无环 |
| 5. patch ownership 规则 | ✅ | direct→real owner; dispatch→write binding; 禁止 blanket 迁移 |
| 6. coverage/pyright/Ruff/AST/README gates | ✅ | plan 门禁完整且正确 |
| 7. Phase A adapter/dispatch/Protocol + Slice 8 13 | ✅ | 14 adapter、14+2 phase 表、13 challenger 函数全部匹配 |
