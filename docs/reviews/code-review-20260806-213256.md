# Code Re-review

## Scope

- Mode: current changes re-review
- Branch or PR: `codex/dual-model-research-mvp`
- Base: `HEAD`
- Output file: `docs/reviews/code-review-20260806-213256.md`
- Included scope: DR-001 fix、`utils/validate_handoff_docs.py`、相关验收测试与 DeepSeek fix artifact
- Excluded scope: 当前分支既有已提交历史
- Parallel review coverage: 无

## Finding Status

- `DR-001`: 已修复。Codex 独立验收结果为 `3 passed`，Ruff 通过，Pyright 为 `0 errors, 0 warnings, 0 informations`。

## Findings

### DR-002-已修复-[中]-统一输出方案使用仓库明确禁止的 object 类型

- **入口/函数**: `_SupportsStrWrite.write`
- **文件(行号)**: `utils/validate_handoff_docs.py:15`
- **输入场景**: 对当前切片执行项目指令检查与严格类型边界审查
- **实际分支**: 为匹配 typeshed 的私有 `_print_message` 参数协议，新增 `Protocol.write(...) -> object`
- **预期行为**: `AGENTS.md` 明确要求新增或修改签名不得使用 `object`、`Any` 或其他无法严格检查的类型；实现应选择无需突破该约束的脱敏边界
- **实际行为**: 生产代码直接引入 `object` 返回类型，且 fix artifact 把它描述为满足严格类型安全
- **直接证据**: `utils/validate_handoff_docs.py:15` 的返回注解为 `object`；`AGENTS.md`“编码硬约束”明确写明“禁止使用 object、Any、无类型参数、无类型返回值”
- **影响**: 当前实现违反项目不可放宽的代码契约，不能进入 accepted checkpoint；Protocol 还把仅为适配 argparse 私有实现的宽类型引入项目所有权范围
- **建议改法和验证点**: 移除 `_SupportsStrWrite` 与所有 `object`/`Any`/type-ignore。优先在本类拥有且严格为 `str` 的 `prog` 边界建立脱敏不变量，使 argparse 的 help、usage、error 都只消费已脱敏 prog；错误 message 继续复用现有 `redact_secret_shapes`。也可采用其他无需禁用类型的方案。三项验收、Ruff、Pyright 必须继续全绿
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

## Open Questions

- 无。

## Residual Risk

- `format_help()` 返回值不经输出句柄时是否也需要保证脱敏，取决于当前 parser 的安全契约；当前仓库未发现直接消费该返回值的调用路径。
- DeepSeek fix artifact 自报 Pyright 退出码为 `1`，但 Codex 用相同命令独立运行得到退出码 `0`；后续 fix artifact 必须记录真实结果。

## Final Re-review Status

- `DR-001`: 已修复。help、直接 `format_help()`、prog/error 和原有 parser-error 四项验收由 Codex 独立运行，结果 `4 passed`。
- `DR-002`: 已修复。第三次 fix 移除了 `_SupportsStrWrite`、`object`、私有 `_print_message` 和不兼容的 `prog` property，改用严格类型的 `format_usage()`、`format_help()`、`error()` 公开扩展点。
- Codex 独立门禁：focused pytest 退出码 `0`，Ruff 退出码 `0`，`pyright utils/validate_handoff_docs.py` 退出码 `0` 且 `0 errors`。
- 第二次 property fix 因 `reportIncompatibleVariableOverride` 被拒绝；最终权威实现与证据记录在 `docs/review/2026-08-06-redacting-argument-parser-prog-agentds-fix-3.md`。
- 当前切片 code re-review 通过。

## Final Integration Validation

- DeepSeek 已独立审核
  `1c6bdc99f32b01577c991eb840c0f28ef6f3eb7a..b7dedc3c7b649e7e3b44a2120f65e460276598dc`；
  artifact 为 `docs/reviews/code-review-20260806-220027.md`。其唯一 Low finding
  经 Codex 裁决为 `rejected-with-reason`，因为各层复用同一个幂等脱敏函数，
  且内层函数也是可直接调用的 Python API。
- MiMo 已对同一精确三提交范围完成第二路独立审核；artifact 为
  `docs/reviews/code-review-20260807-061404.md`。MiMo 首版 artifact 使用错误
  base，Codex 证据裁决后要求原地纠正；最终结论为 `PASS`、范围内
  `Findings: None`。
- 三个仓库门禁全部退出 `0`：
  `utils.validate_handoff_docs`、`utils.codex_review_gate --allow-waiting`、
  `utils.dual_model_pipeline_check`。
- 五个相关测试文件完整运行，结果 `699 passed`，退出 `0`。
- Ruff 针对当前变更相关基础规则
  `E4,E7,E9,F,I,UP035` 通过；DeepSeek 修复了本切片新增的导入顺序和
  `Sequence` 来源问题。未把测试文件的既有 FLY/ISC lint 债务混入本切片。
- Pyright `1.1.408` 对生产文件和相关测试返回
  `0 errors, 0 warnings, 0 informations`。
- `git diff --check` 对生产、测试和 review artifacts 通过。

## Final Decision

`accepted`。当前 parser-prog 修复及其双路独立审核证据可以进入本地 checkpoint。
