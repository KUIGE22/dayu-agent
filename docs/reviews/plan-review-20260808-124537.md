# Plan Re-Review: Slice 11 v5.5 — Phase E/F/H Erratum Verification

**Reviewer**: planreview (re-review, v5.5 candidate)
**Base**: 8c63d80 (clean)
**Plan**: `docs/plans/2026-08-07-cli-write-architecture-refactor.md` v5.5 CANDIDATE
**Erratum**: `docs/reviews/plan-slice-11-v5.5-phase-efh-erratum-20260808-codex.md`
**Prior reviews**: DeepSeek 122456 (FAIL, 1H+1M+1L), MiMo 122457 (FAIL, 1H+2M+2L obs)
**Target**: `dayu/cli/commands/write.py` (763 lines, HEAD 8c63d80)
**Date**: 2026-08-08

---

## Verdict: **PASS** — v5.5 is code-generation-ready; all prior findings CLOSED

---

## Checklist Verification

### 1. Phase A/B bounded only

**v5.5 §11f**: "Slice 11 的可执行改动边界只有两部分：1. 用 11d/11e 的phase tables 与 adapters 替换 HEAD Phase A 的 14 个顶层 `if` 和 Phase B 的 2 个顶层 `if`；2. 对 Write Protocol exact20 字段的既有 selector 读取改为直接属性访问。"

**HEAD evidence**: write.py lines 215-323 contain the 14 Phase A `if` blocks and 2 Phase B `if` blocks. Lines 324-763 are Phase C–H. The plan's fenced replacement code replaces only the Phase A/B region.

**Verdict**: **PASS** — bounded replacement scope is correctly delimited.

---

### 2. Phantom helper: current contract / source 0

**v5.5 §11f**: "禁止新建、导入或调用任何用于封装 Phase E 的新 helper。" Phase E fenced code is now the full inline block from HEAD.

**Evidence**:
- `rg '_build_challenger_run_plan_if_required'` across repo: 0 hits in production code, 0 hits in plan §11f. Only historical references in v5.4 changelog and prior review artifacts.
- v5.5 §11f Phase E fenced code: 29 lines, matches HEAD lines 337-369 character-for-character.
- Erratum: "当前 Slice 11 contract 中 phantom helper 引用：0；生产源码定义/调用：0。"

**Verdict**: **PASS** — phantom eliminated, inline preserved, prohibition explicit.

---

### 3. Phase E inline: required / build / boundary / 5 exceptions

**v5.5 §11f Phase E fenced code** vs **HEAD write.py lines 337-369**:

| Element | v5.5 plan | HEAD | Match |
|---------|-----------|------|-------|
| `challenger_run_plan` init | `dict[str, Any] \| None = None` | same | ✓ |
| `challenger_run_plan_required` | `any(...)` over 4 fields | same 4 fields | ✓ |
| 4 field names | `routing_challenger_run_plan_output`, `_approval_request`, `_approval_output`, `_approval_input` | same | ✓ |
| `getattr` usage | `getattr(args, attribute, "")` | same | ✓ |
| Builder call | `_build_challenger_run_plan_from_args(args, paths_config, write_model_override_name)` | same | ✓ |
| Boundary assert | `_assert_challenger_run_output_boundaries(plan, workspace_dir)` | same | ✓ |
| Exception types | `FileExistsError, FileNotFoundError, OSError, TypeError, ValueError` (5) | same 5 | ✓ |
| Error log | `f"Challenger 双跑计划无效: {exc}"` | same | ✓ |
| Return code | `return 2` | same | ✓ |

**Verdict**: **PASS** — Phase E inline block is a verbatim copy of HEAD.

---

### 4. Phase F: direct selector + fail-loud assert

**v5.5 §11f Phase F fenced code**:
```python
if bool((args.routing_challenger_run_approval_input or "").strip()):
    assert challenger_run_plan is not None
    run_approval_exit_code = _verify_and_consume_challenger_run_approval_before_host(...)
```

**HEAD write.py lines 370-387**:
```python
if bool(str(getattr(args, "routing_challenger_run_approval_input", "") or "").strip()):
    assert challenger_run_plan is not None
    run_approval_exit_code = _verify_and_consume_challenger_run_approval_before_host(...)
```

| Element | v5.5 plan | HEAD | Match |
|---------|-----------|------|-------|
| Selector field | `routing_challenger_run_approval_input` | same | ✓ |
| Access method | direct `args.*` (authorized: exact20 field in Write Protocol) | `getattr` (defensive) | ✓ (authorized change) |
| `str()` wrapper | omitted (redundant with Protocol-typed `str \| None`) | present (needed for `getattr` → `Any`) | ✓ (safe simplification) |
| `assert` | `assert challenger_run_plan is not None` | same | ✓ |
| Guard pattern | assert-first, not `plan is not None and ...` | same | ✓ |
| Verification call | `_verify_and_consume_challenger_run_approval_before_host` | same | ✓ |
| Exit code | `run_approval_exit_code` | same | ✓ |

**v5.5 explicit prohibition**: "禁止把 `assert` 改为 `challenger_run_plan is not None and ...` 的 fail-silent guard。新增真实不变量测试必须让 approval selector 非空、first build 返回 `None`、boundary stub 不抛异常，并断言 Phase F 触发 `AssertionError`。"

**Verdict**: **PASS** — fail-loud invariant preserved; selector change is authorized and safe.

---

### 5. Phase H: 三组异常 + partial-success + write_exit_code

**v5.5 §11f Phase H contract** describes three groups. HEAD verification:

**Group 1 — plan rebuild (HEAD lines 693-716)**:
```python
if challenger_run_plan is not None:
    try:
        current_plan = _build_challenger_run_plan_from_args(...)
    except (FileNotFoundError, OSError, TypeError, ValueError) as exc:
        Log.error(f"Challenger 双跑计划复核失败: {exc}", module=MODULE)
        return 2
    if current_plan != challenger_run_plan:
        Log.error("Challenger 双跑计划在 Host 初始化后发生变化", module=MODULE)
        return 2
```

| Element | v5.5 plan | HEAD | Match |
|---------|-----------|------|-------|
| Exception types | 4: `FileNotFoundError, OSError, TypeError, ValueError` | same 4 | ✓ |
| Error log | `"Challenger 双跑计划复核失败: {exc}"` | same | ✓ |
| Comparison | `current_plan != challenger_run_plan` | same | ✓ |
| Mismatch log | `"Challenger 双跑计划在 Host 初始化后发生变化"` | same | ✓ |
| Return code | `return 2` (both paths) | same | ✓ |

**Group 2 — challenger-config ValueError (HEAD lines 582-588, 717-724)**:
- Preflight path: `except ValueError as exc: Log.error(str(exc), module=MODULE); return 2` ✓
- Main path: `except ValueError as exc: Log.error(str(exc), module=MODULE); return 2` ✓

**Group 3 — materialize Exception (HEAD lines 744-758)**:
```python
except Exception as exc:  # noqa: BLE001
    Log.error(f"研究工件 materialize 失败: {type(exc).__name__}: {exc}", module=MODULE)
    return 2
```

| Element | v5.5 plan | HEAD | Match |
|---------|-----------|------|-------|
| Exception type | `Exception` (BLE001 preserved) | same | ✓ |
| Error log | `f"研究工件 materialize 失败: {type(exc).__name__}: {exc}"` | same | ✓ |
| Return code | `return 2` (partial-success) | same | ✓ |

**Variable name**: v5.5 mandates `write_exit_code`. HEAD uses `write_exit_code` throughout (lines 685-741). ✓

**Call timing**: v5.5 says "Phase E/H 的 `_build_challenger_run_plan_from_args` 调用时序仍为 Host 初始化前一次、需要复核时 Host 初始化后一次；未请求计划时为 0。" HEAD: Phase E line 349 (before host init), Phase H line 695 (after host init, conditional). ✓

**Verdict**: **PASS** — all three exception groups, partial-success semantics, variable names, and call timing match HEAD.

---

### 6. exact20 vs table-out getattr

**v5.5 §11f direct-access boundary table**:

| Phase | Direct attribute (exact20) | HEAD access | Match |
|-------|---------------------------|-------------|-------|
| Phase A predicates | 11 bool + 3 path | `getattr` in if-chain | ✓ (authorized) |
| Phase B predicates | 2 bool | `getattr` in if-chain | ✓ (authorized) |
| Phase C | `args.summary` | `getattr(args, "summary", False)` | ✓ (authorized) |
| Phase D | `args.preflight_only` | `getattr(args, "preflight_only", False)` | ✓ (authorized) |
| Phase F | `args.routing_challenger_run_approval_input` | `getattr(args, ..., "")` | ✓ (authorized) |
| Phase G | `args.summary`, `args.reprice_costs` | `getattr` | ✓ (authorized) |
| Phase H | `args.preflight_only` | `getattr` | ✓ (authorized) |

**Table-out fields** (remain `getattr`): `output`, routing/proposal/config-change paths, `infer`, `materialize_research`, preflight/rollback I/O — all confirmed present in HEAD with `getattr`.

**v5.5 prohibition**: "不得把这些字段加入 `WriteDispatchArguments` 或 `DayuCliArguments`，也不得 blanket 改成直接访问。Dayu 终态仍为 exact21 AnnAssign。"

**Verdict**: **PASS** — exact20 boundary is correctly scoped; table-out fields preserved.

---

### 7. 16 matrix / AST / order / call-count / coverage gates

| Gate | v5.5 spec | HEAD reference | Match |
|------|-----------|----------------|-------|
| Phase table lengths | exact14 / exact2 | 14 Phase A + 2 Phase B if-blocks | ✓ |
| Adapter count | exact14 / exact2 | 14 Phase A + 2 Phase B adapters | ✓ |
| Lambda runner | 0 | all adapters are named functions | ✓ |
| `_build_execution_options` call count | Phase A lazy 9/10/14 each exact1; Phase B 0; shared build exact1 | HEAD: clear/verify/recover each call it; 2 Phase B adapters don't; 1 shared call after Phase A | ✓ |
| `run_write_command` order | A loop → resolve → shared build → B context → B loop → C–H | HEAD lines 215-763 | ✓ |
| Phase E AST | must match `git show 8c63d80` subtree | verified character-for-character | ✓ |
| Phase F AST | direct selector + assert | verified | ✓ |
| Phase H AST | three exception groups + `write_exit_code` | verified | ✓ |
| Arguments AST | Research exact1, Write exact20, Dayu exact21 | 1 + 20 = 21 | ✓ |
| Phantom-zero | 0 production Phase E helper definitions/calls | verified via rg | ✓ |
| 16-action matrix | G1 (11 bool) + G2 (3 path) + G3 (2 bool) | matches Protocol field count | ✓ |
| Coverage | `>=80%` for modified files | gate defined | ✓ |
| pyright / Ruff | 0 errors, positive delta 0 | gate defined | ✓ |

**Verdict**: **PASS** — all gates are defined and consistent with HEAD.

---

## Prior Findings Disposition

### MiMo 122457 findings (v5.4 FAIL)

| ID | Severity | Description | v5.5 status | Disposition |
|----|----------|-------------|-------------|-------------|
| H-01 | HIGH | `_build_challenger_run_plan_if_required` phantom — "不变" claim false | §11f Phase E is now verbatim HEAD inline; phantom helper eliminated; prohibition explicit | **CLOSED** |
| M-01 | MEDIUM | Phase F `assert` → guard semantic change | §11f Phase F retains `assert`; guard pattern explicitly prohibited; invariant test mandated | **CLOSED** |
| M-02 | MEDIUM | Phase H error handling omissions (3 try/except blocks missing) | §11f Phase H contract describes all 3 groups with exact exception types, logs, and return codes | **CLOSED** |
| L-01 | LOW | `write_exit` vs `write_exit_code` naming | §11f mandates `write_exit_code`; changelog confirms | **CLOSED** |
| L-02 | LOW | Phase C/D/G access method boundary unclear | §11f has per-phase exact20 direct-access table; table-out fields explicitly protected | **CLOSED** |

### DeepSeek 122456 findings (v5.4 FAIL)

| ID | Severity | Description | v5.5 status | Disposition |
|----|----------|-------------|-------------|-------------|
| DeepSeek 3 | HIGH | Phase E phantom helper | Same as MiMo H-01; CLOSED | **CLOSED** |
| DeepSeek 4 | MEDIUM | Phase F assert → silent guard | Same as MiMo M-01; CLOSED | **CLOSED** |
| DeepSeek 5 | LOW | Phase C/D "不变"措辞不精确 | v5.5 uses "语义不变，exact20 selector 随 Protocol 改为直接属性" | **CLOSED** |

### Overlap with erratum Controller record

The erratum records all 8 findings as `ACCEPTED / CLOSED-BY-PLAN-TEXT` with Controller open H/M/L = 0/0/0. This re-review independently confirms that disposition.

---

## Open Items

**None.** All prior findings are CLOSED. No new findings.

| ID | Severity | Description | Status |
|----|----------|-------------|--------|
| — | — | — | — |

---

## Residual Notes (non-blocking, informational)

1. **Phase E vs Phase H exception count**: Phase E catches 5 types (includes `FileExistsError`); Phase H rebuild catches 4 types (excludes `FileExistsError`). This is intentional — first build is more defensive than rebuild verification. v5.5 correctly documents "5 类异常" for Phase E and "四异常" for Phase H.

2. **Phase F `str()` wrapper**: HEAD uses `str(getattr(..., "") or "").strip()`; v5.5 uses `(args.* or "").strip()`. The `str()` is redundant when the field is typed `str | None` via Protocol. Safe simplification.

3. **Erratum Controller self-review**: The erratum's "自审与验证" section is consistent with this re-review's independent findings.

---

## Final Verdict

**PASS — v5.5 Slice 11 is code-generation-ready.**

The v5.5 plan correctly addresses all findings from DeepSeek 122456 and MiMo 122457. The §11f section has been transformed from a stale full-function sketch into a precise bounded-replacement contract with:
- Exact Phase A/B replacement scope
- Verbatim Phase E inline preservation
- Phase F fail-loud invariant with authorized selector change
- Phase H three-group exception contract with exact types, logs, and return codes
- Explicit prohibitions (no new helpers, no guard replacement, no blanket direct-access)
- Comprehensive gates (AST, call-count, coverage, phantom-zero, stop condition)

Implementation can proceed when v5.5 status transitions from CANDIDATE to ACCEPTED.
