# Aggregate Fix RT-01 Dual Re-Review

- **Review 类型**：deepreview（dual re-review，review-only）
- **日期**：2026-08-08 20:02:26 CST
- **Output file**：`docs/reviews/code-review-20260808-200226.md`
- **Re-review 基线**：`a0c469a`（HEAD，v5.8 ACCEPTED）
- **初审来源**：
  - DeepSeek lane：`docs/reviews/code-review-20260808-195939.md`（PASS，0/0/0）
  - MiMo lane：`docs/reviews/code-review-20260808-115535.md`（PASS，0/0/0）
- **裁决记录**：`docs/reviews/aggregate-fix-rt-exit-code-review-adjudication-20260808-codex.md`
- **实现记录**：`docs/reviews/aggregate-fix-rt-exit-implementation-20260808-codex.md`
- **参考契约**：`docs/plans/2026-08-07-cli-write-architecture-refactor.md` v5.8 `AGG-RT-CTRL-01`

---

## Verdict

**PASS** — open High/Medium/Low = `0/0/0`

初审 accepted findings=0、open H/M/L=0、无需 fix 的裁决正确。代码仍精确满足 v5.8。

---

## Re-Review 复核维度

### 1. 代码状态确认

当前 `git diff HEAD` 与初审时完全一致：

- `dayu/cli/commands/research_template.py`：2 行变更（docstring Returns + `return 0 if result.get("ok") is True else 1`）
- `tests/cli/test_research_template_command.py`：+34 行（新增 `test_run_validate_source_map_command_returns_nonzero_for_template_mismatch`）

Focused 测试复验：`3 passed, 213 deselected`。无 bit-rot、无意外工作树变更、无新增未跟踪文件干扰。

### 2. 两路初审一致性复核

| 维度 | DeepSeek lane (`195939`) | MiMo lane (`115535`) | 一致？ |
|---|---|---|---|
| Verdict | PASS | PASS | ✅ |
| H/M/L | 0/0/0 | 0/0/0 | ✅ |
| 退出码正确性 | PASS，`is True` 对 `bool` 安全 | PASS，同结论 | ✅ |
| stdout 不变 | PASS | PASS | ✅ |
| 异常传播不变 | PASS | PASS | ✅ |
| dispatch/owner/DAG 不变 | PASS | PASS | ✅ |
| docstring 更新正确 | PASS | PASS | ✅ |
| 类型安全 | PASS，pyright 0 errors | PASS，pyright 0 errors | ✅ |
| 测试覆盖 | PASS，真实 dispatch 双路径 | PASS，valid+invalid 对称 | ✅ |
| 覆盖率 >=80% | PASS，80.42% | PASS，80.42% | ✅ |
| scope creep | 无 | 无 | ✅ |
| plan fidelity | 全部 12 项 ✅ | 全部满足 | ✅ |

**结论**：两路初审在所有实质维度上结论完全一致，不存在分歧。

### 3. MiMo-lane Observation 复核

MiMo lane 指出 `_run_validate_source_map` 的 `return 0 if result.get("ok") is True else 1` 省略了部分 sibling validator（`_run_validate_bundle`、`_run_validate_monitoring_plan`、`_run_validate_workbook_report`、`_run_validate_scheduler_manifest`）使用的 `isinstance(validation, dict) and` 守卫。

**Re-review 确认**：这不是 defect。

- `_run_validate_research_workbook`（line 497）使用与 RT-01 完全相同的 `result.get("ok") is True` 无守卫模式 — 证明两种风格在代码库中并存，属于既有设计差异。
- `validate_monitoring_source_map_payload` 是静态类型 `-> dict[str, object]`，单一返回路径恒返回 `dict`，`isinstance` 守卫在此上下文中是 dead check。
- v5.8 `AGG-RT-CTRL-01` 精确指定了表达式 `return 0 if result.get("ok") is True else 1`，省略守卫是 contract-compliant，不是遗漏。
- MiMo lane 已正确将其定性为 non-blocking observation，未提出 finding。Re-review 确认该定性正确。

### 4. Controller 裁决复核

裁决记录 `aggregate-fix-rt-exit-code-review-adjudication-20260808-codex.md` 声明：

- Controller accepted findings：`0`
- Controller rejected findings：`0`
- Controller deferred findings：`0`
- Controller needs-more-evidence findings：`0`
- Controller open High/Medium/Low：`0/0/0`
- 无需修改 production/tests/README/plan

**Re-review 确认**：两路初审均未提出任何 finding，裁决记录中 accepted/rejected/deferred/needs-evidence 均为 0 是逻辑必然。裁决结论与初审证据一致，无错误、无遗漏。

### 5. v5.8 契约复验

逐项对照 `AGG-RT-CTRL-01`（plan lines 2655-2690）：

| 契约项 | 代码状态 | 初审覆盖 |
|---|---|---|
| Return 精确表达式 | ✅ | 两路均确认 |
| docstring Returns 同步 | ✅ | 两路均确认 |
| JSON stdout 不变 | ✅ | 两路均确认 |
| dispatch/action key 不变 | ✅ | 两路均确认 |
| 异常集合/传播不变 | ✅ | 两路均确认 |
| invalid 测试 financial/consumer | ✅ | 两路均确认 |
| 真实 dispatch 入口 | ✅ | 两路均确认 |
| 断言 1、ok=False、errors 非空 | ✅ | 两路均确认 |
| valid 回归 consumer/consumer | ✅ | 两路均确认 |
| 不新增 Slice 14 | ✅ | 两路均确认 |
| full suite 重跑 | ✅ | 实现记录已确认 |
| 不修改 README | ✅ | 两路均确认 |

**结论**：v5.8 契约全部满足，初审后无退化。

### 6. 未覆盖风险扫描

| 风险域 | 状态 |
|---|---|
| 初审间矛盾或分歧 | 无 — 两路完全一致 |
| 初审遗漏的代码路径 | 无 — `_run_validate_source_map` 仅两条分支（ok=True/False），均已覆盖 |
| 裁决遗漏的初审 observation | 无 — MiMo 的 isinstance observation 已正确识别并定性，无需裁决动作 |
| 工作树污染 | 无 — `git diff HEAD --stat` 仅两个目标文件 |
| 测试 bit-rot | 无 — focused 3 passed 复验通过 |
| 跨 validate runner 不一致引入新 bug | 无 — 其他 5 个 runner 未修改，且此前已正确实现条件退出码 |
| 环境差异（iCloud vs 本地） | 已在实现记录中关闭，非代码风险 |

---

## 总结

Aggregate Fix RT-01 双路初审均 PASS（H/M/L=0/0/0），Controller 裁决 accepted findings=0、open H/M/L=0/0/0、无需 fix。Re-review 确认：

- 当前代码与初审时完全一致，focused 测试仍然 PASS。
- 两路初审在所有实质维度上结论一致，无矛盾、无遗漏。
- MiMo lane 的 isinstance 观察已正确定性为 non-blocking，不是遗漏 defect。
- Controller 裁决与初审证据一致，逻辑正确。
- v5.8 `AGG-RT-CTRL-01` 全部 12 项契约仍然满足。

**最终 Verdict**：PASS。open High/Medium/Low = `0/0/0`。初审裁决正确，无需 fix。

---

*本 artifact 由 Codex dual re-review gate 产出。状态：FINAL。不进入 commit/PR gate。*
