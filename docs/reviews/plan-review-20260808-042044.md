# Plan Corrective Re-Review: v4.8 — Rv4.8-F01/F02/F03 修复验证与回归审计

- **审阅对象**: v4.8 master plan (`docs/plans/2026-08-07-cli-write-architecture-refactor.md`) + Controller artifact (`docs/reviews/plan-slice-6-v4.8-argument-type-ownership-erratum-20260808-codex.md`)
- **审阅目标**: 验证 041140 re-review 的三项 LOW findings（Rv4.8-F01/F02/F03）已被精确修复；确认修复不引入对 S6/S10/S11 ownership/count/gates 的语义回归；验证所有 prior findings 保持闭环
- **审阅日期**: 2026-08-08 04:20 UTC
- **审阅者**: Claude (planreview skill, final corrective re-review)
- **基线**: prior re-review `docs/reviews/plan-review-20260808-041140.md`（PASS, 3 LOW），Controller artifact 更新版，master plan 更新版
- **审阅范围**: plan lines 89（v4.0 verdict）、549（Slice 0 principle）、965/967（Slice 6 ExecutionOptions import）、以及全部 S6/S10/S11 语义关键段的回归审计
- **不审阅范围**: 代码/测试/配置/README、Slice 1–5/7–9/12–13 细节、Controller prose 质量

---

## 0. 审阅方法

三项 LOW finding 逐条（plan line → Controller 裁决 → plan 修复文本）对照验证。随后对 S6/S10/S11 的 12 项语义关键标记做 targeted grep 回归审计。每项标注判定与证据行号。

---

## 1. Rv4.8-F01 修复验证 — ExecutionOptions "真实 owner" 指称

**Prior 041140**: plan line 965 称 `dayu.execution.options` 为 ExecutionOptions 的 "真实 owner"，但类定义在 `dayu.contracts.execution_options`。

**Controller 裁决** (lines 31–35): ACCEPT / TEXT FIX — "将 `dayu.execution.options` 从'真实 owner'改称'CLI 标准导入路径'，并明确规范定义位于 `dayu.contracts.execution_options`；实际 import 路径不变。"

**Plan 修复**:

| 位置 | 修复前 (041140) | 修复后 |
|---|---|---|
| line 965 | `ExecutionOptions` 从真实 owner `dayu.execution.options` import | `ExecutionOptions` 使用 CLI 标准导入路径 `dayu.execution.options`（规范定义位于 `dayu.contracts.execution_options`） |
| line 967 | `ExecutionOptions` 从真实 owner `dayu.execution.options` import | `ExecutionOptions` 使用 CLI 标准导入路径 `dayu.execution.options`（规范定义位于 `dayu.contracts.execution_options`） |

**判定**: ✅ **FIXED** — "真实 owner" 改为 "CLI 标准导入路径"，补充规范定义位置，import 路径不变。

**回归审计**: `dayu.execution.options` 和 `dayu.contracts.model_config` 两个 import 路径均未变更；`ExecutionOptions` 和 `ModelConfigJsonValue` 类型引用未变；Slice 6 四函数签名未变。

---

## 2. Rv4.8-F02 修复验证 — v4.0 历史判决与 v4.8 CANDIDATE 状态共存

**Prior 041140**: plan line 89 v4.0 verdict "计划达到 code-generation-ready 标准，可进入 implementation phase" 可能与当前 v4.8 CANDIDATE/AWAITING DUAL RE-REVIEW 状态产生误导。

**Controller 裁决** (lines 37–41): ACCEPT / CLARIFY — "在 v4.0 `PASS_WITH_RISKS` 的'可进入 implementation'旁明确这是 v4.0 历史判决，当前状态以顶部 v4.8 gate 为唯一权威。"

**Plan 修复** (line 89):

修复后末尾追加:
> **这是 v4.0 的历史判决；当前 gate 状态始终以本文顶部 v4.8 status 为准。**

**判定**: ✅ **FIXED** — 明确标注历史判决属性，指向 v4.8 status 为唯一权威。原 verdict 文本保留作为历史记录，不删除、不修改判决内容。

**回归审计**: 追加文本为纯说明，不改变 verdict 内容、不修改 §4 不变量、不修改 Slice 序列定义、不修改任何 gate 条件。

---

## 3. Rv4.8-F03 修复验证 — Slice 0 原则声明未排除 `DayuCliArguments`

**Prior 041140**: plan line 549 排除列表为 "Slice 10/11 才引入的 dispatch mapping、adapter、phase table"，未显式列举 `DayuCliArguments`（E-07 建议）。

**Controller 裁决** (lines 43–47): ACCEPT / TEXT FIX — "Slice 0 原则显式排除 Slice 10 的 `DayuCliArguments`/dispatch mapping 与 Slice 11 的 adapter/phase table，与实现级说明一致。"

**Plan 修复** (line 549):

| 修复前 | 修复后 |
|---|---|
| **不引用** Slice 10/11 才引入的 dispatch mapping、adapter、phase table | **不引用** Slice 10 才引入的 `DayuCliArguments` 与 dispatch mapping，也不引用 Slice 11 才引入的 adapter 与 phase table |

**判定**: ✅ **FIXED** — `DayuCliArguments` 显式列入排除范围；Slice 10 与 Slice 11 的责任边界清晰分离（"Slice 10 才引入的...也不引用 Slice 11 的..."）。与 line 568 实现级说明完全一致。

**回归审计**: 排除范围的扩展方向是**收窄**（增加排除项），不会导致 Slice 0 错误引用更多未来类型。line 568 的 `argparse.Namespace` 构造指示不受影响。

---

## 4. 语义回归审计 — S6/S10/S11 关键契约

### 4.1 审计矩阵

| # | 语义标记 | Plan 行号 | 041140 状态 | 当前状态 | 判定 |
|---|---|---|---|---|---|
| 1 | Slice 6 四函数 args 使用 `argparse.Namespace` | 965, 967 | ✅ | ✅ 不变 | ✅ |
| 2 | Slice 6 "不 import `DayuCliArguments`" 显式禁令 | 967 末尾 | ✅ | ✅ 不变 | ✅ |
| 3 | Slice 6 gate: `DayuCliArguments` 引用为 0 | 973 | ✅ | ✅ 不变 | ✅ |
| 4 | Slice 10 "原子新建 `arguments.py`" + "唯一创建 owner" | 1028–1029 | ✅ | ✅ 不变 | ✅ |
| 5 | Slice 10 `DayuCliArguments(argparse.Namespace)` 真实子类 | 1037 | ✅ | ✅ 不变 | ✅ |
| 6 | Slice 10 `parse_args(namespace=DayuCliArguments())` | 1055 | ✅ | ✅ 不变 | ✅ |
| 7 | Slice 10 45+1 计数 "45 个 args 签名（1+39+1+2+2）与 1 个 return" | 1079 | ✅ | ✅ 不变 | ✅ |
| 8 | Slice 10 传播表 6 rows 覆盖全部 | 1070–1077 | ✅ | ✅ 不变 | ✅ |
| 9 | Slice 10 Callable 逆变陈述 | 1084–1088 | ✅ | ✅ 不变 | ✅ |
| 10 | Slice 10 rg/pyright/runtime gates | 1162–1174 | ✅ | ✅ 不变 | ✅ |
| 11 | Slice 11 "只追加 `WriteDispatchArguments`" | 1182–1184, 1878 | ✅ | ✅ 不变 | ✅ |
| 12 | Checklist 20: type ownership timeline | 1894–1898 | ✅ | ✅ 不变 | ✅ |

**全部 12 项语义标记 0 变化、0 回归**。修复仅涉及编辑性文本追加与措辞替换，不触及任何 S6/S10/S11 的 ownership 归属、count 计数、gate 条件、或类型契约。

### 4.2 Controller 端一致性

Controller 新增 §"首次 Re-review 观察裁决"（lines 29–50），其中明确:
> "三项均为文字准确性修复，不改变 S6/S10/S11 合同。" (lines 49–50)

Controller 状态更新为 "REVIEW OBSERVATIONS FIXED / AWAITING FINAL DUAL RE-REVIEW" (line 7)。

### 4.3 Footer 一致性

Plan footer (line 1938) 更新为:
> "MiMo 041141 PASS/open0；DeepSeek 041140 核心 PASS 的三项 Low 文字观察均已 ACCEPT/FIXED，等待 final dual re-review。"

与 Controller 的 "REVIEW OBSERVATIONS FIXED / AWAITING FINAL DUAL RE-REVIEW" 一致。

---

## 5. 对抗性检查: 修复引入新问题的可能性

| 潜在风险 | 检查结果 |
|---|---|
| "CLI 标准导入路径" 措辞暗示存在其他（非标准）路径？ | 否 — 仅描述现有惯例，不创造新选择 |
| "规范定义位于" 补充是否引入新的依赖关系？ | 否 — 纯注释，不影响 import 语义 |
| v4.0 verdict 历史标注是否减弱 plan 权威性？ | 否 — 标注澄清历史属性，增加而非减弱 gate 清晰度 |
| Slice 0 排除列表新增 `DayuCliArguments` 是否过度收窄？ | 否 — `DayuCliArguments` 确实在 Slice 10 才引入，排除正确 |
| 三项修复的文本追加是否改变了行号锚定关系？ | 是 — 但语义标记的行号在本文中重新核验，均为当前最新行号 |
| "规范定义位于 `dayu.contracts.execution_options`" 与实际文件路径是否一致？ | ✅ `dayu/contracts/execution_options.py` line 118: `class ExecutionOptions:` — 一致 |

---

## 6. 终态评定

### 6.1 Prior Findings

| ID | 来源 | 严重度 | 状态 |
|---|---|---|---|
| F-01 | 035654 | HIGH | ✅ CLOSED |
| F-02 | 035654 | MEDIUM | ✅ CLOSED |
| F-03 | 035654 | MEDIUM | ✅ CLOSED |
| F-04 | 035654 | LOW | ✅ CLOSED |
| F-05 | 035654 | MEDIUM | ✅ CLOSED |
| Rv4.8-F01 | 041140 | LOW | ✅ **FIXED** (line 965, 967) |
| Rv4.8-F02 | 041140 | LOW | ✅ **FIXED** (line 89) |
| Rv4.8-F03 | 041140 | LOW | ✅ **FIXED** (line 549) |

### 6.2 整体评定

**PASS** — **0 HIGH, 0 MEDIUM, 0 LOW**。

- 三项 Rv4.8-F01/F02/F03 全部精确修复，修复方式为编辑性文本追加/措辞替换，零语义变更。
- S6/S10/S11 的 12 项关键语义标记（ownership/count/gates/contracts）全部不变，零回归。
- Controller artifact 的 "首次 Re-review 观察裁决" 段落准确描述了每项修复。
- Plan footer 与 Controller status 一致。
- Zero new findings 被修复引入。

**v4.8 master plan 可进入 final dual acceptance。** 唯一待办: MiMo 041141 并行 re-review 已 PASS/open0，两路 final re-review 均 PASS → Controller 可推进 accepted plan commit。

---

**审阅完成**。
