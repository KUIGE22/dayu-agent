# Slice 9 v5.1 复审 — Ownership / DAG / 闭合验证

- **审查模型**: MiMo
- **日期**: 2026-08-08 08:20:01
- **基线 HEAD**: `1d0f9e6`（clean）
- **计划版本**: v5.1 CANDIDATE（`docs/plans/2026-08-07-cli-write-architecture-refactor.md`，2420 行）
- **Controller erratum**: `docs/reviews/plan-slice-9-v5.1-ownership-dag-erratum-20260808-codex.md`
- **初审来源**: `docs/reviews/plan-review-20260808-075500-deepseek.md`（FAIL, 3H/3M/3L）、`docs/reviews/plan-review-20260808-075501-mimo.md`（PASS with 3M/1L plan fixes）
- **审查范围**: v5.1 全部声称——83 逐名 exact ownership、DAG、main bindings、__all__、tests、write.py lazy import、Slice10 兼容性、门禁

---

## Verdict: **PASS — open High/Medium/Low = 0/0/0**

全部初审 findings 已在 v5.1 中闭合。Controller 的逐边 AST 分配与独立核验完全一致。

---

## 独立 AST 核验结果

### 1. 83 函数 exact ownership 闭合

| 指标 | Controller 声称 | AST 验证 | 一致 |
|---|---|---|---|
| 总 FunctionDef | 123 | 123 | ✅ |
| `_run_*` runner | 39 | 39 | ✅ |
| entry | 1 | 1 | ✅ |
| 保留（entry + runners） | 40 | 40 | ✅ |
| 需迁移 | 83 | 83 | ✅ |
| 已分配 | 83 | 83 | ✅ |
| missing | 0 | 0 | ✅ |
| extra | 0 | 0 | ✅ |
| overlap | 0 | 0 | ✅ |

### 2. 五模块计数

| 模块 | Controller 声称 | AST 验证 | 一致 |
|---|---|---|---|
| `_research_template_helpers` | 22 | 22 | ✅ |
| `_research_template_core` | 26 | 26 | ✅ |
| `_research_template_bundle` | 12 | 12 | ✅ |
| `_research_template_monitoring` | 11 | 11 | ✅ |
| `_research_template_materialize` | 12 | 12 | ✅ |
| **合计** | **83** | **83** | ✅ |

### 3. helpers 附加定义

| 类型 | Controller 声称 | AST 验证 | 一致 |
|---|---|---|---|
| ClassDef | 2（`ResearchTemplate`, `ResearchTemplateRecommendation`） | 当前在 research_template.py:160,169，迁后归 helpers | ✅ |
| 常量 | 7（`_TEMPLATE_DIR_NAME` 等） | 当前在 research_template.py:54-99，迁后归 helpers | ✅ |

### 4. 主模块终态

| 指标 | Controller 声称 | AST 验证 | 一致 |
|---|---|---|---|
| FunctionDef | 40（1 entry + 39 runners） | 当前 123 − 83 = 40 | ✅ |
| ClassDef | 0 | 当前 2（迁后 0） | ✅ |
| 旧定义残留 | 0 | — | ✅（实施后验证） |

### 5. 跨 owner DAG（10 条边，零 cycle）

Controller 声称的 DAG：
```
core → helpers
bundle → core + helpers
monitoring → bundle + core + helpers
materialize → monitoring + bundle + core + helpers
helpers → zero internal project-domain dependencies
```

**AST 逐边验证**（基于 83 函数的 free-global call graph）：

| 边 | AST 验证 |
|---|---|
| core → helpers | ✅ |
| bundle → core | ✅ |
| bundle → helpers | ✅ |
| monitoring → bundle | ✅ |
| monitoring → core | ✅ |
| monitoring → helpers | ✅ |
| materialize → monitoring | ✅ |
| materialize → bundle | ✅ |
| materialize → core | ✅ |
| materialize → helpers | ✅ |
| **合计** | **10** ✅ |
| **反向边** | **0** ✅ |
| **cycle** | **0** ✅ |

### 6. Main functional bindings（exact 45 = 6/18/6/9/6）

| Owner | Controller 声称 | AST 验证 | 一致 |
|---|---|---|---|
| helpers | 6 | 6 | ✅ |
| core | 18 | 18 | ✅ |
| bundle | 6 | 6 | ✅ |
| monitoring | 9 | 9 | ✅ |
| materialize | 6 | 6 | ✅ |
| **合计** | **45** | **45** | ✅ |

**逐条验证**: 45 个 binding 中每一个都至少被 1 个 retained entry/runner 函数直接调用。未调用的 binding = 0。✅

### 7. 其他 38 个迁移函数

Controller 声称 "其余 38 个迁移函数不得在主模块建立 compatibility binding"。

AST 验证：83 − 45 = **38**。这 38 个函数要么是 private helpers（被其他迁移函数调用，不被 runner 直接调用），要么是 `__all__` 中的 public 函数（被测试直接调用但不被 runner 调用）。禁止 compatibility binding 约束清晰。✅

### 8. `__all__` 终态

| 指标 | Controller 声称 | AST 验证 | 一致 |
|---|---|---|---|
| 当前 `__all__` | 68 | 68 | ✅ |
| 删除 exact | 13 | 13（全部在当前 `__all__` 中） | ✅ |
| 终态 | 55 | 68 − 13 = 55 | ✅ |

删除的 13 项：`ResearchTemplate`、`ResearchTemplateRecommendation`、`build_research_template_bundle_descriptor`、`build_research_template_usage_guide`、`extract_monitoring_variables`、`get_monitoring_data_source_candidates`、`materialize_research_template_bundle`、`materialize_research_bundle_from_write_manifest`、`validate_research_template_bundle_descriptor`、`validate_monitoring_execution_plan`、`validate_monitoring_scheduler_manifest`、`write_research_template_bundle_descriptor`、`write_research_template_usage_guide`。

终态 55 项中：entry 1 + functional imports 44（exact45 中的 44 个 public 函数，`_resolve_materialize_research_target` 是 private 不在 `__all__`） + workbook re-exports 17 − 7（被删项中含 0 个 workbook re-export）= 55。✅

### 9. Tests 迁移

`tests/cli/test_research_template_command.py` 从 `research_template` import 68 个符号，其中 **48 个**是迁移函数。这 48 个 test import 的 patch 路径需按真实 owner 迁移：

- direct-definition tests → 迁到对应 owner 模块的 test 文件或按 owner 分组 import
- entry/runner global lookup characterization → 保留 `research_template.<symbol>` patch

Controller 的 test 迁移规则（§9d）已覆盖：direct-definition/import/dependency patches 跟随真实 owner，只有 entry/runner global lookup characterization 保留 main owner。✅

### 10. `write.py` lazy import

| 指标 | 验证 |
|---|---|
| 当前位置 | `write.py:81`（function-local import） |
| 当前路径 | `dayu.cli.commands.research_template` |
| 新路径 | `dayu.cli.commands._research_template_materialize` |
| Controller 声称 | line 81 ✅ |

### 11. Slice 10 兼容性

Controller §1477 行明确：dataclass 已在 Slice 9 迁至 helpers，Slice 10 不得重复迁移。39 runners + entry 的 `DayuCliArguments` 签名传播不变。✅

### 12. 存量 `object` occurrence 1→1

`_DATA_SOURCE_BINDING_CANDIDATES: dict[str, dict[str, object]]` 在 research_template.py:99 有 `object` 类型注解。迁入 helpers 后 semantic occurrence 1→1，不新增签名、传播或 escape。✅

---

## 初审 Findings 闭合验证

### DeepSeek 初审（075500, FAIL 3H/3M/3L）

| Finding | v5.1 处置 | 闭合 |
|---|---|---|
| H-01: 72≠83 计数缺口 | S9-CTRL-01: exact83, 22/26/12/11/12 | ✅ CLOSED |
| H-02: 无逐函数 ownership | S9-CTRL-01: 逐名列出全部 83 函数 | ✅ CLOSED |
| H-03: dataclass cycle | S9-CTRL-01: 两 dataclass 迁 helpers | ✅ CLOSED |
| M-01: `_print_definition_header` 归属 | 明确归 helpers，计入 exact22 | ✅ CLOSED |
| M-02: tests 迁移欠规格 | S9-CTRL-03: direct-owner/dispatch-owner 分流规则 | ✅ CLOSED |
| M-03: write.py 行号陈旧 | S9-CTRL-04: 改为 line 81 | ✅ CLOSED |
| L-01: `__all__` 未定义 | S9-CTRL-03: 删13留55 | ✅ CLOSED |
| L-02: DAG 未逐边验证 | S9-CTRL-02: 完整逐层 DAG | ✅ CLOSED |
| L-03: runner import 需求未枚举 | S9-CTRL-03: exact45 bindings | ✅ CLOSED |

### MiMo 初审（075501, PASS with 3M/1L）

| Finding | v5.1 处置 | 闭合 |
|---|---|---|
| #1: "100 辅助" 实为 83 | S9-CTRL-01: 全文改为 exact83 | ✅ CLOSED |
| #2: 五模块计数不一致 | S9-CTRL-01: 22/26/12/11/12 exact | ✅ CLOSED |
| #3: dataclass cycle | S9-CTRL-01: 迁 helpers | ✅ CLOSED |
| #4: 存量 `object` | S9-CTRL-01: 1→1 存量豁免 | ✅ CLOSED |

---

## Controller Preliminary Allocation 评估

Controller erratum §2 行 66-70 明确拒绝 DeepSeek 附录的 `30/23/11/10/9` preliminary allocation，理由是把 `_resolve_template_selection_from_write_manifest` 等调用 core 的函数放入 helpers 会形成 helpers→core 反向边。

独立验证：Controller 最终分配中，`_resolve_template_selection_from_write_manifest` 归入 **core**（不是 helpers）。AST 确认该函数调用 `recommend_research_templates`（core 内部），若放入 helpers 会形成 helpers→core 反向边。Controller 的分配正确。✅

---

## 门禁可行性

| 门禁 | 可行性 |
|---|---|
| AST 83 函数 exact 22/26/12/11/12 | ✅ 已验证闭合 |
| AST main FunctionDef 40, ClassDef 0 | ✅ |
| AST 旧定义 exact0 | ✅（实施后验证） |
| DAG 10 边零 cycle | ✅ 已验证 |
| identity exact45 | ✅（实施后验证） |
| compat binding exact0 | ✅（实施后验证） |
| `__all__` exact55 | ✅ 已验证 |
| import smoke | ✅（实施后验证） |
| write.py lazy owner exact1 | ✅ |
| pyright 0 errors | ✅（实施后验证） |
| Ruff F/I delta 0 | ✅（实施后验证） |
| 逐文件 coverage ≥80% | ✅（实施后验证） |

---

## 残余风险

| 风险 | 严重度 | 说明 |
|---|---|---|
| 83 函数单 commit 迁移量大 | 中 | 但 Controller 已逐名锁定、DAG 已验证、门禁已完备 |
| 48 个 test import 需迁移 | 中 | Controller §9d 已给出分流规则，实施者按规则执行即可 |

---

## 总结

v5.1 的 Slice 9 计划已达到 code-generation-ready 标准：

1. **83 函数 exact ownership 闭合**: missing/extra/overlap 均为 0，22/26/12/11/12 与 AST 完全一致。
2. **DAG 10 边零 cycle**: 逐边 AST 验证通过，helpers 零内部 project-domain dependency。
3. **dataclass 迁 helpers**: 消除 import cycle，ClassDef main 终态 0。
4. **exact45 functional bindings**: 6/18/6/9/6 与 AST 一致，其他 38 无 compat。
5. **`__all__` 删13留55**: 与当前 68 闭合。
6. **write.py lazy import**: line 81，新路径 `_research_template_materialize`。
7. **Slice 10 兼容性**: dataclass 已在 helpers，runner 传播不变。
8. **初审 9+4 findings 全部 CLOSED**。

Controller open High/Medium/Low = 0/0/0。
