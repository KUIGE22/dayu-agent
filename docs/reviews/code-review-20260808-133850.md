# Slice 11 Write Dispatch Code Re-Review（Dual Re-Review）

- **日期**: 2026-08-08
- **基线**: `122acd8 gateflow: accept cli write architecture plan v5.5`
- **分支**: `codex/dual-model-research-mvp`
- **Gate**: Slice 11 code review dual re-review（复核 Controller adjudication）
- **Accepted plan**: v5.5 ACCEPTED / DUAL PLAN RE-REVIEW PASS，S11-CTRL-01/02
- **初审来源**:
  - DeepSeek：`docs/reviews/code-review-20260808-132706.md`
  - MiMo：`docs/reviews/code-review-20260808-132707.md`
- **Controller 裁决**: `docs/reviews/slice-11-code-review-adjudication-20260808-codex.md`
- **Reviewer**: DeepSeek（independent re-review）
- **状态**: CODE REVIEW PASS / DUAL RE-REVIEW PASS

---

## 任务

独立验证 Controller 对 9 条 findings（DS F-10 + MiMo F-01–F-08）的逐项
`rejected-with-reason` 裁决是否有直接证据。不修改 production/tests/README/plan/
implementation/adjudication/source reviews。

---

## 验证方法

对每条 finding 执行以下验证：

1. 从源码/plan/测试中提取直接证据（AST、文本匹配、plan contract 引用）
2. 与 Controller 裁决理由交叉核对
3. 判定 CLOSED 或发现裁决矛盾

---

## 逐项验证

### DS F-10 — rejected-with-reason / non-defect → CLOSED ✓

**Controller 理由**: 三层证据互补——AST 结构门禁、真实 parser 集成测试、测试 wrapper
文档化设计。删除任一 Protocol 字段会先由 exact-union 测试明确失败。

**独立验证**:

| 证据层 | 存在 | 位置 |
|---|---|---|
| AST exact union 测试 | ✓ | `test_protocol_and_dayu_fields_form_exact_union`（`test_write_cli_dispatch.py:1293`） |
| Protocol exact20 字段 | ✓ | `arguments.py:19-41`，AST 验证 20 AnnAssign |
| Dayu exact21 = Research(1) + Write(20) | ✓ | `arguments.py:44-67`，AST 验证 21 AnnAssign，union 精确相等 |
| 真实 parser 集成测试 | ✓ | `test_parse_arguments_returns_real_dayu_namespace`（`test_write_cli_dispatch.py:2304`） |
| Wrapper 文档化 | ✓ | `_run_write_command_with_complete_dispatch_args` docstring 明确说明"真实 argparse parser 会写入全部 Write Protocol 字段" |

**判定**: Controller 裁决有充分直接证据。DS F-10 → **CLOSED / non-defect**。

---

### MiMo F-01 — rejected-with-reason / pre-existing / out-of-scope → CLOSED ✓

**Controller 理由**: Phase F `assert challenger_run_plan is not None` 在 HEAD 已存在；
S11-CTRL-02 明确要求保留；改为显式异常会改变 accepted behavior。

**独立验证**:

| 证据 | 结果 |
|---|---|
| S11-CTRL-02 锁定 assert | ✓ Master plan 明确："Phase F 保留 fail-loud `assert challenger_run_plan is not None`" |
| 当前代码有 assert | ✓ `write.py:631`：`assert challenger_run_plan is not None` |
| HEAD 同样使用 assert | ✓ MiMo reviewer 自认："HEAD 原代码同样使用 assert，此问题在 Slice 11 之前已存在" |
| Erratum 确认 | ✓ `plan-slice-11-v5.5-phase-efh-erratum` line 46: "保留 `assert challenger_run_plan is not None`" |

**判定**: Controller 裁决有充分直接证据。MiMo F-01 → **CLOSED / pre-existing / out-of-scope**。

---

### MiMo F-02 — rejected-with-reason / pre-existing / out-of-scope → CLOSED ✓

**Controller 理由**: Phase H materialize broad `except Exception` 是 HEAD 已有 partial-success
边界；S11-CTRL-02 明确要求保留该异常组。

**独立验证**:

| 证据 | 结果 |
|---|---|
| S11-CTRL-02 锁定 Exception | ✓ Master plan: "materialize `Exception` partial-success `return 2`" |
| 当前代码保留 | ✓ `write.py:1002`：`except Exception as exc:  # noqa: BLE001` |
| HEAD pre-existing | ✓ MiMo reviewer 自认："HEAD 原代码使用同样写法" |
| Erratum 确认 | ✓ "Phase H 保留三组异常边界与 partial-success exit 2" |

**判定**: Controller 裁决有充分直接证据。MiMo F-02 → **CLOSED / pre-existing / out-of-scope**。

---

### MiMo F-03 — rejected-with-reason / non-defect → CLOSED ✓

**Controller 理由**: fixture-Protocol 同步已由 exact union AST 测试、真实 parser 集成测试和
direct-call runtime fixture 共同保护；新增等价字段枚举不提升 correctness signal。

**独立验证**:

| 证据 | 结果 |
|---|---|
| Exact union AST 测试存在 | ✓ `test_protocol_and_dayu_fields_form_exact_union` 验证 Protocol exact20 + Dayu exact21 + union 相等 |
| 真实 parser 集成测试存在 | ✓ `test_parse_arguments_returns_real_dayu_namespace` |
| `_make_write_args` 含全部 20 Protocol 字段 | ✓ 代码审查确认 base dict 逐字段列出 |

**判定**: Controller 裁决有充分直接证据。现有 AST 门禁已覆盖字段同步。MiMo F-03 → **CLOSED / non-defect**。

---

### MiMo F-04 — rejected-with-reason / pre-existing / out-of-scope → CLOSED ✓

**Controller 理由**: Summary 路径 optional-path 解析样板来自 HEAD；v5.5 锁定 table-out 字段
继续使用 `getattr`；V4-07 已 REJECTED。

**独立验证**:

| 证据 | 结果 |
|---|---|
| S11-CTRL-02 锁定 table-out getattr | ✓ Master plan: "所有 table-out argparse 字段与动态 Phase E inventory 保持 HEAD `getattr`" |
| V4-07 REJECTED | ✓ Master plan §3: "V4-07 (Phase G Path 重复模式) — REJECTED — 忠实现状，不在本 task scope" |
| Summary 路径 27 个 getattr 无 Protocol 泄漏 | ✓ AST 验证：27 个 getattr 字段名均为 table-out，leaked=∅ |

**判定**: Controller 裁决有充分直接证据。MiMo F-04 → **CLOSED / pre-existing / out-of-scope**。

---

### MiMo F-05 — rejected-with-reason / non-defect → CLOSED ✓

**Controller 理由**: 测试已用 phase table exact adapter identity/order 与 16-action runtime
matrix 锁定完整 runner 集合；mock helper 维护成本是未来编辑成本，非当前 correctness defect。

**独立验证**:

| 证据 | 结果 |
|---|---|
| Adapter identity/order 测试 | ✓ `test_phase_tables_bind_exact_adapter_order` 验证 14+2 adapter 精确顺序 |
| Group 1（11 bool A）参数化 | ✓ `TestWritePhaseTableGroup1PhaseABool` 11 参数化 case |
| Group 2（3 path A）参数化 | ✓ `TestWritePhaseTableGroup2PhaseAPath` 3×2 参数化 |
| Group 3（2 bool B）参数化 | ✓ `TestWritePhaseTableGroup3PhaseBBool` 2 参数化 |
| Mixed priority | ✓ `TestWritePhaseTableMixedPriority` |
| 每个 case 验证 other runners 零调用 | ✓ 断言 `call_counts[other_key] == 0` |

**判定**: Controller 裁决有充分直接证据。16-action runtime matrix 已覆盖完整 runner 集合。
MiMo F-05 → **CLOSED / non-defect**。

---

### MiMo F-06 — rejected-with-reason / non-defect → CLOSED ✓

**Controller 理由**: Reviewer 自身明确该差异是 accepted design；逐路径测试锁定 build count。

**独立验证**:

| 证据 | 结果 |
|---|---|
| Reviewer 自认非 bug | ✓ MiMo F-06: "这不是 bug——设计意图是只有需要 execution_options 的 runner 才构造" |
| Reviewer 自认可接受 | ✓ MiMo F-06: "可接受当前设计" |
| Build count 测试 | ✓ `test_clear_verify_recover_call_build_execution_options_inline`（build=1） |
| | ✓ `test_path_runners_do_not_call_build_execution_options`（build=0） |
| | ✓ `test_apply_rollback_use_precomputed_execution_options`（build=1） |

**判定**: Controller 裁决有充分直接证据。MiMo F-06 → **CLOSED / non-defect**。

---

### MiMo F-07 — rejected-with-reason / non-defect → CLOSED ✓

**Controller 理由**: Slice 10 已有真实 `parse_arguments()` 返回 Dayu namespace 的集成测试；
engine corpus 覆盖全面 parser 行为；Slice 11 无需复制 parser 参数矩阵。

**独立验证**:

| 证据 | 结果 |
|---|---|
| Slice 10 parser 测试存在 | ✓ `test_parse_arguments_returns_real_dayu_namespace`（`test_write_cli_dispatch.py:2304`） |
| Reviewer 承认覆盖存在 | ✓ MiMo F-07: "test_cli_running_config.py 已有更全面的 parse_arguments 覆盖" |
| Slice 11 scope 不含 parser 测试扩展 | ✓ S11-CTRL-02 仅涉及 Phase A/B replacement + Phase E/F/H 保留 |

**判定**: Controller 裁决有充分直接证据。MiMo F-07 → **CLOSED / non-defect**。

---

### MiMo F-08 — rejected-with-reason / non-defect → CLOSED ✓

**Controller 理由**: Reviewer 自身认可 adapter 是统一 typed context callable 的 accepted v5.5
架构边界；exact16 adapter 与 identity/order/matrix 门禁已验证；不是 compatibility wrapper。

**独立验证**:

| 证据 | 结果 |
|---|---|
| Reviewer 自认可接受 | ✓ MiMo F-08: "可接受当前设计" |
| Adapter 非 compatibility wrapper | ✓ 每个 adapter 解包 typed context → keyword args，有实质语义转换 |
| 3 个 adapter 有额外逻辑 | ✓ clear/verify/recover adapter 内联 `_build_execution_options` |
| 架构依据 | ✓ v5.5 plan: Phase A/B dispatch 使用 typed context + adapter pattern |
| Identity/order 门禁 | ✓ `test_phase_tables_bind_exact_adapter_order` |

**判定**: Controller 裁决有充分直接证据。MiMo F-08 → **CLOSED / non-defect**。

---

## 最终 Findings 映射

| Finding | 最终状态 | 关闭理由 | 证据类型 |
|---|---|---|---|
| DS F-10 | **CLOSED** | non-defect | AST gate + parser test + documented design |
| MiMo F-01 | **CLOSED** | pre-existing / out-of-scope | HEAD assert + S11-CTRL-02 locked |
| MiMo F-02 | **CLOSED** | pre-existing / out-of-scope | HEAD broad except + S11-CTRL-02 locked |
| MiMo F-03 | **CLOSED** | non-defect | Exact union AST test exists |
| MiMo F-04 | **CLOSED** | pre-existing / out-of-scope | HEAD pattern + V4-07 REJECTED |
| MiMo F-05 | **CLOSED** | non-defect | 16-action runtime matrix exists |
| MiMo F-06 | **CLOSED** | non-defect | Reviewer self-acknowledged |
| MiMo F-07 | **CLOSED** | non-defect | Slice 10 parser coverage exists |
| MiMo F-08 | **CLOSED** | non-defect | Reviewer self-acknowledged |

## 汇总

| 指标 | 值 |
|---|---|
| Accepted findings | **0** |
| Open HIGH | **0** |
| Open MEDIUM | **0** |
| Open LOW | **0** |
| 总 CLOSED | **9/9** |
| 需要 fix | **0** |
| 新 blocker | **0** |
| Controller 裁决矛盾 | **0** |

## 结论

**CODE REVIEW PASS / DUAL RE-REVIEW PASS**。

Controller 对全部 9 条 findings 的 `rejected-with-reason` 裁决均有独立可复现的直接证据
支撑，无一依赖间接推断或假设。每条裁决的关键证据均可从源码、accepted plan contract 或
reviewer 自述中直接提取，无需额外解释。

- 3 条因 **pre-existing / out-of-scope** 关闭：MiMo F-01（HEAD assert）、F-02（HEAD broad
  except）、F-04（HEAD summary boilerplate + V4-07 REJECTED）——均被 S11-CTRL-02 明确锁定。
- 3 条因 **reviewer self-acknowledged** 关闭：MiMo F-06（"这不是 bug"）、F-07（"已有覆盖"）、
  F-08（"可接受当前设计"）。
- 3 条因 **现有门禁已覆盖** 关闭：DS F-10（AST + parser test + doc）、MiMo F-03（exact
  union test）、F-05（16-action runtime matrix）。

当前 WIP 无 accepted finding、无 fix、无新 blocker。冻结状态与 Controller 裁决一致。
可进入 accepted commit gate。
