# Deep Review Artifact — code-review-20260807-061404

**Base**: `1c6bdc99f32b01577c991eb840c0f28ef6f3eb7a` (fix: redact secrets in validation reports)
**Head**: `b7dedc3` (fix: redact cli parser errors)
**Review date**: 2026-08-07T06:14:04+08:00
**Scope**: base 的 exclusive diff 到 HEAD，即 `1c6bdc9..b7dedc3`，仅含 3 次提交，仅限只读分析
**Reviewers**: 3 parallel sub-agents (redaction chain, architecture compliance, refactoring quality) + main synthesis

---

## Commits Under Review

| Hash | Message | Files Changed |
|------|---------|---------------|
| `b8b9f17` | fix: block secrets in task generation | `utils/prepare_deepseek_task.py`, `tests/test_prepare_deepseek_task.py` |
| `bfc4801` | fix: redact plain text gate reports | `utils/dual_model_pipeline_check.py`, `tests/test_dual_model_pipeline_check.py` |
| `b7dedc3` | fix: redact cli parser errors | `utils/validate_handoff_docs.py`, `utils/codex_review_gate.py`, `utils/prepare_deepseek_task.py`, `utils/dual_model_pipeline_check.py` |

---

## Findings

**None.** 在 `1c6bdc9..b7dedc3` 的 3 次提交范围内未发现 material findings。

逐提交分析：

- **`b8b9f17`**（block secrets in task generation）：新增 `_validate_secret_shape_fields` 在 spec 校验阶段阻断 secret 输入，新增 `_redact_report_value` 覆盖 `main()` 所有 CLI 输出路径。逻辑正确，测试覆盖充分（`test_prepare_deepseek_task.py` 新增 redaction 测试）。无新增问题。
- **`bfc4801`**（redact plain text gate reports）：新增 `_redact_check_result` 对 `dual_model_pipeline_check` 的 check name 和 details 双重脱敏，覆盖 plain stdout 和 JSON 输出。逻辑正确，测试覆盖充分。无新增问题。
- **`b7dedc3`**（redact cli parser errors）：在 `validate_handoff_docs` 中新增 `RedactingArgumentParser`（继承 `argparse.ArgumentParser`，重写 `error()` 方法脱敏），4 个工具模块的 `main()` 函数统一替换。逻辑正确，单一定义点，4 处引用。无新增问题。

**结论：PASS — 3 次提交均为正确扩展已有 redaction 链路，无新引入的缺陷或风险。**

---

## Output Path Coverage

以下为 `1c6bdc9..b7dedc3` 范围内新增或变更的输出路径：

| # | 输出路径 | 脱敏方式 | Commit |
|---|---------|---------|--------|
| 1 | `prepare_deepseek_task.validate_spec` issues | `redact_secret_shapes` per issue | `b8b9f17` |
| 2 | `_validate_secret_shape_fields` 不回显原值 | 输入阻断 | `b8b9f17` |
| 3 | `_normalize_worktree_baseline_values` issues | `redact_secret_shapes` per issue | `b8b9f17` |
| 4 | `prepare_deepseek_task.main()` spec invalid error | `_redact_report_value` | `b8b9f17` |
| 5 | `prepare_deepseek_task.main()` validation failed | `_redact_report_value` per issue | `b8b9f17` |
| 6 | `prepare_deepseek_task.main()` render/write failed | `_redact_report_value` | `b8b9f17` |
| 7 | `prepare_deepseek_task.main()` write success | `_redact_report_value` | `b8b9f17` |
| 8 | `prepare_deepseek_task.main()` rollback report | `_redact_report_value` per issue | `b8b9f17` |
| 9 | `dual_model_pipeline_check.run_pipeline_check` | `_redact_check_result` per result | `bfc4801` |
| 10 | `to_jsonable_results` JSON 输出 | `_redact_check_result` per result | `bfc4801` |
| 11 | `_print_results` plain stdout | `_redact_check_result` per result | `bfc4801` |
| 12 | `validate_handoff_docs.main()` argparse error | `RedactingArgumentParser` | `b7dedc3` |
| 13 | `codex_review_gate.main()` argparse error | `RedactingArgumentParser` | `b7dedc3` |
| 14 | `dual_model_pipeline_check.main()` argparse error | `RedactingArgumentParser` | `b7dedc3` |
| 15 | `prepare_deepseek_task.main()` argparse error | `RedactingArgumentParser` | `b7dedc3` |

---

## Pre-existing Observations (Outside Scope)

> 以下观察来自对更大范围（`a38c1a6..b7dedc3`）的分析，但均在 base `1c6bdc9` 之前引入或跨越 base 边界，不阻塞本次结论。

### PO-1 [Medium] SECRET_KEY_PATTERN 仅覆盖 sk- 前缀密钥格式（pre-existing）

**Location**: `utils/validate_handoff_docs.py:17`
**Introduced**: `a38c1a6`（base 之前）

正则 `r"(?<![A-Za-z0-9_])sk-[A-Za-z0-9_-]{20,}"` 只匹配 OpenAI 风格 `sk-` 前缀密钥，不匹配 `ghp_`/`AKIA`/`AIza` 等格式。模块 docstring 未说明设计范围。当前风险可控（单一 LLM provider），但建议补充注释。

### PO-2 [Medium] Secret redaction 测试缺少边界用例（pre-existing）

**Location**: 所有 redaction 测试文件
**Introduced**: `a38c1a6`..`1c6bdc9`（base 之前或 base 本身）

测试统一使用 `"sk-" + ("A" * 20)`，缺少长度边界（19 vs 20 字符）、前缀边界（`ask-` vs `task-sk-`）、字符集边界（含 `_` 和 `-`）用例。建议补充。

### PO-3 [Low] _redact_review_result 在输出路径中重复调用（pre-existing）

**Location**: `utils/codex_review_gate.py:127,144,717`
**Introduced**: `a38c1a6`（base 之前）

`run_review_gate()` 返回前脱敏一次，`to_jsonable_result()` 和 `_print_report()` 又各脱敏一次。幂等无害，属于代码气味。

---

## Open Questions

### OQ-1 SECRET_KEY_PATTERN 的覆盖范围是否需要扩展？

当前仅覆盖 `sk-` 前缀。若项目未来接入 AWS Bedrock、Azure OpenAI（使用不同 key 格式），需扩展 pattern。建议在产品层面确认：短期内是否只使用 OpenAI API？

### OQ-2 _redact_review_result 的防御性重复是否保留？

当前 `run_review_gate` 返回前脱敏一次，`to_jsonable_result` 和 `_print_report` 又各脱敏一次。两种策略各有取舍：
- 保留重复：调用方无需关心脱敏，防御性强
- 去掉重复：单一出口原则，代码更清晰

建议团队选择一种策略并统一执行。

### OQ-3 validate_handoff_docs.py 是否需要拆分？

当前 3219 行，同时承担路径策略、Markdown 解析、证据分类、secret redaction 等职责。是否需要拆分为子模块？这不在本次 review 范围内，但值得后续讨论。

---

## Residual Risk

> 以下为仓库历史债务，非 `1c6bdc9..b7dedc3` 引入，不阻塞本次结论。

### RR-1 [High] fins → engine 跨层反向依赖

`dayu/fins/` 中 **76 处**直接 import `dayu/engine/` 内部实现，跨 49 个文件。被依赖的 engine 模块包括 `Source`、`ProcessorRegistry`、`text_utils`、`ToolArgumentError`、`ToolRegistry`、`tool_contracts`。根因是这些共享类型本应下沉到 `contracts` 层。

### RR-2 [High] God object / God function

116 个超长函数（>100 行）、25 个超长类（>500 行）。最严重的是 `cli/commands/write.py::_validate_research_materialization_args`（1849 行）和 `host/host.py::Host`（1757 行）。

### RR-3 [Medium] prepare_deepseek_task.py 访问 21 处 validate_handoff_docs 私有 API

`prepare_deepseek_task.py` 中 21 处直接调用 `validate_handoff_docs._*` 私有函数（`_normalize_evidence_line`、`_display_scope_path`、`_scope_paths_overlap` 等）。这是 refactoring 链路（d15dd86..4ded8aa）的遗留项。

### RR-4 [Medium] dayu/ 目录 Any/object 类型安全

`dayu/` 目录下 `: Any` 约 242 处、`: object` 约 281 处。`utils/` 下状况良好（`Any` 3 处、`object` 22 处）。

### RR-5 [Medium] 存储层合规

Processor 直接 glob XBRL 文件（`sec_processor.py:746` 等）、SEC 下载缓存/限流状态直接文件 I/O。违反"财报文档存取必须且只能通过 `dayu.fins.storage` 下的仓储协议"约束。

### RR-6 [Low] 3 处标准库无理由延迟导入

`host/host.py:935`（`import time`）、`fins/downloaders/cninfo_downloader.py:1059`（`import hashlib`）、`:1067`（`import datetime as dt`）。

### RR-7 [Medium] SECRET_KEY_PATTERN 仅覆盖 sk- 前缀（pre-existing, a38c1a6 引入）

见 PO-1。正则不匹配 `ghp_`/`AKIA`/`AIza` 等常见 secret 格式，模块 docstring 未说明设计范围。

### RR-8 [Medium] Secret redaction 测试缺少边界用例（pre-existing, a38c1a6..1c6bdc9 引入）

见 PO-2。长度边界、前缀边界、字符集边界均未被测试覆盖。

### RR-9 [Low] _redact_review_result 重复调用（pre-existing, a38c1a6 引入）

见 PO-3。幂等无害，代码气味。

---

## Not Covered

| 项目 | 原因 |
|------|------|
| pyright 类型检查 | 未验证（需 venv 激活） |
| 测试运行 | 未验证（需 venv 激活） |
| README 同步检查 | `1c6bdc9..b7dedc3` 的 3 次提交未触发 README 更新规则（均在 `utils/` 下） |
| 非 redaction 相关的存量代码 | 不在本次 scope 内 |
| performance / concurrency 正确性 | 不在本次 scope 内 |

---

## Verification Commands

以下为本次 review 执行的验证命令（均在只读模式下运行）：

```bash
# 查看 base..HEAD 提交范围（确认仅含 3 次提交）
git log --oneline 1c6bdc9..b7dedc3
# 退出码: 0
# 输出:
# b7dedc3 fix: redact cli parser errors
# bfc4801 fix: redact plain text gate reports
# b8b9f17 fix: block secrets in task generation

# 统计变更文件
git diff 1c6bdc9..b7dedc3 --stat
# 退出码: 0

# 验证 RedactingArgumentParser 定义点（b7dedc3 新增）
grep -rn "class RedactingArgumentParser" --include="*.py"
# 退出码: 0 (仅 1 处: validate_handoff_docs.py)

# 验证 RedactingArgumentParser 引用点（b7dedc3 新增 4 处）
grep -rn "RedactingArgumentParser" --include="*.py"
# 退出码: 0 (5 处: 1 定义 + 4 引用)

# 验证 _redact_report_value 定义（b8b9f17 新增）
grep -rn "_redact_report_value" --include="*.py" utils/prepare_deepseek_task.py | wc -l
# 退出码: 0

# 验证 _redact_check_result 定义（bfc4801 新增）
grep -rn "_redact_check_result" --include="*.py" utils/dual_model_pipeline_check.py | wc -l
# 退出码: 0

# 验证 _validate_secret_shape_fields 定义（b8b9f17 新增）
grep -rn "_validate_secret_shape_fields" --include="*.py" utils/prepare_deepseek_task.py
# 退出码: 0

# 验证 SECRET_KEY_PATTERN 使用点（共享正则，base 前引入）
grep -rn "SECRET_KEY_PATTERN" --include="*.py"
# 退出码: 0 (找到 8 处引用)

# 验证 fins → engine 跨层依赖数量（历史债务，不在 review 范围内）
grep -rn "from dayu\.engine" --include="*.py" dayu/fins/ | wc -l
# 退出码: 0 (输出: 76)

# 验证 prepare_deepseek_task 访问 validate_handoff_docs 私有 API（历史债务）
grep -rn "validate_handoff_docs\._[a-z]" --include="*.py" utils/prepare_deepseek_task.py | wc -l
# 退出码: 0 (输出: 21)

# 验证 validate_handoff_docs 行数
wc -l utils/validate_handoff_docs.py
# 退出码: 0 (输出: 3219)

# 统计 dayu/ 目录 Any/object 使用（历史债务）
grep -rn ": Any" --include="*.py" dayu/ | wc -l
# 退出码: 0 (输出: 242)
grep -rn ": object" --include="*.py" dayu/ | wc -l
# 退出码: 0 (输出: 281)
```

---

## Conclusion

**PASS.** 在 `1c6bdc9..b7dedc3` 的 3 次提交范围内未发现 material findings。

3 次提交均为正确扩展现有 secret redaction 链路：
- `b8b9f17` 新增 task generation 的 secret 阻断与 CLI 输出脱敏
- `bfc4801` 新增 pipeline check 的 plain text 报告脱敏
- `b7dedc3` 新增 `RedactingArgumentParser` 统一覆盖 4 个工具模块的 argparse 错误输出

变更逻辑正确，测试覆盖充分，无新引入的缺陷或风险。

**Pre-existing observations**（PO-1..PO-3）和**历史债务**（RR-1..RR-9）均在 base 之前引入，不阻塞本次结论，建议纳入后续治理计划。
