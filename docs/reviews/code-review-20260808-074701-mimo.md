# Slice 8 Code Review（复审）— Challenger 与配置回滚迁移

- **审查人**: mimo
- **日期**: 2026-08-08
- **基线**: `fcf4602` (HEAD)
- **范围**: workspace uncommitted changes
- **初审**: `docs/reviews/code-review-20260808-073323-mimo.md`
- **DeepSeek**: `docs/reviews/code-review-20260808-073322-deepseek.md`
- **Controller**: `docs/reviews/slice-8-code-review-adjudication-20260808-codex.md`
- **Verdict**: **PASS**

---

## 复审原因

初审 L-1 声称实现文档"13 个 Challenger 函数"不精确，应为 14。经 Python AST 实测确认：

- `_write_challenger.py`：**13** 个顶级 `FunctionDef`（exact）
- `_write_config_rollback.py`：**1** 个顶级 `FunctionDef`（exact）
- 总迁移：**14**

初审 L-1 基于错误计数，实现文档与 accepted v5.0 的"13"表述正确。L-1 应予关闭。

---

## 前置结构验证（全部 PASS）

| 验证项 | 结果 | 证据 |
|---|---|---|
| `_write_challenger.py` 顶级函数数 | **13**（exact） | `python3 ast.parse()` 确认 13 个 `FunctionDef` |
| `_write_config_rollback.py` 顶级函数数 | **1**（exact） | `python3 ast.parse()` 确认 1 个 `FunctionDef` |
| 总迁移函数数 | **14** | 13 + 1 |
| `write.py` 本地定义数 | 2（`_materialize_research_after_write`、`run_write_command`） | AST 确认 |
| `write.py` 旧 14 定义残留 | 0 | AST 确认 |
| `write.py` 从 `_write_challenger` import 数 | 10（exact） | AST 确认 |
| `write.py` 从 `_write_config_rollback` import 数 | 1（exact） | AST 确认 |
| `run_write_command` 使用的 Challenger 符号 | 10（exact） | AST 确认 |
| `run_write_command` 使用的 rollback 符号 | 1（exact） | AST 确认 |
| 内部 helper（`_challenger_preflight_cli_args`、`_load_current_challenger_proposal`、`_manifest_has_usable_company_facets`）在 `write.py` 中的兼容绑定 | 0 | AST 确认 |
| 14 迁移函数去 docstring 后 AST 等价 | **14/14 PASS** | `ast.dump()` 逐函数对比 |
| 反向 import（新 owner → `write.py`） | 0 | AST 确认 |
| `MODULE` 本地定义 | 0（均从 `_write_config_helpers` 单一真源 import） | AST 确认 |
| `Any` semantic uses | `_write_challenger.py`: 6、`write.py`: 1、合计 7（HEAD 7→当前 7） | AST 确认 |
| 既有 function-local domain import | 按 exact AST 保留 | 代码确认 |
| owner/compat lazy 新增 import | 0 | 代码确认 |
| `git diff --check` | PASS | 实现文档确认 |
| Ruff full-rule finding-code multiset | HEAD 与当前无正增量 | 实现文档确认 |
| Pyright | `0 errors, 0 warnings, 0 informations` | 实现文档确认 |
| 测试 | 659 passed（独立复跑）/ 812 passed（实现文档全量） | 实测确认 |
| 覆盖率 | `_write_challenger.py`: 83.0%、`_write_config_rollback.py`: 91.2%、`write.py`: 83.1%（均 ≥80%） | 实现文档确认 |
| Docstring | 13/13 + 1/1 函数均有完整中文 Args/Returns/Raises | AST 确认 |
| 模块概览 docstring | 两个新 owner 均有 | AST 确认 |

---

## 初审 L-1 复核

### L-1: 实现文档 Challenger 函数计数表述不精确 — **CLOSED**

**初审声称**: 实现文档第 37 行写"exact 迁移 13 个 Challenger 函数"，但 `_write_challenger.py` 实际含 14 个顶层函数定义。

**复审结论**: **初审计数错误**。`python3 ast.parse()` 实测 `_write_challenger.py` 仅有 **13** 个顶级 `FunctionDef`，与实现文档一致。初审将 `_manifest_has_usable_company_facets` 重复计数或误将某内部引用计为函数定义。

**状态**: CLOSED — 实现文档"13 个 Challenger 函数"与 AST 实测一致，accepted v5.0 的逐名清单正确。

---

## DeepSeek 三项 Finding 复核

DeepSeek 提出三项 finding，均针对测试覆盖补充建议：

### DS-01: `_build_auto_bootstrap_args` 缺乏直接单元测试覆盖 — **PRE-EXISTING**

- **DeepSeek 严重性**: Medium
- **复审结论**: `git show HEAD:tests/application/test_write_challenger.py` 中 `grep 'build_auto_bootstrap_args'` 结果为 0。HEAD 即无直接测试。本 Slice 未引入新行为，仅迁移既有函数。
- **状态**: PRE-EXISTING — 不属于本 Slice 缺陷。建议后续 Slice 择机补充。

### DS-02: `_challenger_preflight_cli_args` 切片语义未经直接测试验证 — **PRE-EXISTING**

- **DeepSeek 严重性**: Low
- **复审结论**: HEAD 测试中 `grep 'preflight_cli_args'` 结果为 0。切片 `[1:]` 语义与 HEAD AST 一致，本 Slice 未修改此函数。
- **状态**: PRE-EXISTING — 不属于本 Slice 缺陷。

### DS-03: `_assert_challenger_run_output_boundaries` 错误路径未经直接测试覆盖 — **PRE-EXISTING**

- **DeepSeek 严重性**: Low
- **复审结论**: HEAD 测试中 `grep 'output_boundaries'` 结果为 0。错误路径逻辑与 HEAD AST 一致，本 Slice 未修改此函数。
- **状态**: PRE-EXISTING — 不属于本 Slice 缺陷。

---

## 独立复查项

除初审与 DeepSeek 已覆盖的维度外，复审额外验证：

### A. write.py removed imports 完整性

AST 级 diff 确认以下旧依赖已从 `write.py` 移除：

- `collections.abc.Mapping`、`dataclasses.replace`、`datetime.UTC`、`datetime.datetime`
- `dayu.cli.commands._write_snapshot_builder.build_snapshot_builder`
- `dayu.cli.dependency_setup.WorkspaceConfig`
- `dayu.execution.options.ExecutionOptions`
- `dayu.services.contracts.WriteRunConfig`
- 所有 `write_model_challenger_*`、`write_model_configuration_*`、`write_model_health`、`write_run_comparison` imports

`write.py` 新增 imports 仅为 `_write_challenger`（10 symbols）和 `_write_config_rollback`（1 symbol）。✓

### B. rollback DAG 无环

`_write_config_rollback.py` → `_write_snapshot_builder` → `_write_config_application`。`_write_challenger.py` 不依赖上述任何模块。两个新 owner 均不 import `write.py`。无环。✓

### C. 测试 monkeypatch 路径正确性

| 测试文件 | 旧路径 | 新路径 | 原因 |
|---|---|---|---|
| `test_write_challenger.py` | `dayu.cli.commands.write.Log.info` | `dayu.cli.commands._write_challenger.Log.info` | 函数迁入新 owner |
| `test_write_challenger.py` | `dayu.cli.commands.write._run_write_stage` | `dayu.cli.commands._write_challenger._run_write_stage` | 函数迁入新 owner |
| `test_write_challenger.py` | `dayu.cli.commands.write.build_write_model_health_trend` | `dayu.cli.commands._write_challenger.build_write_model_health_trend` | 函数迁入新 owner |
| `test_write_model_configuration_preapplication.py` | `write_command_module` (rollback patches) | `write_config_rollback_command_module` | 函数迁入新 owner |
| `test_cli_running_config.py` | `from dayu.cli.commands.write import _needs_auto_research_bootstrap` | `from dayu.cli.commands._write_challenger import _needs_auto_research_bootstrap` | 函数迁入新 owner |

所有 patch 路径指向函数的实际定义模块。✓

### D. `_CHALLENGER_COMPARISON_FILE` 常量迁移

从 `write.py` 迁入 `_write_challenger.py`。`write.py` 中不再定义或引用此常量。`_run_champion_challenger_experiment` 在新 owner 中正确引用。✓

### E. identity 测试计数

`test_write_challenger_and_rollback_imports_preserve_identity` 包含 11 个 `is` 断言：10 个 Challenger import binding + 1 个 rollback import binding。与 `write.py` 的 10+1 import 一致。✓

---

## Open Findings

| # | 严重性 | 描述 | 状态 |
|---|---|---|---|
| （无） | — | — | — |

**目标 open=0 达成。**

---

## Residual Risk

1. **既有 sqlite `ResourceWarning`**: 全量测试 812 passed 附带既有 sqlite 资源未关闭警告。本 Slice 不改变资源生命周期管理，此风险为 pre-existing。
2. **engine test 既有 `I001`**: Ruff `I001` 出现在 `tests/engine/test_cli_running_config.py`，为 HEAD 既有，本 Slice 未新增或扩散。
3. **既有 function-local domain import**: `_manifest_has_usable_company_facets` 中的 `from dayu.cli.research_template_routing import load_company_facets_from_manifest` 按 exact AST 保留。HEAD 既有设计选择。
4. **Rollback `build_snapshot_builder` 默认 label**: HEAD 既有行为，本 Slice 未修改。
5. **DeepSeek 三项测试覆盖建议**: 均为 HEAD 既有的测试粒度问题，建议后续 Slice 择机补充，不影响本 Slice 通过。
