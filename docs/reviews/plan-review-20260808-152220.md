# Plan Re-Review — v5.6 Aggregate Gate Erratum 双路复审

- **日期**: 2026-08-08 15:22
- **基线**: clean HEAD `2a4bafb`
- **分支**: `codex/dual-model-research-mvp`
- **被审 artifact**: `docs/plans/2026-08-07-cli-write-architecture-refactor.md` (v5.6 CANDIDATE diff) + `docs/reviews/plan-v5.6-aggregate-gate-erratum-20260808-codex.md`
- **初审来源**: `docs/reviews/plan-review-20260808-150148.md` (DeepSeek, AGG-01..05) + `docs/reviews/plan-review-20260808-150149.md` (MiMo, FAIL, H01/M01/L01)
- **审查类型**: adversarial re-review — 验证 erratum 是否正确处置全部初审 findings，header/status/tail 一致，历史计数未被改写，终态闭合正确，替代门禁可执行

---

## 0. 验证清单

### 0.1 Header / Status / Tail 一致性

| 位置 | 文本 | 一致性 |
|---|---|---|
| Header (line 1) | `v5.6 CANDIDATE / AWAITING DUAL RE-REVIEW` | ✓ |
| Status (line 42) | `v5.6 CANDIDATE / AWAITING DUAL RE-REVIEW（AGG-CTRL-01..03 已写入；等待双路 plan re-review）` | ✓ |
| Tail (line 2684) | `v5.6 CANDIDATE / AWAITING DUAL RE-REVIEW（clean HEAD 2a4bafb...等待双路 plan re-review）` | ✓ |

**结论**: 三处统一为 `v5.6 CANDIDATE / AWAITING DUAL RE-REVIEW`，v5.5 ACCEPTED 历史完整保留。**PASS**。

### 0.2 Slice 9 历史 123=83+40 未被改写

Plan diff 在 §11 item 15 的处置：

```
+ Slice 9 当时主模块 FunctionDef exact40（entry1 + runners39）且
+ `83+40=123` 的历史闭合保持不变；
+ Slice 10 新增 selector 后 aggregate terminal 主模块 FunctionDef exact41
```

**验证**:
- 历史公式 `83+40=123` 明确标注为 **Slice 9 当时**（past tense），与 Slice 10 后终态 41 以时间线区分
- MiMo 150149 建议改为 `83+41=124` → Controller **REJECTED-WITH-REASON**：selector 是 Slice 10 后续增量，Slice 9 当时确实不存在
- 逻辑自洽：83 migrated + 40 retained (Slice 9) = 123；83 migrated + 41 retained (Slice 10 terminal) = 124；两个等式在不同时间截面上均成立

**结论**: 历史 123 未被改写。**PASS**。

### 0.3 Slice 10 后 Terminal Exact 41 闭合

Plan diff 更新了以下位置：

| 位置 | 旧值 | 新值 |
|---|---|---|
| §2.1 target structure | `FunctionDef exact40：entry1 + runners39` | `aggregate terminal FunctionDef exact41：entry1 + selector1 + runners39` |
| §9a (在 §11 item 15 的 gate) | `主模块 FunctionDef exact40` | 拆分为 Slice 9 当时 exact40 + Slice 10 后 terminal exact41 |
| §11 item 2 | LOC gate | `顶层 FunctionDef exact41（entry 1 + Slice 10 selector 1 + runners 39）` |

**当前 HEAD AST 验证**:
```
entry     = run_research_template_command              1
selector  = _resolve_research_template_action          1  ← S10-CTRL-04
runners   = _run_*                                     39
                                                      ---
Total                                                   41  ✓
```

**结论**: Terminal exact41 全文一致闭合。**PASS**。

### 0.4 1014/1369 仅为 Informational

Plan diff 中的处置：

| 位置 | 处置 |
|---|---|
| AGG-CTRL-01 | "当前 `1014/1369` 仅作 informational measurement，不以当前值上浮为新的任意上限" |
| §8 God file 行 | "aggregate terminal `write.py=1014`、`research_template.py=1369` 仅为 informational LOC measurement，不是完成门槛" |
| §9 aggregate gate | "LOC measurement 本身不触发 stop condition" |
| §11 item 2 | "当前 LOC `1014/1369` 仅为 informational measurement，不是完成门槛" |

**验证**:
- 全文搜索 `850`、`800`、`≤` 配合 LOC：在 diff 后的 plan 中，旧的 `≤ 850`/`≤ 800` hard gate **零残留**
- 全文搜索 `1050`、`1400`：**零出现** — MiMo 建议的任意新上限未被采纳（Controller REJECTED）
- 1014/1369 出现在三处，每处均带 `informational`/`不是完成门槛` 限定词

**结论**: LOC 已降为纯信息性。**PASS**。

### 0.5 绝对 LOC Gate 已被可执行结构门禁完整替代

Plan 在 §9 新增了 **aggregate pre-deepreview 结构门禁**，包含可执行 AST 断言：

| 门禁项 | 验证方式 | 当前状态 |
|---|---|---|
| `write.py` FunctionDef exact18 | `ast.parse` → `len(functions) == 18` | ✓ |
| `write.py` adapter exact16 | `sum(name.endswith("_adapter"))` | ✓ |
| `write.py` non-adapter = {materialize, entry} | set difference | ✓ |
| `write.py` Phase A table 14 entries | `isinstance(..., ast.Tuple) and len(elts) == 14` | ✓ |
| `write.py` Phase B table 2 entries | `isinstance(..., ast.Tuple) and len(elts) == 2` | ✓ |
| `research_template.py` FunctionDef exact41 | `len(functions) == 41` | ✓ |
| `research_template.py` entry + selector + runners39 | name-based partition | ✓ |
| `research_template.py` ClassDef exact0 | `not any(isinstance(..., ast.ClassDef))` | ✓ |
| `research_template.py` dispatch mapping 39-key | `isinstance(..., ast.Dict) and len(keys) == 39` | ✓ |
| `research_template.py` `__all__` 55-entry | `isinstance(..., ast.List) and len(elts) == 55` | ✓ |
| 五 owner `22/26/12/11/12` | 逐文件 FunctionDef 计数 | ✓ |
| helpers ClassDef exact2 | 逐文件 ClassDef 计数 | ✓ |
| DAG 单向 / 零反向 import | AST free-global 验证 | ✓ |
| 零 compatibility binding | targeted rg + identity 测试 | ✓ |

**验证**:上述全部 gate 在 HEAD `2a4bafb` 上独立执行并 **PASS**（见 §0.6 执行结果）。

**替代完整性**: 旧 LOC gate 的意图是防止 God file 回退。新门禁覆盖了旧 gate 的**全部保护维度**（函数数、表项数、owner 分布、DAG 方向、compat 为 0），且每个维度均可**独立可执行验证**，不依赖人工目测 LOC。新门禁比旧 LOC gate **更严格**（禁止了 LOC 不超标但结构违规的情况）且**更精确**（直接验证架构属性而非代理指标）。

**结论**: 替代完整且可执行。**PASS**。

### 0.6 无 Slice 14 / No Code Changes

Plan erratum §4 "明确拒绝的替代方案"：
- "不引入 Slice 14，也不为满足失真的 LOC 数字改 code、tests 或 README"
- "不采用 1050/1400 等任意放宽的 LOC 上限"
- "不把 Slice 9 历史基线从 123 改成 124"

Plan diff 统计：仅 plan `.md` 文件被修改，零生产代码、零测试、零 README 变动。

**结论**: 零 code change。**PASS**。

### 0.7 全部初审 Finding Closed 逐项复核

#### DeepSeek 150148（5 findings）

| ID | 描述 | Controller 裁决 | 处置验证 |
|---|---|---|---|
| AGG-01 | write.py LOC gate stale | ACCEPTED / CLOSED-BY-PLAN-TEXT | AGG-CTRL-01 删除硬门槛，替换为 exact18 + Phase tables + DAG gate |
| AGG-02 | research_template.py LOC gate stale | ACCEPTED / CLOSED-BY-PLAN-TEXT | AGG-CTRL-01 同上，替换为 exact41 + dispatch + __all__ + owner gate |
| AGG-03 | FunctionDef count plan accounting lag | ACCEPTED / CLOSED-BY-PLAN-TEXT | AGG-CTRL-02 terminal exact41，区分 Slice 9 历史 40 与 Slice 10 后 41 |
| AGG-04 | 是否需要新增 Slice 14 | ACCEPTED / CLOSED-BY-PLAN-TEXT | AGG-CTRL-03 明确不新增 Slice，ownership 已闭合 |
| AGG-05 | 继续迁移是否破坏 contract | ACCEPTED / CLOSED-BY-PLAN-TEXT | AGG-CTRL-03 以结构/行为/类型门禁为 stop condition |

**DeepSeek 5/5 CLOSED** ✓

#### MiMo 150149（3 findings）

| ID | 描述 | Controller 裁决 | 处置验证 |
|---|---|---|---|
| H01 | research_template.py LOC gate 超标 | 事实 ACCEPTED；1400 方案 REJECTED-WITH-REASON | 不设新任意上限，以结构门禁替代。新数值 1400 同样会漂移 |
| M01 | FunctionDef 40→41 计数未同步 | 事实 ACCEPTED；124 历史改写 REJECTED-WITH-REASON | 终态 exact41，Slice 9 当时 123 保留。Selector 是 Slice 10 增量，非 Slice 9 漏计 |
| L01 | write.py LOC gate 超标 | 事实 ACCEPTED；1050 方案 REJECTED-WITH-REASON | 同上，不设新任意上限 |

**MiMo 3/3 CLOSED**（事实部分采纳，方案部分有理拒绝） ✓

**合计: 8/8 findings CLOSED** ✓

---

## 1. Adversarial 压力测试

### 1.1 结构门禁是否真的比 LOC gate 更严格？

**测试**: 构造一个满足旧 LOC gate 但违反新结构门禁的场景。

```
假设 write.py = 800 行（满足 ≤850）但：
- 新增第 19 个 FunctionDef（违反 exact18 gate）
- Phase A 表只有 13 entries（违反 exact14 gate）
- 新增 compatibility re-export（违反 zero-compat gate）

旧 gate: PASS (800 ≤ 850)
新 gate: FAIL (3 项结构违规)
```

**结论**: 新门禁**比旧 LOC gate 更严格**，捕获了 LOC 无法捕获的架构违规。

### 1.2 如果未来新增 runner 或 adapter 会怎样？

新增功能必然改变 exact 计数 → 结构门禁 FAIL → 强制 plan update。这正是设计意图：架构变更必须经过显式 plan 更新，不能被 LOC slack 悄悄吸收。

### 1.3 erratum 是否引入了新的模糊性？

- `exact18`、`exact41`、`exact14+2` 等计数**零模糊性**（AST 可判定）
- `零 compatibility re-export` 由 import 路径 targeted rg + identity 测试判定
- DAG 单向性由 AST free-global 验证判定
- 所有门禁均为**确定性可执行断言**

**结论**: 零新模糊性。

---

## 2. Verdict: PASS

| 严重度 | 数量 | 说明 |
|---|---|---|
| **H** | 0 | — |
| **M** | 0 | — |
| **L** | 0 | — |

**Controller open H/M/L: 0/0/0**

### 通过理由

1. **Header/status/tail 一致**: 全部统一为 `v5.6 CANDIDATE / AWAITING DUAL RE-REVIEW`，v5.5 历史保留。
2. **Slice 9 历史 123=83+40 未被改写**: 以时间线区分 Slice 9 当时 40 与 Slice 10 后 terminal 41，两个等式在各自时间截面上均成立。
3. **Slice 10 后 terminal exact41 闭合**: entry1 + selector1 + runners39 = 41，全文三处一致。
4. **1014/1369 仅 informational**: 旧 hard gate 零残留，未引入 1050/1400 等新任意上限。
5. **绝对 LOC gate 已被可执行结构门禁完整替代**: exact FunctionDef/AnnAssign/dispatch/owner/DAG/zero-compat gate 全部在 HEAD `2a4bafb` 上独立执行 PASS。
6. **无 Slice 14 / no code changes**: 仅 plan text 修改，零生产/测试/README 变更。
7. **8/8 初审 findings CLOSED**: DeepSeek 5/5 + MiMo 3/3，事实采纳、方案有理拒绝。

### 推荐下一步

1. Controller 确认本复审后，将 plan status 从 `CANDIDATE / AWAITING DUAL RE-REVIEW` 推进到 `ACCEPTED / DUAL PLAN RE-REVIEW PASS`。
2. 进入 aggregate deepreview 前，执行 §9 的 aggregate pre-deepreview 结构门禁脚本。
3. 无 code/test/README 变更需求。

---

## 3. 自审

- 本次复审仅读取 HEAD `2a4bafb` 的生产代码、plan diff、erratum artifact 与两份初审 review。
- 所有结构门禁均通过独立 `ast.parse` 脚本在当前 HEAD 上重新执行验证（非引用初审结果）。
- 未修改任何 plan、代码、测试或 README。
- 未执行 `git add`、`git commit`、`git push` 或 `git stash`。
- 输出文件: `docs/reviews/plan-review-20260808-152220.md`
