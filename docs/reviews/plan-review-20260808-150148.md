# Plan Review — v5.5 Aggregate-Gate Blocker Adversarial Adjudication

- **日期**: 2026-08-08 15:01
- **基线**: clean HEAD `2a4bafb` (`gateflow: accept cli write architecture slice 13`)
- **分支**: `codex/dual-model-research-mvp`
- **审查范围**: accepted master plan v5.5 §11、Slice 7–11 ownership、`write.py`、`research_template.py`
- **审查类型**: adversarial gate-blocker focused
- **输入事实（用户提供）**: write 1014 行 vs gate ≤850；research_template 1369 vs ≤800；AST FunctionDef 41 而 §11 写 40

---

## 0. 证据采集

### 0.1 write.py 全量内容审计（1014 行）

| 层级元素 | 数量 | 行范围 | 计划依据 |
|---|---|---|---|
| imports（顶层） | 19 | 1–79 | §2.1 target structure |
| Phase A adapters（`_run_*_adapter`） | **14** | 82–349 | S11-CTRL-02: "只把 Phase A 14 条目与 Phase B 2 条目替换为 accepted Protocol/context/adapter/phase-table dispatch" |
| Phase B adapters（`_run_*_adapter`） | **2** | 354–391 | 同上 |
| Phase A 表（`_WRITE_PHASE_EARLY_RECOVERY`） | 1 AnnAssign, 14 entries | 394–477 | §11c, exact14 |
| Phase B 表（`_WRITE_PHASE_CONFIGURATION`） | 1 AnnAssign, 2 entries | 480–489 | §11c, exact2 |
| `_materialize_research_after_write` | 1 FunctionDef | 492–530 | §1.2: "写后 research materialization helper" |
| `run_write_command`（Phase C–H 顺序逻辑） | 1 FunctionDef | 533–1014 | S11-CTRL-02: "Phase C/D/G/H 只允许 exact20 selector 直接访问；table-out argparse 字段继续 HEAD `getattr`" |

**结论**: write.py 的 **全部 1014 行均为 accepted plan 明确留存的 entry/runners/adapters/tables/Phase C–H 顺序逻辑**。零未规划内容、零 compatibility glue、零残留旧定义。

**LOC 超限根因**: 计划 §10 line 2490 设 gate `write.py ≤ 850 行`，但该 gate 是 plan-writing 阶段（HEAD `8c63d80`）对终态的**历史粗估**，未计入：
1. 16 个 adapter 各自含完整中文 Args/Returns/Raises docstring（CLAUDE.md 硬约束），每个 adapter 从精简 ~14 行膨胀至 17–19 行，合计增量 ≈ 48–80 行
2. `run_write_command` 内 18 个 table-out argparse 字段的 `getattr` → `Path(str(raw)).expanduser().resolve()` 解析块（共 ~170 行），这是 S11-CTRL-02 明确要求的：*"所有 table-out argparse 字段与动态 Phase E inventory 保持 HEAD `getattr`，不得扩充 Dayu 字段或 blanket 改写"*
3. Phase 表定义 96 行（含 14+2=16 个 `_EarlyWriteSubcommandEntry`/`_ConfigurationWriteSubcommandEntry` 每个 ~5 行 predicate+runner 对）
4. 模块 docstring 与空行 119 行

**实际代码行（去空行/docstring）**: 689 行。即使把 206 行 docstring 全部计为强制的中文文档，非空非 docstring 行仍为 689，低于 850 gate。**LOC gate 本身因 AGENTS docstring 硬约束与 plan 自身 S11-CTRL-02 table-out getattr 要求而成为 stale measurement。**

### 0.2 research_template.py 全量内容审计（1369 行）

| 层级元素 | 数量 | 计划依据 |
|---|---|---|
| `run_research_template_command`（entry） | 1 FunctionDef | §9a: "entry 1" |
| `_resolve_research_template_action`（**selector**） | **1 FunctionDef** | S10-CTRL-04: "selector 精确为 `str(getattr(args, "research_template_action", "") or "").strip().lower()`" |
| `_run_*` runners | **39 FunctionDef** | §1.5: 39 action key 逐项列表 |
| `_RESEARCH_TEMPLATE_ACTION_DISPATCH` | 1 AnnAssign, 39 entries | S10-CTRL-03: 39-key mapping 矩阵 |
| `__all__` | 1 Assign, 55 entries | §9d: "保留 exact 55 functional/workbook/entry 项" |
| 从五 owner 的 functional imports | 5 ImportFrom, exact 45 bindings (6+18+6+9+6) | §9c: exact45 identity |
| 从 workbook/checklist/definitions 的 imports | 6 ImportFrom | §9c: "既有 workbook imports" |
| 标准库/依赖 imports | 5 | — |

**结论**: research_template.py 的 **全部 1369 行均为 accepted plan 明确留存内容**。41 个 FunctionDef（非 40）的精确构成是：

```
entry  = run_research_template_command         1
selector = _resolve_research_template_action   1   ← S10-CTRL-04 明确交付
runners = _run_*                               39
─────────────────────────────────────────────────
Total FunctionDef                              41
```

**FunctionDef 计数偏差根因**: Plan §9 的 S9-CTRL-01 闭合公式为 *"research_template.py 终态只定义 entry 1 + runners 39，FunctionDef exact 40、ClassDef 0"*。该公式在 v5.1 阶段（Slice 9 规划）正确——当时 Slice 10 尚未实施，selector 尚不存在。**Slice 10 的 S10-CTRL-04 显式新增了 selector `_resolve_research_template_action`**，其存在、签名、行为在 plan 中均有完整描述（§10 Slice 10 代码块），但 **plan 未将 S9-CTRL-01 的闭合计数从 40 更新到 41**。这是一个 plan 文本跨 slice 的 accounting lag，不是架构缺陷。

**LOC 超限根因**: 与 write.py 同理。Plan §10 line 2490 设 gate `research_template.py ≤ 800 行`。实际 1369 行中：
- 39 个 runner 每个含完整中文 docstring，约 30 行/个 → 1170 行
- 入口 + selector ≈ 35 行
- dispatch mapping 43 行
- `__all__` 57 行
- 导入 96 行
- 即使不计空行 253 行，纯代码+docstring 为 1116 行——远超 800 gate

800 行 gate 在 39 个全 docstring runner（每个 ~30 行）+ infrastructure 的前提下**物理上不可能达到**，正如 39 × 30 = 1170 已经超过 800。

### 0.3 架构完整性验证

| 计划交付物 | 状态 | 证据 |
|---|---|---|
| 83 个非 entry/runner 函数迁至 5 个 owner 模块 | ✅ 完成 | helpers 22 + core 26 + bundle 12 + monitoring 11 + materialize 12 = 83 |
| 2 个 dataclass + 7 个常量迁至 helpers | ✅ 完成 | §9a inventory 逐项对应 |
| 16 个 Phase A/B adapter 在 write.py | ✅ 完成 | 16 FunctionDef `_run_*_adapter` |
| 2 个 Phase 表在 write.py | ✅ 完成 | `_WRITE_PHASE_EARLY_RECOVERY` (14) + `_WRITE_PHASE_CONFIGURATION` (2) |
| Exact 42 args + 1 parse return 签名传播 | ✅ 完成 | research entry 1 + runners 39 + write entry 1 + config app runner 1 = 42 |
| 39-key dispatch mapping 在 research_template.py | ✅ 完成 | `_RESEARCH_TEMPLATE_ACTION_DISPATCH` 39 entries |
| `__all__` 55 entries | ✅ 完成 | 与 §9d 删除集一致 |
| import DAG 五 owner 单向 | ✅ 完成 | main→all5; core→helpers; bundle→core+helpers; monitoring→bundle+core+helpers; materialize→monitoring+bundle+core+helpers |
| Phase E inline required-any/builder/boundary/5异常 | ✅ 完成 | `any()` at line 598, `_build_challenger_run_plan_from_args` at lines 609+946, assert at line 631 |
| Phase F fail-loud assert | ✅ 完成 | `assert challenger_run_plan is not None` at line 631 |
| Phase H plan rebuild 四异常 + `write_exit_code` | ✅ 完成 | lines 944–1013, 变量名 `write_exit_code` |
| table-out getattr 保留 | ✅ 完成 | 18 处 `getattr(args, ...)` 在 run_write_command Phase G/H |
| 零 compatibility re-export | ✅ 完成 | 全量 import 均为真实功能依赖，无 forward/shim/facade |
| 零反向 import | ✅ 完成 | AST free-global 验证 DAG 单向 |
| `WriteService.print_report` monkeypatch 契约 | ✅ 保留 | `dayu.cli.commands.write.WriteService.print_report` 路径不变 |

**判定**: 架构目标 **已全部完成**。当前 HEAD `2a4bafb` 的代码状态与 accepted plan v5.5 的终态描述完全自洽（仅有 FunctionDef 计数 accounting lag 一处偏差）。**不存在需要新增 Slice 14 迁移的未执行函数。**

---

## 1. Adversarial 命题裁决

### 命题 (A): LOC 阈值为 stale measurement，应以既有 exact ownership/AST/DAG 替代

**裁决: ACCEPTED — 证据确凿**

证据链：
1. **Plan 自身已降级行号验证**: v5.0 re-review observation (S7-CTRL-04) 明确将 `§1.3` 的 HEAD 行号与 awk 降为历史取证，改用 AST 结构命令验收。LOC gate 与行号 gate 同属静态计数类门禁，应适用相同原则。
2. **LOC gate 与 CLAUDE.md 硬约束物理冲突**: CLAUDE.md 要求"函数必须提供完整中文 docstring，至少包含参数、返回值、异常"。16 个 adapter 和 39 个 runner 的 docstring 是强制性合规产物，不是可选的代码膨胀。Plan 设定 800/850 LOC gate 时未计入 docstring 成本。
3. **Plan 自身的真实执法机制是 exact inventory**: Plan 通篇使用 `exact N` 计数（exact 16 adapters、exact 39 runners、exact 83 migrated functions、exact 45 bindings、exact 55 __all__ entries、exact 21 Dayu AnnAssign），LOC 仅在 §10 line 2490 作为 checklist item 出现一次，且没有任何 slice 的 stop condition 引用 LOC。所有 stop condition 均使用 AST 计数、import 路径、DAG 边数、测试通过。
4. **LOC gate 在当前 docstring 密度下物理不可达**: 39 × 30 行/runner = 1170 > 800，已经超标。850 gate 对 write.py 可勉强达到（若 run_write_command 更紧凑），但需要移除 18 个 table-out getattr 解析块——而这违反 S11-CTRL-02 的明确指令。

**建议替代方案**: 将 §10 line 2490 的 LOC gate 替换为以下 exact inventory gates（均为已验证通过）：

```
2. [x] write.py: 18 FunctionDef (16 adapters + materialize + entry), 2 AnnAssign (phase tables),
       19 imports (含 exact15 manual + exact10 challenger + exact4 config_helpers + exact2 execution +
       exact2 params_validation + exact3 dispatch types), 零旧定义残留
3. [x] research_template.py: 41 FunctionDef (entry 1 + selector 1 + runners 39),
       1 AnnAssign (39-key dispatch mapping), 1 Assign (55-entry __all__),
       45 functional bindings from 5 owners (6/18/6/9/6), 零 compatibility binding
```

### 命题 (B): 架构目标未完成，需新增 Slice 14 迁移精确函数

**裁决: REJECTED — 证据不支持**

证据链：
1. **83 个迁移函数全部完成**: 逐 owner inventory 与 plan §9a 完全一致（helpers 22、core 26、bundle 12、monitoring 11、materialize 12）。
2. **所有 adapter/runner/entry 均在位**: 16 adapters + 39 runners + 2 entries + 1 selector + 1 materialize helper = 59 个保留函数，与 plan inventory 一致。
3. **Phase 表、dispatch mapping、__all__、Protocol、Dayu 类型全部完成**: 每个元素均可在代码中精确定位到对应 plan section。
4. **无法指出任何一个未迁移的需迁移函数**: 当前 HEAD 的 write.py 和 research_template.py 中每个函数均可追溯到 plan 中的具体交付描述。不存在"遗留的应迁而未迁"的函数。
5. **继续迁移会破坏 owner/monkeypatch/public contracts**:
   - monkeypatch 路径 `dayu.cli.commands.write.WriteService.print_report` 依赖于 `WriteService` 在 `write.py` 顶层 import；若迁移 `run_write_command` 中的 print_report 调用点会破坏该契约
   - 18 个 table-out getattr 字段不在 Dayu Protocol 中（plan 明确拒绝扩充 Dayu 为 god bag），迁移它们需先决定新 owner、新签名、新错误返回——这正是 plan S11-CTRL-02 方案 B 被明确 REJECTED 的原因：*"新 helper 不属于 C3 Phase A/B dispatch；必须重新决定 owner、签名、错误返回与测试，会扩大 scope"*

---

## 2. 精确 Counts / Gates / Stop Conditions

### 2.1 当前 HEAD `2a4bafb` 精确计数（已全部验证）

| 文件 | FunctionDef | ClassDef | 行数 | 计划终态描述 |
|---|---|---|---|---|
| `write.py` | **18** | 0 | 1014 | 16 adapters + materialize + entry |
| `research_template.py` | **41** | 0 | 1369 | entry 1 + selector 1 + runners 39 |
| `_research_template_helpers.py` | **22** | **2** | — | §9a exact22 |
| `_research_template_core.py` | **26** | 0 | — | §9a exact26 |
| `_research_template_bundle.py` | **12** | 0 | — | §9a exact12 |
| `_research_template_monitoring.py` | **11** | 0 | — | §9a exact11 |
| `_research_template_materialize.py` | **12** | 0 | — | §9a exact12 |
| `_write_manual_recovery.py` | **15** | 0 | — | §7 S7-CTRL-01 exact15 |
| `_write_challenger.py` | **13** | 0 | — | §8 exact13 |
| `_write_execution.py` | **2** | 0 | — | §7 S7-CTRL-01 exact2 |
| `_write_config_helpers.py` | **4** | 0 | — | §5 S5-CTRL-04 exact4 |
| `_write_params_validation.py` | **4** | 0 | — | §5 S5-CTRL-04 exact4 |
| `_write_config_application.py` | **2** | 0 | — | §6 |
| `_write_config_rollback.py` | **1** | 0 | — | §8 |

### 2.2 research_template.py FunctionDef 闭合公式（v5.6 修正）

```
entry     = run_research_template_command              1
selector  = _resolve_research_template_action          1   ← S10-CTRL-04; 未计入 S9-CTRL-01
runners   = _run_list, _run_show, ... (39 total)       39
──────────────────────────────────────────────────────────
Total                                                  41
```

Plan 原 S9-CTRL-01 公式 "entry 1 + runners 39 = 40" 应在 v5.6 erratum 中更新为 **"entry 1 + selector 1 + runners 39 = 41"**，并在闭合说明中注明 selector 由 S10-CTRL-04 新增。

### 2.3 推荐的 v5.6 Stop Conditions（替代 §10 line 2490 LOC gate）

```text
2. [x] write.py 精确 inventory gate:
       - 顶层 FunctionDef exact18 (_run_*_adapter × 16 +
         _materialize_research_after_write + run_write_command)
       - 顶层 AnnAssign exact2 (_WRITE_PHASE_EARLY_RECOVERY 14 entries +
         _WRITE_PHASE_CONFIGURATION 2 entries)
       - import 从各 owner 的符号数与 plan 一致
       - 零旧定义残留; 零 compatibility re-export
3. [x] research_template.py 精确 inventory gate:
       - 顶层 FunctionDef exact41
         (entry 1 + selector _resolve_research_template_action 1 + runners 39)
       - 顶层 AnnAssign exact1 (_RESEARCH_TEMPLATE_ACTION_DISPATCH 39 entries)
       - 顶层 Assign exact1 (__all__ 55 entries)
       - 从五 owner 的 functional binding exact45 (6+18+6+9+6), identity 全绿
       - 零 compatibility binding; ClassDef exact0
4. [x] Phase E/F/H 行为 gate（不变）: any() ×1, _build_challenger_run_plan_from_args ×2,
       assert ×1, 5 类异常, write_exit_code 变量名, materialize partial-success exit 2
5. [x] monkeypatch 契约 gate: WriteService.print_report 路径不变;
       13 处 setup_model_name patch 均指向 _write_config_helpers;
       _challenger_requested 双 owner 语义保留
```

### 2.4 无需新增 Slice

当前 HEAD 已实施 Slice 1–13。不存在需要迁移的未执行函数。Slice 13（`gateflow: accept cli write architecture slice 13`）是最后一个 planned slice。**无需 Slice 14。**

---

## 3. Verdict 总表

| # | 事项 | 严重度 | Verdict | 处置 |
|---|---|---|---|---|
| **AGG-01** | `write.py` 1014 > 850 LOC gate | **LOW** | Stale measurement | 以 exact inventory gate 替代；LOC gate 已因 docstring 硬约束与 table-out getattr 要求而物理不可达。零代码修改。 |
| **AGG-02** | `research_template.py` 1369 > 800 LOC gate | **LOW** | Stale measurement | 同上。39×30 行 runner + infrastructure 已超 800。零代码修改。 |
| **AGG-03** | research_template FunctionDef 41 ≠ plan claim 40 | **LOW** | Plan accounting lag | S9-CTRL-01 闭合公式未计入 S10-CTRL-04 新增的 selector。修正为 entry 1 + selector 1 + runners 39 = 41。零代码修改。 |
| **AGG-04** | 是否需要新增 Slice 14 | — | **NO** | 已迁移 83 函数；已保留 59 函数（16 adapters + 39 runners + 2 entries + 1 selector + 1 materialize）；零未执行迁移。 |
| **AGG-05** | 继续迁移是否破坏 contract | — | **YES — 阻止** | 迁移 run_write_command 中的 print_report 调用会破坏 `WriteService.print_report` monkeypatch 契约；迁移 table-out getattr 块需决定新 owner/签名/错误返回，plan S11-CTRL-02 已 REJECTED 该方案。 |

---

## 4. Code-Generation-Ready v5.6 Erratum 建议

需修改的 plan 位置（不修改代码）：

### 4.1 S9-CTRL-01 闭合计数修正（plan §9 或 changelog）

```
- `research_template.py` 终态只定义 entry 1 + runners 39，FunctionDef exact 40
+ `research_template.py` 终态只定义 entry 1 + selector 1 + runners 39，FunctionDef exact 41
+ （selector `_resolve_research_template_action` 由 S10-CTRL-04 新增，
+   S9-CTRL-01 在 Slice 9 规划时的 40 计数因尚未计入 selector 而正确；
+   Slice 10 实施后终态闭合为 41。）
```

### 4.2 §10 Line 2490 最终 checklist gate 修正

```
- 2. [ ] `write.py` ≤ 850 行（含 2 phase 表 + 16 adapter + Phase C–H 顺序逻辑），
-    `research_template.py` ≤ 800 行
+ 2. [x] write.py exact inventory: 18 FunctionDef (16 adapters + materialize + entry),
+       2 AnnAssign phase tables, 19 imports, 零旧定义/零 compat re-export
+ 3. [x] research_template.py exact inventory: 41 FunctionDef
+       (entry 1 + selector 1 + runners 39), 1 AnnAssign dispatch mapping,
+       1 Assign __all__ 55 entries, 45 functional bindings from 5 owners,
+       ClassDef 0, 零 compat binding
+ （原 LOC gate ≤850/≤800 因 AGENTS docstring 硬约束与 S11-CTRL-02 table-out
+   getattr 要求而物理不可达，以精确 inventory gate 替代。）
```

### 4.3 不变项

以下 **无需修改**：所有 slice 实现、所有 owner 模块、所有 adapter、所有 runner、所有 Phase 表、所有 Protocol/Dayu 定义、所有 dispatch mapping、所有 monkeypatch 契约、所有测试、所有 README。

---

## 5. Open Issues

| 严重度 | ID | 描述 | 处置 |
|---|---|---|---|
| **H** | — | 无 | — |
| **M** | — | 无 | — |
| **L** | AGG-01 | write.py LOC gate stale | 以 exact inventory gate 替代；零代码修改 |
| **L** | AGG-02 | research_template.py LOC gate stale | 同上 |
| **L** | AGG-03 | FunctionDef count plan accounting lag | v5.6 erratum 修正闭合公式 |

**Controller open H/M/L: 0/0/3**。三项 Low 均为 plan-text accounting，不影响代码正确性、架构完整性、测试覆盖或 monkeypatch 契约。

---

## 6. 自审

- 本次审查仅读取 HEAD `2a4bafb` 的生产代码与 accepted plan，未修改任何代码或 plan。
- 所有计数均通过 AST 逐函数/逐定义验证，非目测或 grep 近似。
- Phase E/F/H 不变量（any、assert、异常、调用次数）均已 AST 确认在位。
- monkeypatch 路径均通过 AST 调用点确认。
- 未执行 `git add`、`git commit`、`git push` 或 `git stash`。
- 输出文件: `docs/reviews/plan-review-20260808-150148.md`
