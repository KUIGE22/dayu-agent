# Plan Re-Review: v4.5 W16-CTRL-01 Adversarial Re-Review

**Review ID**: `plan-review-20260808-004533`
**Date**: 2026-08-08
**Reviewer**: Codex (独立 adversarial re-review)
**Scope**: 对 v4.5 W16-CTRL-01 plan erratum 做独立 adversarial re-review — 验证三 TypedDict 字段完整性、helper exact signature 合规、scope 边界自洽、ID/时序语义保持、其他 slices 零变化、35 tests + pyright 门禁足够
**Baseline**:
- `docs/plans/2026-08-07-cli-write-architecture-refactor.md` (v4.5 CANDIDATE)
- `docs/reviews/plan-review-20260808-002853.md` (DeepSeek type-contract review)
- `docs/reviews/plan-review-20260808-002854.md` (MiMo type-contract review)
- `docs/reviews/plan-slice-4-v4.5-type-erratum-20260808-codex.md` (Controller erratum artifact)
- `docs/reviews/AGENTS.md` (编码硬约束)
- `dayu/cli/commands/research_workbook.py` (1129 lines, 26 functions)
- `tests/cli/test_research_template_command.py` (research_workbook 35 tests)
**Status**: 只审查，不修改 plan/code/tests，不 commit/push。

---

## 总体结论: PASS

**PASS** — v4.5 W16-CTRL-01 erratum 已正确、完整、自洽地修正了 v4.4 plan line 699 的 `list[dict[str, object]]` 类型约束冲突。三 TypedDict 字段与 JSON shape 精确一致，helper exact signature 零 `object`/`Any`，scope 边界清晰（17 存量签名 out-of-scope），ID/时序语义保持，其他 14 slices 零语义变化，35 tests + pyright 0 errors 门禁足够。

**Findings**: High 0 · Medium 0 · Low 4

---

## 1. 验证矩阵

### 1.1 三 TypedDict 字段 vs 实际 JSON shape — PASS

| 验证项 | 预期 | 实际 | 证据 | 结果 |
|--------|------|------|------|------|
| `_ResearchWorkbookEvidence.source: str` | 构造/校验代码要求 `str` | 校验代码 line 251-252: `str(record.get("source", ""))` | `research_workbook.py:251` | ✓ |
| `_ResearchWorkbookEvidence.reference: str` | 同上 | `str(record.get("reference", ""))` | `research_workbook.py:252` | ✓ |
| `_ResearchWorkbookEvidence.finding: str` | 同上 | `str(record.get("finding", ""))` | `research_workbook.py:252` | ✓ |
| `_ResearchWorkbookItem.item_id: str` | 构造代码 `f"item-{item_key}"` | `str` 字面量拼接 | `research_workbook.py:68` | ✓ |
| `_ResearchWorkbookItem.prompt: str` | 来自 `current_items` 元素的字符串 | `prompt` 来自 `- ` bullet 解析 | `research_workbook.py:69,95-97` | ✓ |
| `_ResearchWorkbookItem.status: str` | 初始 `"open"` | `str` 字面量 | `research_workbook.py:70` | ✓ |
| `_ResearchWorkbookItem.response: str` | 初始 `""` | `str` 字面量 | `research_workbook.py:71` | ✓ |
| `_ResearchWorkbookItem.evidence: list[_ResearchWorkbookEvidence]` | 初始 `[]` | 空 list，兼容泛型 | `research_workbook.py:72` | ✓ |
| `_ResearchWorkbookItem.analyst_notes: str` | 初始 `""` | `str` 字面量 | `research_workbook.py:73` | ✓ |
| `_ResearchWorkbookItem.evidence_required: bool` | 初始 `True` | `bool` 字面量 | `research_workbook.py:74` | ✓ |
| `_ResearchWorkbookSection.section_id: str` | 构造代码 `f"section-{section_key}"` | `str` 字面量拼接 | `research_workbook.py:79` | ✓ |
| `_ResearchWorkbookSection.title: str` | `current_title` | 来自 `## ` heading 解析 | `research_workbook.py:80,90` | ✓ |
| `_ResearchWorkbookSection.category: str` | `current_category` | `_research_workbook_section_category(title)` 返回 `str` | `research_workbook.py:81,91,1086` | ✓ |
| `_ResearchWorkbookSection.items: list[_ResearchWorkbookItem]` | `items` 列表 | 内层 `append` 构建 | `research_workbook.py:82` | ✓ |

**全部 14 个字段精确匹配，零遗漏，零多余。**

### 1.2 Helper Exact Signature — PASS

```
plan v4.5 Slice 4 (lines 742-749):
def _append_research_workbook_section(
    sections: list[_ResearchWorkbookSection],
    *,
    normalized: str,
    current_title: str,
    current_category: str,
    current_items: list[str],
) -> None:
```

| 检查项 | 结果 | 证据 |
|--------|------|------|
| 零 `object` | ✓ | 所有参数/返回值均为具体类型 |
| 零 `Any` | ✓ | 同上 |
| 零 `cast` | ✓ | plan line 766 显式禁止 |
| 零 `type: ignore` | ✓ | plan line 766 显式禁止 |
| 零 glue wrapper | ✓ | plan line 766 显式禁止 |
| 模块级 `def`（非闭包） | ✓ | plan line 742，符合 AGENTS.md:42 "优先使用模块级私有辅助函数" |
| `*` 分隔位置/关键字参数 | ✓ | 与模块既有风格一致（`build_research_workbook_payload` 等） |
| 完整中文 docstring | ✓ | plan line 754-755 要求 Args/Returns/Raises |
| 保留 early return guard | ✓ | plan line 754 "保持现有 early return" |
| `section_index = len(sections)` | ✓ | plan line 754，位置索引语义不变 |
| hash material + 截断长度不变 | ✓ | plan line 754-755 |
| 字段值与 append 时序不变 | ✓ | plan line 755 |

### 1.3 Public Builder 与 17 存量签名 Out-of-Scope — PASS

**17 处存量 `dict[str, object]` 统计**（按 occurrence 计数）：

| # | 函数签名 | `dict[str, object]` 出现 | 位置 |
|---|---------|------------------------|------|
| 1 | `build_research_workbook_payload(...) ->` | 1 (return) | line 40 |
| 2-3 | `validate_research_workbook_payload(payload: ...) ->` | 2 (param + return) | line 138 |
| 4 | `build_research_workbook_report(workbook_payload: ...) ->` | 1 (param) | line 299 |
| 5 | `inspect_research_workbook_report(...) ->` | 1 (return) | line 424 |
| 6 | `discover_research_workbook_reports(...) ->` | 1 (return) | line 507 |
| 7 | `build_research_workbook_report_status_snapshot(...) ->` | 1 (return) | line 527 |
| 8-10 | `build_research_workbook_update_preview(workbook_payload: ..., evidence_records: ...) ->` | 3 (param×2 + return) | lines 604, 610, 611 |
| 11-12 | `write_research_workbook_update(..., evidence_records: ...) ->` | 2 (param + return) | lines 699, 700 |
| 13 | `build_research_workbook_rollback_preview(...) ->` | 1 (return) | line 741 |
| 14 | `write_research_workbook_rollback(...) ->` | 1 (return) | line 815 |
| 15 | `inspect_research_workbook(...) ->` | 1 (return) | line 879 |
| 16 | `discover_research_workbooks(...) ->` | 1 (return) | line 923 |
| 17 | `build_research_workbook_status_snapshot(...) ->` | 1 (return) | line 938 |
| — | `_load_json_object(...) -> dict[str, object]` | (private, 不在 public scope) | line 1017 |
| — | `_sha256_json_object(payload: dict[str, object]) -> str` | (private, 不在 public scope) | line 1076 |

**总计：13 个函数签名含 17 处 `dict[str, object]`。** plan line 764 表述 "17 个存量签名" 系 occurrence 计数（非独立函数签名数），语义精确度有微小偏差（见 Low finding #1），但不影响 scope 边界正确性。

| 检查项 | 结果 | 证据 |
|--------|------|------|
| W16 不修改任何 public 函数签名 | ✓ | 仅新增 `_` 前缀私有函数和 TypedDict |
| `build_research_workbook_payload(...) -> dict[str, object]` 返回类型不变 | ✓ | plan line 763-764 显式声明 |
| `write_research_workbook_update` 的 `evidence_records: list[dict[str, object]]` 不变 | ✓ | 不在 W16 scope |
| 其余 11 个 public 函数签名不变 | ✓ | 同上 |

### 1.4 sections/items 局部类型精化 — PASS

| 变量 | 当前类型 (line) | v4.5 要求类型 | 兼容性 |
|------|----------------|-------------|--------|
| `sections` (line 45) | `list[dict[str, object]]` | `list[_ResearchWorkbookSection]` | ✓ — 同函数内局部注解变更 |
| `items` (helper 内) | 无显式注解 (line 61) | `list[_ResearchWorkbookItem]` | ✓ — 新增局部注解 |
| 返回值 `dict[str, object]["sections"]` | `list[_ResearchWorkbookSection]` 可赋值给 `object` | 兼容 | ✓ — 子类型赋值 |

**关键验证**：`sections` 变量注解从 `list[dict[str, object]]` 改为 `list[_ResearchWorkbookSection]` 是**必须的** — 否则 pyright 的 list 不变性（invariance）会报参数类型不兼容：

```
Argument of type "list[dict[str, object]]" cannot be assigned
  to parameter of type "list[_ResearchWorkbookSection]"
```

plan line 757-759 已显式要求此同步修改。 ✓

### 1.5 Heading/EOF 两 Flush 调用 + ID/时序语义 — PASS

**调用点 1 — heading 切换 flush（原 line 89）**：
```python
if stripped.startswith("## "):
    _append_research_workbook_section(  # flush 上一 section
        sections,
        normalized=normalized,
        current_title=current_title,
        current_category=current_category,
        current_items=current_items,
    )
    current_title = stripped.removeprefix("## ").strip()
    current_category = _research_workbook_section_category(current_title)
    current_items = []
    continue
```

**调用点 2 — EOF flush（原 line 107）**：
```python
_append_research_workbook_section(  # flush 最后一 section
    sections,
    normalized=normalized,
    current_title=current_title,
    current_category=current_category,
    current_items=current_items,
)
```

| 时序语义 | 保证机制 | 提取后是否保持 |
|----------|----------|---------------|
| 首次 heading 前不 flush（`current_category = ""` guard） | helper 内 `if not current_category: return` | ✓ plan line 754 "保持现有 early return" |
| 重复 heading ID 不碰撞 | `section_index = len(sections)` 在 append 前取值，sections 是同一可变列表引用 | ✓ Python 引用语义，与闭包等价 |
| 重复 bullet ID 不碰撞 | `item_index = enumerate(current_items)` 内层索引 | ✓ 不依赖外作用域 |
| hash material 确定性 | `f"{normalized}\0{section_index}\0{category}\0{title}"` | ✓ 所有参数均显式传入 |
| 截断长度 | `[:10]` section, `[:12]` item | ✓ plan line 755 要求保持 |
| `current_items = []`（clear） | 调用点后执行，不在 helper 内 | ✓ 调用点不变（见 Low finding #2） |

**测试覆盖**：`test_build_research_workbook_payload_keeps_ids_unique_for_duplicate_lines`（line 345）构造重复 heading + 重复 bullet 模板，验证 `section_id` 和 `item_id` 唯一性 + 自校验通过。提取后三项断言全部保持。 ✓

### 1.6 Top/Status/Tree/W16/Slice4/Tail 自洽 — PASS

| 自洽检查 | 结果 | 证据 |
|----------|------|------|
| header (line 1) 状态行 `v4.5 CANDIDATE / AWAITING DUAL RE-REVIEW` | ✓ | line 1 |
| header changelog (line 12) 摘要与 Slice 4 实施段一致 | ✓ | 三 TypedDict 字段、helper 签名、两个调用点、scope 边界均对应 |
| §1.7 W16 摘要 (line 329) 与 Slice 4 实施段 (lines 698-781) 一致 | ✓ | 逐项比对通过（见 §1.8） |
| 架构树 (line 370) `research_workbook.py # _append_research_workbook_section` | ✓ | 函数名正确（v4.4 旧名 `_append_current_section` 已修正） |
| Slice 4 header (line 698) "W16 — append_current_section 提取" | ✓ | 标题保留原始 work item 名，正文使用 v4.5 精化名 |
| tail (line ~1640) 状态行与 header 一致 | ✓ | `v4.5 CANDIDATE / AWAITING DUAL RE-REVIEW`，HEAD `81df373` |
| tail `object` 审计表 | ✓ | 3 处有界例外均来自 §4.3 声明的 `print_report` 传播链，非 W16 引入 |
| `Any` / `cast` / `type: ignore` / `glue` / `getattr` / `hasattr` 审计 | ✓ | Slice 4 段零匹配（仅 line 766 "禁止新增" 出现） |

### 1.7 其他 Slices 零语义变化 — PASS

```text
$ grep -n 'Slice [12356789]\|Slice 1[0-2]' plan.md | grep -i 'w16\|research_workbook\|_append\|TypedDict\|_ResearchWorkbook'
NO CROSS-CONTAMINATION
```

| Slice | 受 W16-CTRL-01 影响 | 证据 |
|-------|-------------------|------|
| Slice 0–3 | 零 | 无 W16/TypedDict 引用 |
| Slice 4 | 全部（erratum target） | plan lines 698-781 |
| Slice 5–12 | 零 | 无 W16/TypedDict 引用 |

### 1.8 §1.7 W16 摘要 vs Slice 4 实施段 — 逐项比对 PASS

| §1.7 (line 329) 承诺 | Slice 4 (lines 698-781) 实现 | 一致 |
|----------------------|---------------------------|------|
| 三个私有 TypedDict 字段列表 | lines 710-737 完整字段定义 | ✓ |
| `_ResearchWorkbookEvidence(source, reference, finding)` | lines 710-715 | ✓ |
| `_ResearchWorkbookItem(7 字段)` | lines 718-727 | ✓ |
| `_ResearchWorkbookSection(4 字段)` | lines 730-737 | ✓ |
| exact helper signature | lines 742-749 | ✓ |
| `sections: list[_ResearchWorkbookSection]` 局部精化 | lines 757-758 显式列出 | ✓ |
| `items: list[_ResearchWorkbookItem]` 局部精化 | line 759 显式列出 | ✓ |
| 两个调用点替换 | lines 760-762 描述 | ✓ |
| public builder `dict[str, object]` 不变 | lines 763-764 | ✓ |
| 17 存量签名 out-of-scope | line 764-765 | ✓ |
| 禁止 Any/object/cast/type:ignore/glue | line 766-768 | ✓ |
| 验证: pytest -k research_workbook + pyright | lines 771-775 | ✓ |
| 基线: 35 passed / 0 errors | line 777 | ✓ |

**全部 13 项承诺逐条落地，零遗漏。**

---

## 2. Live 基线确认

```text
pytest tests/cli/test_research_template_command.py -k "research_workbook" -q
→ 35 passed, 177 deselected in 0.82s

pyright dayu/cli/commands/research_workbook.py tests/cli/test_research_template_command.py
→ 0 errors, 0 warnings, 0 informations
```

当前源码处于 **pre-W16 状态**（嵌套闭包未提取）。plan 要求实现后维持 35 passed + 0 errors。基线已确认。 ✓

---

## 3. Adversarial Failure Pass

### 3.1 反例注入 — 全部通过

| 反例场景 | 注入方式 | 防御机制 | 结果 |
|----------|---------|---------|------|
| helper 签名引入 `object` | 验证每一参数/返回值注解 | TypedDict 全部精确类型 | 零 `object` ✓ |
| TypedDict 漏字段 | 逐字段对 `research_workbook.py:62-84` | 14/14 字段全部覆盖 | ✓ |
| TypedDict 多字段（过约束） | 对构造代码反查 | 无多余字段 | ✓ |
| `sections` 局部类型忘记同步 | pyright list invariance 检查 | plan line 757-758 显式要求 | ✓ |
| `items` 局部类型缺失 | plan line 759 显式要求 | 实施后 pyright 检查 | ✓ |
| 从 `dayu.fins` 导入 `JsonObject` | plan line 88 "禁止跨模块 alias" | 未采用 C1 方案 | ✓ |
| `total=False` 语义破坏 | plan 未声明 `total` | 默认 `total=True` — 所有字段 Required | ✓ |
| `_ResearchWorkbookEvidence` 字段过少（遗漏 `date` 等） | 对照校验代码 line 251-252 | 仅 `source/reference/finding` 三个必需字段 | ✓ |
| 提取后 `section_index` 引用不同对象 | Python 引用语义 | 传入同一 `sections` 列表引用 | ✓ |
| BOM 模板第一 section 丢失 | early return guard 相同 | helper 内 `if not current_category: return` | ✓ |
| 其他 slice 被 W16 污染 | grep cross-contamination | 零匹配 | ✓ |

### 3.2 `_ResearchWorkbookEvidence` 过约束评估（Low Finding #3 根因分析）

`_ResearchWorkbookEvidence` 定义三个 Required `str` 字段 `source/reference/finding`。在以下场景评估风险：

| 场景 | 触发条件 | 影响 |
|------|----------|------|
| `_append_research_workbook_section` 构造 | 始终创建 `evidence: []`（空列表） | 零风险 — 空列表兼容任何 `list[T]` |
| `build_research_workbook_payload` 内 `items: list[_ResearchWorkbookItem]` | items 的 evidence 字段均为 `[]` | 零风险 — 构造时无 evidence 数据 |
| 下游 `write_research_workbook_update` 追加 evidence | 通过 `dict[str, object]` 访问，不经过 TypedDict | 零风险 — 绕过类型检查 |
| 未来代码尝试用 `_ResearchWorkbookEvidence` 构造含额外字段的 evidence | 需同时修改 constructor + TypedDict 定义 | 极低 — TypedDict 定义在本模块内，修改成本最小 |
| pyright reportExcessFields | 取决于 pyright 配置 | standard 模式（项目当前）不报 excess fields |

**结论**：`_ResearchWorkbookEvidence` 在当前 W16 scope 内零风险。即使未来 evidence 结构扩展（如新增 `date`、`confidence` 字段），仅需更新 TypedDict 定义和构造代码，属于正常的 schema 演进。

### 3.3 `from __future__ import annotations` 兼容性

`research_workbook.py` line 3 有 `from __future__ import annotations`（PEP 563）。已通过 `python -c` 验证 TypedDict + `__future__` annotations 完全兼容：

```
class _Evidence(TypedDict):
    source: str
    reference: str
    finding: str

# with __future__ annotations:
# - 运行时: annotations 为字符串（延迟求值）
# - pyright: 编译期解析，不受影响
# - TypedDict: 正常工作，isinstance(s, dict) == True
```

已验证：dict 兼容性、属性访问、mutation、evidence extend 全部正常。 ✓

---

## 4. Findings

### High: 0

无。

### Medium: 0

无。潜在 `_ResearchWorkbookEvidence` 过约束经 adversarial analysis 确认为 Low severity（当前 scope 内零触发路径，见 §3.2）。

### Low: 4

#### L1: "17 个存量签名" 计数表述精度

- **位置**: plan line 764 + header line 12 + §1.7 line 329
- **事实**: 实际为 **13 个函数签名**含 **17 处** `dict[str, object]` occurrence（含 2 个 private helper `_load_json_object`/`_sha256_json_object` 则为 15 个签名/19 处）
- **影响**: 零。不影响 scope 边界正确性 — "不在 W16 范围内" 对 13 或 17 均成立
- **建议**: 可统一表述为 "本模块包含该 builder 在内的所有存量 `dict[str, object]` 签名（共 13 函数 17 处 occurrence）" — 不阻塞

#### L2: `current_items = []` 时序未显式标注

- **位置**: plan lines 760-762（描述两个调用点，未提及 `current_items = []`）
- **事实**: 两个调用点后均紧接 `current_items = []`（heading flush 后 line 92，EOF flush 后隐式结束）。此 clear 在调用点执行，不在 helper 内 — 语义正确
- **影响**: 零 — plan 已说明 "替换两个调用点"，隐式保留调用点上下文不变。但若实施者疏忽将 `current_items = []` 移入 helper 内，会导致第一个 heading 后的 bullet 丢失（因为 helper 清空了 items 但调用方期望 OLD section 被 flush 后 OLD items 被丢弃）
- **建议**: 实施时在 helper docstring 或调用点注释中显式标注 "调用方负责在调用后执行 `current_items = []`"

#### L3: `_ResearchWorkbookEvidence` 与 `deepcopy(evidence_records)` 的类型分界

- **位置**: `research_workbook.py:654` `evidence.extend(deepcopy(evidence_records))` 与 `_ResearchWorkbookEvidence` TypedDict
- **事实**: `evidence_records` 的公开 API 类型为 `list[dict[str, object]]`（`write_research_workbook_update` line 699）。如果未来 `evidence_records` 也改用 `list[_ResearchWorkbookEvidence]`，`deepcopy` 的返回类型推断可能需要显式注解
- **影响**: 当前 W16 scope 内零影响 — `build_research_workbook_update_preview` 通过 `dict[str, object]` 边界访问 payload，不经过 TypedDict 类型
- **建议**: 不阻塞。未来全面 TypedDict 化的独立 work unit 需注意此边界

#### L4: plan 正文未显式覆盖 `from __future__ import annotations` 兼容性

- **位置**: plan Slice 4 段（lines 698-781）
- **事实**: plan 未提及 `from __future__ import annotations` + TypedDict 交互。此分析在 002854 review §4.1 中完成，已验证通过
- **影响**: 零 — 已验证兼容，plan 非必须包含此实现细节
- **建议**: 可选，在实施 checklist 中加一行 "确认 `from __future__ import annotations` 与 TypedDict 兼容" — 不阻塞

---

## 5. 验证命令（实施后门禁）

```bash
# 所有 research_workbook 测试
pytest tests/cli/test_research_template_command.py -k "research_workbook" -q
# 预期: 35 passed

# 类型检查
pyright dayu/cli/commands/research_workbook.py tests/cli/test_research_template_command.py
# 预期: 0 errors, 0 warnings, 0 informations

# 额外: 确认 return type 的 dict[str, object] 兼容
pyright dayu/cli/commands/research_workbook.py --verifytypes
```

---

## 6. 最终裁决

| 维度 | 结果 |
|------|------|
| 三 TypedDict 字段 vs JSON shape | **PASS** — 14/14 精确一致 |
| helper exact signature 零 object/Any | **PASS** — 全部精确类型 |
| public builder + 17 存量 out-of-scope | **PASS** — scope 边界清晰 |
| sections/items 局部类型精化 | **PASS** — plan 显式要求同步修改 |
| heading/EOF 两 flush ID/时序语义 | **PASS** — 引用语义 + early return |
| top/status/tree/W16/Slice4/tail 自洽 | **PASS** — 13/13 承诺逐条落地 |
| 其他 slices 零语义变化 | **PASS** — 零 cross-contamination |
| 35 tests + pyright 门禁 | **PASS** — 基线 35 passed / 0 errors |
| AGENTS.md 硬约束合规 | **PASS** — 零新增 object/Any |
| **总评** | **PASS** |

**v4.5 W16-CTRL-01 erratum 可以 ACCEPT。Slice 4 可以按修正后的 plan 实施。**
