# Slice 11 Code Review — Corrective Re-Review (MiMo)

**Baseline**: `122acd8 gateflow: accept cli write architecture plan v5.5`
**Branch**: `codex/dual-model-research-mvp`
**Scope**: `dayu/cli/arguments.py`, `dayu/cli/commands/_write_dispatch.py` (new), `dayu/cli/commands/write.py`, `tests/application/test_write_cli_dispatch.py`, `tests/application/test_write_challenger.py`, `tests/engine/test_cli_running_config.py`, `tests/README.md`
**Reviewer**: MiMo
**Date**: 2026-08-08
**Source review**: `docs/reviews/code-review-20260808-132707.md`
**Adjudication**: `docs/reviews/slice-11-code-review-adjudication-20260808-codex.md`

---

## 初审 scope 错误纠正

初审 artifact（132707）将 reviewer metadata 错误标注为 `codex (independent)`，实际执行 pane 为 MiMo。
初审将 8 个 finding 标记为 OPEN（2H/3M/3L），但其中：

- F-01/F-02：HEAD pre-existing 且 S11-CTRL-02 明确锁定保留，不属于 Slice 11 scope
- F-03/F-05：已有 AST/identity 矩阵覆盖，建议增加等价断言不提升 correctness signal
- F-04：HEAD pre-existing，v5.5 锁定 table-out getattr，Slice 12 未授权 helper extraction
- F-06/F-08：review 自认 acceptable 的 accepted design，非 defect
- F-07：Slice 10 + engine corpus 已覆盖真实 parser 行为，无需 Slice 11 复制

初审将 pre-existing/accepted-plan-preserved/non-defect 建议计为 open 是 scope 错误。

---

## Conformance Checklist（复核后）

| 检查项 | 结果 | 说明 |
|--------|------|------|
| Write Protocol exact20 | PASS | `WriteDispatchArguments` 20 fields (16 bool + 4 str\|None)，AST 测试锁定 |
| DayuCliArguments exact21 | PASS | Research(1) + Write(20) = 21，继承 `argparse.Namespace`，无 `__init__` |
| 4 frozen dataclasses | PASS | `_WriteCommandContext(2)`, `_WriteConfigurationContext(4)`, `_EarlyWriteSubcommandEntry(2)`, `_ConfigurationWriteSubcommandEntry(2)` |
| 16 adapters | PASS | 14 Phase A + 2 Phase B，AST 测试锁定精确名称与顺序 |
| 14+2 phase table entries | PASS | `_WRITE_PHASE_EARLY_RECOVERY(14)` + `_WRITE_PHASE_CONFIGURATION(2)` |
| Mixed selectors priority | PASS | HEAD 优先级语义保持：bool > path > config > summary |
| execution_options per path exact1 | PASS | Phase A: 3 adapters 内联 build=1；Phase B: 主函数预计算 build=1；测试逐路径断言 |
| Phase B 不重算 | PASS | `_WriteConfigurationContext` 接收预计算 `execution_options`，adapter 直接复用 |
| PhaseE inline AST-exact | PASS | `_build_challenger_run_plan_from_args` + `_assert_challenger_run_output_boundaries`，inline block 与 HEAD 一致 |
| PhaseE 5 exceptions | PASS | `FileExistsError, FileNotFoundError, OSError, TypeError, ValueError` → exit 2，测试参数化覆盖 |
| PhaseF fail-loud assert | PASS | `assert challenger_run_plan is not None`，S11-CTRL-02 锁定保留，测试验证 `AssertionError` |
| PhaseH 3 groups exceptions | PASS | S11-CTRL-02 锁定：(1) plan rebuild 4 类 → exit 2；(2) config ValueError → exit 2；(3) materialize broad catch → exit 2 partial-success |
| PhaseH write_exit_code | PASS | `write_exit_code != 0` 时直接返回，不进入 materialize |
| table-out getattr | PASS | S11-CTRL-02 锁定：summary 路径所有 optional 字段通过 `getattr(args, ..., None)` 安全读取 |
| 测试 fixture 真实 parser 缺省 | PASS | exact union AST 门禁 + 真实 `parse_arguments()` 集成测试 + direct-call runtime fixture 三层互补 |
| 大测试矩阵真实断言 | PASS | 覆盖 dispatch 顺序、exit code、build count、side-effect count、identity/order/matrix |

---

## Finding 逐项复核

### F-01: Phase F bare `assert` — CLOSED

**初审判定**: HIGH / OPEN
**Controller 裁决**: rejected-with-reason / pre-existing / out-of-scope
**复核判定**: **CLOSED**

`assert challenger_run_plan is not None` 在 HEAD 已存在。S11-CTRL-02 明确要求保留该 fail-loud assert，并有 `AssertionError` 回归测试覆盖。Slice 11 的职责是原样保持 HEAD 行为，不是改进 `-O` 兼容性。改为显式异常会改变 accepted behavior，违反 AST/行为保持契约。

---

### F-02: Phase H materialize `except Exception` — CLOSED

**初审判定**: HIGH / OPEN
**Controller 裁决**: rejected-with-reason / pre-existing / out-of-scope
**复核判定**: **CLOSED**

Broad `except Exception` 是 HEAD 已有的 partial-success 边界。S11-CTRL-02 明确要求保留该异常组、日志与 `return 2`。Slice 11 原样保持了 HEAD 行为。收窄异常集合会改变 accepted behavior，不属于当前 slice scope。

---

### F-03: 测试 fixture 与 Protocol 字段紧耦合 — CLOSED

**初审判定**: MEDIUM / OPEN
**Controller 裁决**: rejected-with-reason / non-defect
**复核判定**: **CLOSED**

`WriteDispatchArguments` 与 `DayuCliArguments` 的 exact union 已由 `test_protocol_and_dayu_fields_form_exact_union` 的 AST 门禁锁定（断言 exact20/21、set equality、无 `__init__`、structural conformance）。真实 `parse_arguments()` 集成测试验证 argparse 向真实 Dayu 实例写入字段。`_make_write_args` direct-call fixture 覆盖非 parser 测试边界。三层证据互补；建议增加另一份等价字段枚举不提升 correctness signal。

---

### F-04: summary 路径 10 处 `getattr` + `Path.resolve()` 重复模式 — CLOSED

**初审判定**: MEDIUM / OPEN
**Controller 裁决**: rejected-with-reason / pre-existing / out-of-scope
**复核判定**: **CLOSED**

Summary 路径的 optional-path 解析样板来自 HEAD。v5.5 明确锁定 table-out 字段继续使用既有 `getattr` 语义，S11-CTRL-02 也确认保留。Slice 12 未授权该 helper extraction；当前抽取会扩大 ownership 与行为变更范围。

---

### F-05: `_install_write_baseline_mocks` 覆盖全量 16 runner 的维护成本 — CLOSED

**初审判定**: MEDIUM / OPEN
**Controller 裁决**: rejected-with-reason / non-defect
**复核判定**: **CLOSED**

测试已用 `test_phase_tables_bind_exact_adapter_order`（phase table 的 exact adapter identity/order）与 `TestWritePhaseTableGroup1PhaseABool` / `Group2PhaseAPath` / `Group3PhaseBBool`（16-action runtime matrix）锁定完整 runner 集合。Baseline mock helper 的维护成本是已受结构和行为门禁覆盖的未来编辑成本，不是当前 correctness defect。

---

### F-06: Phase A `_build_execution_options` 调用不一致 — CLOSED

**初审判定**: LOW / OPEN
**Controller 裁决**: rejected-with-reason / non-defect
**复核判定**: **CLOSED**

Review 自身明确该差异是 accepted design：只有需要 `ExecutionOptions` 的三个 Phase A adapter（clear/verify/recover）惰性构造，其他 11 个 adapter 不构造。逐路径测试（`TestLazyComputePhaseOrder`）锁定每条路径的 build count=0 或 1。设计意图正确且受测试覆盖，无 defect。

---

### F-07: `parse_arguments` 测试仅覆盖最小 case — CLOSED

**初审判定**: LOW / OPEN
**Controller 裁决**: rejected-with-reason / non-defect
**复核判定**: **CLOSED**

Slice 10 已有 `test_parse_arguments_returns_real_dayu_namespace` 验证真实 parser 返回 Dayu namespace。`test_cli_running_config.py`（10932 行）覆盖全面 parser 行为，包括所有 dispatch 相关字段的解析、默认值和多 flag 组合。review 自身也承认该覆盖存在（"注意 `test_cli_running_config.py` 已有更全面的 `parse_arguments` 覆盖"）。Slice 11 无需复制 parser 参数矩阵。

---

### F-08: 16 个 adapter 是纯透传 wrapper — CLOSED

**初审判定**: LOW / OPEN
**Controller 裁决**: rejected-with-reason / non-defect
**复核判定**: **CLOSED**

Review 自身认可 adapter 是统一 typed context callable 的 accepted v5.5 架构边界。Exact16 adapter 与 identity/order/matrix 门禁已经验证结构与行为。它们不是 compatibility wrapper（不为保持旧导入路径转发符号），也没有新增外部 surface。v5.5 设计选择通过 phase table entry 的 `Callable[[Context], int]` 签名统一 dispatch 接口，adapter 是该契约的必要组成部分。

---

### DS F-10: 测试 wrapper 字段补齐观察 — CLOSED

**DeepSeek 判定**: LOW observation / 已判 PASS
**Controller 裁决**: rejected-with-reason / non-defect
**复核判定**: **CLOSED**

`WriteDispatchArguments` 与 `DayuCliArguments` 的 exact union 由 AST 门禁锁定，真实 `parse_arguments()` 集成测试验证 argparse 向真实 Dayu 实例写入字段，direct-call fixture 的 runtime wrapper 再覆盖非 parser 测试边界。三层证据互补；删除任一 Protocol 字段会先由 exact-union 测试明确失败。该观察不是缺陷。

---

## Summary

| Finding | 初审 Sev | 初审 Status | Controller 裁决 | 复核 Status |
|---------|----------|-------------|-----------------|-------------|
| F-01    | HIGH     | OPEN        | pre-existing / out-of-scope | **CLOSED** |
| F-02    | HIGH     | OPEN        | pre-existing / out-of-scope | **CLOSED** |
| F-03    | MEDIUM   | OPEN        | non-defect      | **CLOSED** |
| F-04    | MEDIUM   | OPEN        | pre-existing / out-of-scope | **CLOSED** |
| F-05    | MEDIUM   | OPEN        | non-defect      | **CLOSED** |
| F-06    | LOW      | OPEN        | non-defect      | **CLOSED** |
| F-07    | LOW      | OPEN        | non-defect      | **CLOSED** |
| F-08    | LOW      | OPEN        | non-defect      | **CLOSED** |
| DS F-10 | LOW      | observation | non-defect      | **CLOSED** |

| Severity | Open | Closed |
|----------|------|--------|
| HIGH     | 0    | 2      |
| MEDIUM   | 0    | 3      |
| LOW      | 0    | 4      |
| **Total**| **0**| **9**  |

---

## Verdict

**PASS**

- Accepted material open finding = 0
- Open High/Medium/Low = 0/0/0
- 无需修改 production、tests、README、master plan 或 source review
- 未执行 `git add`、`git commit`、`git push` 或 `git stash`
