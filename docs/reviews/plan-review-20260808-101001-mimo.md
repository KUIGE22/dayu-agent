# Slice 10 v5.2 Targeted Re-Review（MiMo）

- **审查对象**: `docs/plans/2026-08-07-cli-write-architecture-refactor.md` v5.2 Slice 10 (lines 1492–1704)
- **审查目标**: 核验初审 MiMo 095101 全部 H/M/L findings 是否 CLOSED；确认 S10-CTRL-03/04 裁决与 plan text 一致
- **审查日期**: 2026-08-08
- **审查者**: MiMo (targeted re-review)
- **分支**: `codex/dual-model-research-mvp`
- **HEAD**: `8eface5`
- **输入**:
  - Master plan v5.2: `docs/plans/2026-08-07-cli-write-architecture-refactor.md` (2497 行)
  - Controller erratum: `docs/reviews/plan-slice-10-v5.2-dispatch-invariant-erratum-20260808-codex.md`
  - 初审: `docs/reviews/plan-review-20260808-095101-mimo.md`
  - HEAD 源码: `dayu/cli/commands/research_template.py` (835 行)
  - 测试: `tests/application/test_write_cli_dispatch.py`、`tests/cli/test_research_template_definitions.py`

---

## 0. 方法

逐项核验初审 095101 的 2 HIGH + 2 MEDIUM + 3 LOW findings 对应的 v5.2 plan text 与 S10-CTRL-03/04 裁决，确认行为等价性。只读，不改 plan/code/tests/已有 artifact。

---

## 1. 逐项核验

### H01: runner 异常处理丢失 → **CLOSED**

**初审事实**: Plan entry example 删除 try/except，runner 的 `FileNotFoundError` / `FileExistsError` / `ValueError` 从 "catch + stderr + return 1" 变为 "unhandled exception"。

**v5.2 plan entry (lines 1646–1653)**:
```python
    try:
        runner = _RESEARCH_TEMPLATE_ACTION_DISPATCH.get(action)
        if runner is None:
            return 1
        return runner(args)
    except (FileNotFoundError, FileExistsError, ValueError) as exc:
        print(f"research-template error: {exc}", file=sys.stderr)
        return 1
```

**S10-CTRL-03**: "mapping lookup、runner-none 分支和 runner 调用均置于 `try`；精确保留三类异常、stderr 文本与返回 `1`"。

**等价性**: `try` 覆盖 mapping `.get()` + `runner is None` 分支 + `runner(args)` 调用。catch tuple exact `(FileNotFoundError, FileExistsError, ValueError)`。stderr exact `f"research-template error: {exc}"`。return `1`。与 HEAD lines 46–54 完全等价。✓

---

### H02: 未知 action 退出码/输出漂移 → **CLOSED**

**初审事实**: Plan 使用 `return 2` + `Log.error(...)`，HEAD 对未知 action 静默返回 `1`。

**v5.2 plan entry (lines 1648–1649)**:
```python
        if runner is None:
            return 1
```

**S10-CTRL-03**: "未知 action 静默返回 `1`"；"入口禁止 `Log`、`MODULE`、新错误输出或 `return 2`"。

**等价性**: 无 stdout、无 stderr、return `1`。与 HEAD line 55 `return 1`（无输出）完全等价。MiMo 初审 095101 §3.3 选项 A（完全保持 HEAD 行为）已被采纳。✓

---

### M01: `Log` / `MODULE` 未定义 → **CLOSED**

**初审事实**: Plan entry 引用 `Log.error(...)` 和 `MODULE`，两者均未在 `research_template.py` 中 import/定义。

**v5.2 plan**: entry function body (lines 1644–1653) 不含 `Log` 或 `MODULE`。§4.2 S10-CTRL-03 (line 616): "入口禁止 import/引用 `Log` 或 `MODULE`"。

**等价性**: 入口不引入新依赖面，不新增 research-template 日志常量。与 HEAD 一致。✓

---

### M02: selector 直接字段语义漂移 → **CLOSED**

**初审事实**: Plan `_resolve_research_template_action` 使用 `args.research_template_action.strip().lower()` 直接访问，HEAD 使用 `str(getattr(args, "research_template_action", "") or "").strip().lower()` 防御性编码。

**v5.2 plan selector (line 1552)**:
```python
    return str(getattr(args, "research_template_action", "") or "").strip().lower()
```

**S10-CTRL-04**: "selector exact body 为 `str(getattr(args, "research_template_action", "") or "").strip().lower()`；该唯一有界 `getattr` 只保留 HEAD 对缺字段、`None`、非字符串值的 runtime defensive 语义"。

**等价性**: selector body 与 HEAD line 101 的表达式逐字符一致。`str(...)` + `or ""` + `.strip().lower()` 链完整保留。✓

---

### L01: 39 mapping 一致性 → **CLOSED**

**初审事实**: Plan mapping 39 key 与 HEAD 39 个 `if action ==` 分支一一对应，顺序和 key 字符串完全一致。

**v5.2 plan**: mapping 清单 (lines 1587–1627) 仍为 39 条目，与 §1.5 的 39 个 action key 完全一致。S10-CTRL-03 验证契约: "39-key mapping 参数化矩阵逐项验证 key、runner 与真实调用"。

**无修复项**，确认通过。✓

---

### L02: runner 内部 `getattr` 边界不清 → **CLOSED**

**初审事实**: `DayuCliArguments` 继承 `argparse.Namespace`，runner 内部 `getattr(args, ...)` 行为不变，plan 未明确是否清除。

**v5.2 plan (line 1577)**: "39 runners 内部表外 helper/`getattr` 保持原样"。

**S10-CTRL-04**: "runner 内部表外存量 `getattr` 不在本 Slice 迁移范围"。

**确认为 non-goal**，plan 明确排除。✓

---

### L03: `_resolve_research_template_action` 未在 deliverable 清单中 → **CLOSED**

**初审事实**: helper 在 plan 中定义但未被显式列为 Slice 10 deliverable。

**v5.2 plan (line 1558)**: "selector 是 Slice 10 的明确 deliverable"。

**S10-CTRL-04**: "selector 是 Slice 10 deliverable 但不计入 45 个 `DayuCliArguments` args 签名传播"。

**确认为显式 deliverable**。✓

---

## 2. S10-CTRL-03/04 计数核验

### 2.1 45 args + 1 return 计数

S10-CTRL-04 明确：

| 分项 | 数量 |
|---|---:|
| research-template entry | 1 |
| runners | 39 |
| write entry | 1 |
| Slice 6 config-application | 2 |
| snapshot-builder | 2 |
| **args 签名合计** | **45** |
| parse_arguments return | 1 |

plan §5 Slice 10 传播表 (lines 1563–1571) 逐模块列出上述 45 个目标，闭合。selector 参数为 Protocol（非 `DayuCliArguments`），不计入传播。✓

### 2.2 39-key mapping 与 runners 闭合

plan §1.5 (line 269) 列出 39 个 action key。mapping (lines 1587–1627) 精确 39 条目。传播表 39 runners。三处闭合。✓

### 2.3 entry behavior 与 HEAD 等价矩阵

| 路径 | HEAD | v5.2 plan | 等价 |
|---|---|---|:---:|
| 已知 action, runner 正常返回 0 | return 0 | return 0 | ✓ |
| 已知 action, runner 抛 FileNotFoundError | stderr + return 1 | stderr + return 1 | ✓ |
| 已知 action, runner 抛 FileExistsError | stderr + return 1 | stderr + return 1 | ✓ |
| 已知 action, runner 抛 ValueError | stderr + return 1 | stderr + return 1 | ✓ |
| 未知 action | return 1（无输出） | return 1（无输出） | ✓ |

---

## 3. 残余观察（非 defect，不阻塞）

无新 findings。v5.2 plan text + S10-CTRL-03/04 已完整覆盖初审全部 7 项。plan verification gates (lines 1679–1703) 显式列出 stale audit（`Log`/`MODULE`/未知 action 新输出/`return 2` 为 0、selector exact bounded fallback 为 1），与 S10-CTRL-03 闭合。

---

## 4. Verdict

**PASS**

初审 MiMo 095101 的 2 HIGH + 2 MEDIUM + 3 LOW findings 全部 **CLOSED**。v5.2 plan entry 与 HEAD `run_research_template_command` 在全部行为路径上等价。S10-CTRL-03/04 裁决已完整写入 plan text。45 args + 1 return 计数闭合。39-key mapping 与 runners 一致。selector exact fallback 保留 HEAD defensive 语义。open High/Medium/Low = **0/0/0**。

Slice 10 v5.2 达到 code-generation-ready 标准，可在 dual re-review PASS 后实施。

---

*本 review 仅做 plan artifact 层面的 targeted re-verification，不修改 plan/code/tests/已有 artifact。*
