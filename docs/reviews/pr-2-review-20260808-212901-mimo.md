# Code Review

## Scope

- Mode: PR
- Branch or PR: PR-2, https://github.com/KUIGE22/dayu-agent/pull/2；head `codex/dual-model-research-mvp-recovered` @ `783b15a`
- Base: 实际使用 `origin/main`（merge-base `2115c86`）。本地无 `main` 分支，`git diff main...HEAD` 报 `fatal: ambiguous argument`，故按 `origin/main...HEAD` 执行，此为对 handoff 所述 base 的 scope 偏差。
- Output file: `docs/reviews/pr-2-review-20260808-212901-mimo.md`
- Included scope: `origin/main...HEAD` 完整 diff（265 文件，+66689/-11415）；重点走读 `dayu/redaction.py`、`dayu/engine/model_circuit_breaker.py`、`dayu/engine/async_anthropic_runner.py`、`dayu/contracts/model_usage.py`、`dayu/contracts/model_failover.py`、`dayu/services/internal/write_pipeline/model_usage_ledger.py`、`dayu/cli/arguments.py`、`dayu/cli/commands/research_template.py`、`utils/ci_pr_pyright.py`、`utils/codex_review_gate.py`、`.github/workflows/ci-pr-required.yml`、`.github/workflows/dual-model-gates.yml`；核对 `CLAUDE.md`、aggregate validation / adjudication / RT-01 artifacts；独立复跑 pyright ratchet 与两条测试 lane。
- Excluded scope: `dayu/services/write_model_configuration_*` 约 20 个新增 service 模块与 write_pipeline 大部分内部实现未逐行走读（合计约 2.5 万新增行）；GitHub PR metadata 与 CI check 状态未读（`gh` 未认证）。
- Parallel review coverage: 无。三次 subagent launch 均以 `API Error: 400 本站仅支持以下模型` 失败（含改用 `sonnet` 重试），并行专项 review 不可用，全部结论由主 reviewer 直接走读得出。

## Findings

### 1-accepted-已修复-[高]-密钥脱敏只覆盖 `sk-` 前缀，Gemini/Serper/FMP 真实密钥会原文写入 stderr

> **最终状态（corrective re-review 回写）**: `accepted` → **已修复**，裁决 ID `PR2-SEC-01`。`AIza` 静态形状、动态环境值精确脱敏、16 字符短值门槛、Git SHA 负例、两个 scanner 显式 matcher 均已实测确认。首轮后另闭环了 DeepSeek 复现的动态匹配误报：新增环境候选资格门（无空白 + 至少两类字符，或 >=32 位纯 hex），实测普通短语/纯字母长词不再误报、hex key 仍精确匹配、未配置 Git SHA 不误报、两 scanner 端到端 `ok=True`。复核详见 `docs/reviews/pr-2-rereview-20260808-230101-mimo.md`。
- **入口/函数**: CLI 参数解析失败路径，`RedactingArgumentParser.error()` / `format_usage()` / `format_help()`
- **文件(行号)**: `dayu/redaction.py:27`（`SECRET_KEY_PATTERN`）、`:47`、`:63`、`:79`、`:93`、`:107`
- **输入场景**: 用户误把密钥贴到期望数值的 CLI 参数上，例如 `--count AIzaSyA1b2C3d4E5f6G7h8I9j0KlMnOpQrStUvWx`（Gemini）或 `--count 0123456789abcdef0123456789abcdef01234567`（Serper/FMP 十六进制形态）。
- **实际分支**: `argparse` 类型转换失败 → `error()` → `redact_secret_shapes(message)` → `SECRET_KEY_PATTERN.sub(...)` 未命中 → 原文透传。
- **预期行为**: 该模块存在的唯一目的是防止密钥出现在 usage/help/error 输出中，应覆盖本项目实际使用的全部密钥形态。
- **实际行为**: 仅 `sk-` 前缀被替换为 `<redacted>`。实测（真实构造 `RedactingArgumentParser` + `--count type=int`）：`sk-abcdefghij0123456789abcdef` → `invalid int value: '<redacted>'`（已脱敏）；`AIzaSyA1b2C3d4E5f6G7h8I9j0KlMnOpQrStUvWx` → `invalid int value: 'AIzaSyA1b2C3d4E5f6G7h8I9j0KlMnOpQrStUvWx'`（**明文泄漏**）；`0123456789abcdef0123456789abcdef01234567` → **明文泄漏**。
- **直接证据**: 正则 `dayu/redaction.py:27` 硬绑定 `sk-` 字面前缀，无其它 alternation。项目确实使用非 `sk-` 密钥：`dayu/contracts/env_keys.py:13 SERPER_API_KEY_ENV`、`:16 FMP_API_KEY_ENV`；`dayu/cli/commands/init.py:315 api_key_name="GEMINI_API_KEY"`、`:207 api_key_name_override="MIMO_API_KEY"`。同一盲区被 `utils/codex_review_gate.py` 的 `HANDOFF_SECRET_SCAN_PATHS` 复用，故 handoff 文档密钥扫描存在同样漏检。
- **影响**: 静默失效 + 凭据泄漏。密钥进入 stderr 后会被 CI 日志、shell history、终端回滚、工单附件留存；`GEMINI_API_KEY` 与 `SERPER_API_KEY` 均为计费凭据，泄漏后果不可通过重跑消除，只能靠轮换。
- **建议改法和验证点**: 将单一正则改为按 provider 形态组成的模式集合（`sk-`、`AIza[0-9A-Za-z_-]{35}`、`{32,}` 长十六进制串等），或改为「按 `env_keys.py` 已知密钥环境变量的实际取值做值级脱敏」以避免形态猜测。验证点：对每个 `dayu/contracts/env_keys.py` 中声明的密钥环境变量各加一条 `error()` 明文断言，断言输出不含原值。
- **修复风险（低/中/高）**: 低。改动集中在单模块正则/替换逻辑，无跨层影响；需注意长十六进制模式勿误伤 commit sha 等合法输出。
- **严重程度（低/中/高/严重）**: 高

### 2-accepted-已修复-[中]-脱敏测试只断言 `sk-` 形态，导致 finding 1 的盲区对测试完全不可见

> **最终状态（corrective re-review 回写）**: `accepted` → **已修复**，与 finding 1 合并为同一根因与 fix scope（`PR2-SEC-01`）。三个测试文件新增共 362 行，覆盖 argparse stderr、共享原语、两 scanner、同进程轮换、精确值语义、短值负例、matcher/输出策略解耦、Git SHA 负例，以及 corrective 轮新增的普通短语/纯字母/边界反例。本机复跑 focused 261 passed、`dayu/redaction.py` 覆盖率 100%。
- **入口/函数**: `tests/test_codex_review_gate.py` 密钥扫描相关测试
- **文件(行号)**: `tests/test_codex_review_gate.py:32`、`:50`、`:88`、`:121`、`:150`
- **输入场景**: 任何非 `sk-` 前缀密钥形态。
- **实际分支**: 测试仅构造 `key_value = "sk-" + ("A" * 20)`，只走命中分支，未命中分支无覆盖。
- **预期行为**: 安全类断言应覆盖项目实际使用的每种密钥形态，尤其是 negative/未命中路径。
- **实际行为**: 5 处断言全部复用同一 `sk-` 形态，Gemini/Serper/FMP 形态零覆盖；测试全绿但防护实际失效。
- **直接证据**: 上述 5 行均以 `"sk-"` 字面量构造输入；全文件无其它密钥形态字面量。
- **影响**: 仅局部行为错误（测试面），但直接掩盖 finding 1，使该缺陷可长期存活并在后续重构中被判定为「已有测试保护」。
- **建议改法和验证点**: 随 finding 1 修复一并参数化该测试（`pytest.mark.parametrize` 覆盖各 provider 形态），并保留一条「未知形态不应误报」的 negative case。验证点：故意把正则退回 `sk-` 单形态，参数化测试必须失败。
- **修复风险（低/中/高）**: 低。仅测试文件改动。
- **严重程度（低/中/高/严重）**: 中

### 3-rejected-已关闭-[中]-新增 CLI 全量走 `getattr` + `DayuCliArguments` 仅 42 处注解，pyright 对参数链路失去检查能力

> **最终状态（re-review 回写）**: `rejected-with-reason` → **已关闭**（accepted design）。理由：accepted v5.3/v5.4/v5.5 明确把 `DayuCliArguments` 限定为 consumer Protocol 所需 exact21 字段，并保留表外 argparse 字段的存量动态读取，以避免把 Namespace 子类扩成 god bag。**本 finding 标题中的「42 处注解」为我的计数错误**：AST 复核确认 `DayuCliArguments` 恰为 21 个注解字段，原计数误把同文件的 `WriteDispatchArguments`(20) 与 `ResearchTemplateDispatchArguments`(1) 两个独立 Protocol 计入。
- **入口/函数**: 各 `dayu/cli/commands/*` 命令入口读取参数处，例如 `_run_*` 系列
- **文件(行号)**: `dayu/cli/arguments.py:44`（`class DayuCliArguments(argparse.Namespace)`，PR 新增文件，注解字段 42 处）；消费侧如 `dayu/cli/commands/research_template.py:611`、`:1207`、`:1236`
- **输入场景**: 任何 CLI 子命令调用；以及重命名/删除某个参数后未同步全部消费点的演进场景。
- **实际分支**: 参数不经 typed 字段读取，而走 `getattr(args, "...")`，其中大量带静默默认值。
- **预期行为**: 按 CLAUDE.md，`hasattr`/`getattr` 需充分理由且不得用于逃避类型与边界设计；参数应经 typed 字段消费，使 pyright 能静态发现参数名漂移。
- **实际行为**: PR 新增的 21 个 `dayu/cli/*` 文件含 **396** 处 `getattr(`（`origin/main` 基线同目录仅 **70** 处），显著扩散而非收敛。AST 统计 `research_template.py` 内 **94 处带静默默认值**（`write` ×17、`overwrite` ×16、`base` ×16、`output` ×13、`json` ×11、`recursive` ×6 …），39 处无默认值。典型 `research_template.py:611`：`_load_json_object(Path(str(getattr(args, "source_map"))).resolve())` —— 参数名写错或被重命名时不报错，`str(None)` 会被当作路径 `"None"` 继续下游。
- **直接证据**: `git diff origin/main...HEAD --diff-filter=A -- 'dayu/cli/*'` 得 21 个新文件，其 `getattr(` 计数 396；`git grep -ho 'getattr(' origin/main -- 'dayu/cli/*'` 计数 70。`dayu/cli/arguments.py` 为 `A`（新增），注解字段仅 42 处，远少于实际消费的参数数量。
- **影响**: 静默失效 + 可维护性。参数重命名/删除不会被 pyright 或测试捕获，只在运行期表现为「默认值悄悄生效」或以 `"None"` 作为路径的错误行为；这也解释了为何 ratchet 显示 0 诊断——检查面本身被绕过。
- **建议改法和验证点**: 把实际消费的参数补全为 `DayuCliArguments` 注解字段，消费点改为属性访问；确需动态读取的位置集中到少量显式 helper 并说明理由。验证点：删除一个参数定义后 pyright 应报错（当前不报）。
- **修复风险（低/中/高）**: 中。触及全部 CLI 命令入口，需逐命令回归；建议按子命令分批推进而非一次性替换。
- **严重程度（低/中/高/严重）**: 中

### 4-deferred-未修复-[低]-新增 service 层大量 `dict[str, Any]` / `Mapping[str, Any]` 公共签名，违反 CLAUDE.md 类型硬约束

> **最终状态（re-review 回写）**: `deferred-with-owner` → **仍未修复**（严重度由中纠正为低 maintainability debt）。**Owner**: 后续独立 `typed-write-payload-contracts` work unit，由 Service owner / 仓库 maintainer 负责，按 payload family 逐个引入 TypedDict/dataclass。裁决理由：多数签名属 architecture work-unit 前置 payload 契约，accepted v4.4 精确保留了若干 public validator 的 Mapping 边界；本 finding 未提交具体错误 payload、静默分支或相对 accepted plan 的类型扩散证据。**本项未被修复，不得记为 fixed。**
- **入口/函数**: `write_model_configuration_*` 系列 service 的公共函数与 payload 处理 helper
- **文件(行号)**: `dayu/services/write_model_configuration_manual_recovery_clearance.py:351`、`:405`、`:462`、`:480`、`:525`、`:605`、`:641`、`:784`、`:888`、`:948`、`:1060`、`:1119`、`:1155`、`:1174`；同类问题见 `write_model_configuration_rollback_application.py`、`write_model_configuration_application.py` 等
- **输入场景**: 所有经这些 service 流转的配置 payload / routing snapshot / gate state。
- **实际分支**: payload 以 `Mapping[str, Any]` 入参、以 `dict[str, Any]` 出参，键与值类型不受静态约束。
- **预期行为**: CLAUDE.md 明确「禁止使用 `object`、`Any`、无类型参数、无类型返回值」，跨层数据应由 TypedDict / dataclass / Protocol 承载。
- **实际行为**: PR 新增 49 个 `dayu/**/*.py` 中 `Any` 相关注解命中 **773** 处；单文件最高 `write_model_configuration_manual_recovery_clearance.py` 121 处，且 `Any` 出现在公共返回签名（如 `:1155 -> dict[str, Any]`、`:1174 -> tuple[list[dict[str, Any]], list[str]]`）而非仅限内部边界。
- **直接证据**: 上列行号为实际签名位置；`grep -cE '\bAny\b'` 按文件计数排序头部为 121/94/71/67/49/41/35/34/34/32/30/29。
- **影响**: 可维护性 + 静默失效风险。payload 键名拼写错误、结构漂移、`None` 混入均无法被 pyright 捕获，与 finding 3 叠加后本 PR 新增的两大主体（CLI 与 write service）都处于弱类型状态。
- **建议改法和验证点**: 为跨函数流转的 payload / gate state / routing snapshot 定义 TypedDict 或 dataclass，`Any` 只保留在真正的 JSON 解析入口一层并立即收敛。验证点：改造后对错误键名的构造应产生 pyright 报错。
- **修复风险（低/中/高）**: 中。涉及模块多，但多为签名与构造点的机械收敛，可按文件分批。
- **严重程度（低/中/高/严重）**: 中

### 5-rejected-已关闭-[中]-required CI 的 pyright 由全量门禁降级为 ratchet，且 checkout 未取 base sha

> **最终状态（re-review 回写）**: `rejected-with-reason` → **已关闭**（accepted CI ratchet）。理由：pyright ratchet 是 accepted Slice 13 门禁；`_ensure_ref_available`（`utils/ci_pr_pyright.py:154-174`）的 docstring 已显式声明 `Raises: subprocess.CalledProcessError`，即浅 checkout 下按 base SHA **fail-closed 为设计意图**而非疏漏。GitHub required check 已 success，HEAD/BASE 均零诊断（本机复跑 pyright: 0 errors / 0 warnings / 777 files）。我原将「traceback 文案不美观」定为 Medium 属过度定级。
- **入口/函数**: GitHub Actions required job「Run full-repo pyright diagnostic ratchet」
- **文件(行号)**: `.github/workflows/ci-pr-required.yml`（`- run: pyright` → `python -m utils.ci_pr_pyright --base ... --head ...`；`actions/checkout@v4` 见 `:19`、`:47`，均未设 `fetch-depth`）；`utils/ci_pr_pyright.py:154-174`（`_ensure_ref_available`）
- **输入场景**: 任意 PR 触发 required job。
- **实际分支**: `actions/checkout@v4` 默认 `fetch-depth: 1`，工作区不含 `pull_request.base.sha`；`_ensure_ref_available` 以 `check=True` 执行 fetch。
- **预期行为**: required 门禁应稳定判定类型健康；缺失 base ref 时应给出明确可诊断的失败信息。
- **实际行为**: 门禁语义由「全仓零错误」放宽为「不新增诊断」，既存错误不再阻塞；且在本地模拟缺失 base sha 时 `_ensure_ref_available` 抛出未捕获 `CalledProcessError` traceback（fail-closed，但无可读诊断）。
- **直接证据**: workflow diff 中 `- name: Run pyright / run: pyright` 被替换为 ratchet 调用；两处 `actions/checkout@v4` 无 `fetch-depth`；`utils/ci_pr_pyright.py:154-174` fetch 使用 `check=True` 且调用侧无 `except`。
- **影响**: 观测性缺口 + 门禁强度下降。当前仓库 HEAD/BASE 均为 0 诊断（见下方独立复跑），故**尚无既存错误被放过**，实际损害有限；但配合 finding 3/4 的弱类型面，ratchet 无法阻止「类型检查覆盖不到的新代码」。
- **建议改法和验证点**: 为 checkout 显式设置 `fetch-depth: 0`（或按需 fetch base），并在 `_ensure_ref_available` 失败时输出明确错误文案后退出既定错误码。验证点：构造缺失 base ref 的场景，应得到可读错误而非 traceback。
- **修复风险（低/中/高）**: 低。仅 CI 配置与错误处理分支。
- **严重程度（低/中/高/严重）**: 中

### 6-rejected-已关闭-[中]-`dual-model-gates.yml` 的 `paths:` 白名单不含生产代码，纯生产改动 PR 永不触发该门禁

> **最终状态（re-review 回写）**: `rejected-with-reason` → **已关闭**（workflow scope is explicit）。理由：`dual-model-gates.yml` 是 handoff 文档与配套 gate 脚本的 focused workflow，其 `paths:` 与测试清单精确同源；生产代码由 `ci-pr-required.yml` 覆盖。纯 `dayu/**` 不触发 focused handoff gate 是**职责边界**，不是门禁静默丢失。本 PR 同时修改 handoff 路径，真实 workflow 已运行并成功。
- **入口/函数**: workflow trigger `on: pull_request` 的 `paths:` 过滤
- **文件(行号)**: `.github/workflows/dual-model-gates.yml:6-22`
- **输入场景**: 只修改 `dayu/**` 生产代码、不动 handoff 文档与 4 个 `utils/*` 门禁脚本的 PR。
- **实际分支**: 路径不匹配白名单任一条目 → workflow 不被调度 → 门禁显示为「未运行」而非「失败」。
- **预期行为**: 若该门禁用于保障双模型流水线契约，则涉及其所约束语义的生产改动应触发。
- **实际行为**: 白名单仅覆盖 workflow 自身、`DEEPSEEK_INBOX.md` / `CODEX_REVIEW.md` / `spec.md` / `architecture.md` / `task.md` / `progress.md` / `decisions.md` / `test_plan.md`、`docs/handoff/**`、4 个 `utils/*` 脚本及其 5 个测试文件；无任何 `dayu/**` 条目。
- **直接证据**: `.github/workflows/dual-model-gates.yml:6-22` 白名单逐条列出，全部为文档与门禁脚本路径。
- **影响**: 观测性缺口。门禁「静默不运行」比「失败」更危险——PR 页面不会出现红色信号，易被解读为已通过。
- **建议改法和验证点**: 明确该门禁意图：若仅校验 handoff 文档一致性，则在 workflow 名称/描述中写明适用范围以免误读为生产门禁；若需约束生产语义，则把相关 `dayu/**` 路径纳入白名单。验证点：提交一个纯 `dayu/**` PR，确认门禁按预期运行或按预期明确不适用。
- **修复风险（低/中/高）**: 低。仅 trigger 配置。
- **严重程度（低/中/高/严重）**: 中

### 7-rejected-已关闭-[中]-validation artifact 记录的 tree hash 与被 review 的 head 不一致，且所述 HEAD 的 tree 对象已不可读

> **最终状态（re-review 回写）**: `rejected-with-reason` → **已关闭**（historical artifact, current evidence exists）。理由：该 aggregate validation artifact 记录的是其**当时** HEAD，并未声称验证未来 commit；后续 RT-01 有独立 implementation、双路 re-review 与 accepted commit，recovery commit 的 tree 与原 accepted head 逐字节一致（本 review 已验 `783b15a^{tree}` == `172de45^{tree}`），且本轮又在 `783b15a` 上复跑 tests/pyright/CI。历史 artifact 不应被改写成当前 commit。
- **入口/函数**: 证据链核对（非运行期缺陷）
- **文件(行号)**: `docs/reviews/aggregate-cli-write-architecture-validation-20260808-codex.md`（212 行，声明 HEAD `4d2535d`、`dayu` tree `5eb6a0e8762de333be4ed57451bd0793e33b98c3`、`tests` tree `2a7d71132f2be9a2cadeb6f34777fc0de73623d8`）
- **输入场景**: 以该 artifact 作为「本 PR 已通过 219/219 pyright 与 7121 全量测试」的验收依据。
- **实际分支**: artifact 所述 tree 与 `783b15a` 实际 tree 不同源。
- **预期行为**: 验收 artifact 声明的 tree hash 应可在被 review 的 head 上复现，否则不能作为该 head 的验收证据。
- **实际行为**: 实际 HEAD `dayu` tree = `3497bc39a51bb45ecc62799054eed7408db41c54`、`tests` tree = `2139f142c9b713fe421f53aabe7d40056ee53cbb`，与 artifact 声明均不匹配；`git cat-file -t bc75156ff98c522d84b09b87f731fdad45e97f35`（`4d2535d` 的 tree）返回 `fatal: could not get object info`，即被验证对象已不可读；tree `5eb6a0e8` 仅由 `a0c469a`、`4c5bb1b` 可达。
- **直接证据**: 上述 `git cat-file` / tree hash 比对结果；另 `git fsck` 报多处 broken links 及 `worktrees/dayu-agent-mimo-review-clean/HEAD: detached HEAD points at nothing`。
- **影响**: 仅证据链可信度问题，非代码缺陷。**缓解事实**：`git diff a0c469a HEAD` 显示 validation 之后的生产代码增量仅 2 文件 / 38 行（`research_template.py` 的 RT-01 修复 + 对应回归测试），故 artifact 结论与当前代码的实质偏离很小。
- **建议改法和验证点**: 由 Controller 在当前 head 上重新生成一份 tree-hash 可复现的验收记录，或在 artifact 中显式标注其对应 commit 已被 rewrite 及增量范围。验证点：新 artifact 声明的 tree hash 可用 `git rev-parse HEAD:dayu` 直接复现。
- **修复风险（低/中/高）**: 低。仅文档与流程动作，不涉代码。
- **严重程度（低/中/高/严重）**: 中

### 8-accepted-已修复-[低]-新增模块公共函数缺失 CLAUDE.md 强制要求的中文 docstring

> **最终状态（re-review 回写）**: `accepted` → **已修复**，裁决 ID `PR2-DOC-01`。AST 复核确认 exact6 全部补齐且三段（参数/返回值/异常）齐全：`model_circuit_breaker.py:100/:126/:215/:246`、`model_usage_ledger.py:520 record`/`:582 build_summary`，且仅新增 docstring 行、未改可执行行为或数据契约。本 finding 中提及的私有 helper docstring 债务（breaker 17 处 / ledger 22 处）不在 exact6 范围，已并入 deferred 的 `production-docstring-compliance` work unit（exact488，owner: 仓库 maintainer/Controller）。
- **入口/函数**: `ModelUsageLedger.record` / `build_summary`；`model_circuit_breaker` 的 `locked_entry` / `reset`
- **文件(行号)**: `dayu/services/internal/write_pipeline/model_usage_ledger.py:520`、`:564`；`dayu/engine/model_circuit_breaker.py:100`、`:113`、`:190`、`:208`
- **输入场景**: 不适用（静态规范违反）。
- **实际分支**: 不适用。
- **预期行为**: CLAUDE.md 要求「函数必须提供完整中文 docstring，至少包含参数、返回值、异常」。
- **实际行为**: AST 检查确认上述公共函数 docstring 为 `None`；`record` 另在 `:544` 使用 `dict[str, Any]`（与 finding 4 同类）。同一模块内私有 helper 缺失 docstring 22 处 / breaker 17 处，按项目约束私有 helper 要求较弱，此处只报公共面。
- **直接证据**: `ast.get_docstring()` 对上述函数返回 `None`（逐文件 AST 扫描结果）。
- **影响**: 仅局部可维护性。`record` 与 `build_summary` 是账本对外写入与汇总入口，缺少契约说明会增加后续误用风险。
- **建议改法和验证点**: 补齐参数/返回值/异常三段中文 docstring；`record` 的 payload 类型随 finding 4 一并收敛。验证点：AST 扫描新增模块公共函数 docstring 缺失数为 0。
- **修复风险（低/中/高）**: 低。
- **严重程度（低/中/高/严重）**: 低

## 已核实为正确、明确不作为 finding 的项

以下项经沿真实代码路径走读或实测验证后判定正确，记录以避免 Controller 重复裁决：

- `dayu/engine/model_circuit_breaker.py` 的 CLOSED/OPEN/HALF_OPEN 状态机与 generation 栅栏：实测「并发探针被 `half_open_probe_in_flight` 拒绝」「cooldown 再次到期后 generation 递增至 2 并 `half_open_probe_recovered`」「gen-1 陈旧成功被忽略」「CLOSED 后 gen-1 陈旧失败被忽略」，行为正确。
- `dayu/engine/async_anthropic_runner.py` 畸形响应处理：`missing content`、`content` 为 dict、`content: null` 三种输入均收敛为 `choices=[]`，由基类 `_process_non_stream` 产出 `error_event(..., error_type="response_error")`，未被当作成功。
- `dayu/contracts/model_usage.py` / `model_failover.py` 双 dialect usage 归一化（`prompt_tokens`/`completion_tokens` 与 `input_tokens`/`cache_read_input_tokens`/`cache_creation_input_tokens`/`output_tokens`）正确。
- RT-01 修复真实存在且非文档粉饰：`git diff a0c469a HEAD` 显示 `return 0` → `return 0 if result.get("ok") is True else 1` 且 docstring 同步为「校验成功时返回 0，校验失败时返回 1」，6 个 validator（L479/595/745/850/1009/1171）语义一致。
- recovery commit 未掩盖或改变代码：`783b15a^{tree}` == `172de45^{tree}` == `bece4776515c00585cbc6d0a026c79205f181e82`，且 `git diff 172de45 HEAD` 为空。其代价是单次 squash（`git diff 4586e04 783b15a` = 265 文件 / +66689/-11415）摧毁了 per-slice 可 review 历史，此点已并入 finding 7 的证据链语境。

## 独立复跑结果（本机）

- pyright ratchet：`PASS`，但 HEAD 诊断 0 条 / BASE 诊断 0 条，与 validation artifact 声明的 219/219 不一致。`pyrightconfig.json` 未被本 PR 修改，差异归因于环境而非配置放宽。
- focused dual-model lane：`699 passed in 17.94s`，与 artifact 声明一致。
- required lane `pytest -q --timeout=120 -m "not integration and not slow and not e2e" -p no:randomly`：`1 failed, 7121 passed, 5 skipped, 9 deselected`。唯一失败 `tests/engine/test_web_tools.py::test_search_with_serper_requires_api_key` 经核实为 **pre-existing + environment-only，不计入本 PR finding**：该测试在 `origin/main` 上已存在（`git show origin/main:tests/engine/test_web_tools.py` 命中 1 处），本 PR 对该文件的新增仅为 `test_search_web_auto_suppresses_auth_failed_key_until_key_changes`；失败原因是本机 `SERPER_API_KEY` 已导出而该测试未 `monkeypatch.delenv`（同文件 `:384-385`、`:797-798`、`:889` 等其它测试均有 delenv）。CI 无该环境变量时应通过。

## Open Questions

- 无法读取 PR-2 的 GitHub metadata 与 CI check 状态（`gh` 未认证，`gh pr view 2` 要求 `gh auth login`），故无法核对「PR 页面上 required 门禁的实际结论」是否与本地复跑一致；按 skill 约束不编造 PR facts。
- `dual-model-gates.yml` 的设计意图未在 diff 中明确：finding 6 的严重度取决于它是「handoff 文档一致性门禁」还是「生产语义门禁」，需 Controller 确认后再定级。

## Residual Risk

- **未走读区域**：`dayu/services/write_model_configuration_*` 约 20 个新增 service 模块与 write_pipeline 大部分内部实现未逐行走读，合计约 2.5 万新增行。这些模块正是 finding 4 弱类型问题最集中处，其中可能存在未被发现的状态机 / 幂等 / 部分提交缺陷。
- **并行 review 完全不可用**：三次 subagent launch 均因 `API Error: 400` 失败，覆盖面仅为单 reviewer 直接走读所能达到的范围，深度与广度均低于本 PR 规模应有的水平。
- **类型门禁实际覆盖面存疑**：本机 pyright HEAD/BASE 均 0 诊断，与 artifact 的 219/219 差异未完全归因；结合 finding 3/4，「0 诊断」更可能反映检查面被 `getattr` 与 `Any` 绕过，而非代码类型健康。
- **`git fsck` broken links 与失效 worktree HEAD** 表明仓库对象图已受 history rewrite 影响，后续任何依赖历史 commit 的复核（bisect、per-slice diff、artifact 追溯）可能无法完成。
- **强制记录的 Ruff residual（artifact §8 要求不得省略）**：positive 0 / negative 29，mandatory PR residual 216 —— B009:53、BLE001:3、FURB162:12、ISC004:1、RUF010:22、RUF022:2、SIM102:1、TRY004:118、UP012:2、UP035:2。本次未独立复跑 Ruff。
- **测试缺口**：除 finding 2 的脱敏形态缺口外，`test_search_with_serper_requires_api_key` 暴露出该测试文件存在环境隔离不一致（部分测试 delenv、部分不 delenv），属 pre-existing 卫生问题，会使本地与 CI 结果分歧。

## Verdict

> **最终状态（corrective re-review 回写 2026-08-08）**：本节记录的是**初审当时**的结论。经 Controller 裁决、修复复核与 SEC 误报闭环的 corrective re-review 后，最终结论为 **PASS**，open High/Medium/Low = **0/0/0**。deferred-with-owner 事项 2 项另列且**不计入 open**。逐项状态映射见下方表格，完整复核见 `docs/reviews/pr-2-rereview-20260808-230101-mimo.md`。以下初审原文保留不改写。

### 初审最终状态映射

| 初审 finding | 初审严重度 | 最终状态 | Owner（如适用） |
|---|---|---|---|
| 1 — 脱敏只覆盖 `sk-` | 高 | **accepted-已修复**（PR2-SEC-01，含误报闭环复测） | — |
| 2 — 脱敏测试只断言 `sk-` | 中 | **accepted-已修复**（PR2-SEC-01 合并根因；已补普通短语/纯字母/边界反例） | — |
| 3 — CLI `getattr` / 类型面 | 中 | **rejected-已关闭**（accepted design；exact21 符合计划，42 计数有误） | — |
| 4 — service 层 `Any` 签名 | 中 | **deferred-未修复**（纠正为 Low，**不计入 open**） | `typed-write-payload-contracts` work unit — Service owner / 仓库 maintainer |
| 5 — pyright ratchet 门禁降级 | 中 | **rejected-已关闭**（accepted Slice 13；fail-closed 为设计意图） | — |
| 6 — `dual-model-gates.yml` paths | 中 | **rejected-已关闭**（focused workflow 职责边界明确） | — |
| 7 — validation artifact tree 不一致 | 中 | **rejected-已关闭**（历史 artifact，未声称验证未来 commit） | — |
| 8 — exact6 缺中文 docstring | 低 | **accepted-已修复**（PR2-DOC-01） | 私有 helper 余债 → `production-docstring-compliance`（exact488，**不计入 open**）— 仓库 maintainer/Controller |

### 初审原文（保留）

**Request changes（不建议以当前状态 merge）。**

阻塞项为 finding 1：`dayu/redaction.py` 是本 PR 新增的安全防护模块，但实测证明它对项目实际使用的 Gemini / Serper / FMP 密钥形态完全无效，而 finding 2 使该缺陷在测试面不可见——「新增了防护」与「防护有效」在此处被混同。finding 1 与 2 修复成本低（限于单模块正则与其参数化测试），建议作为 merge 前置条件。

finding 3、4 属于本 PR 两大主体（新增 CLI 与 write service）的系统性类型弱化，且与 finding 5 的门禁降级形成叠加：既绕过了 pyright 检查面，又放宽了门禁强度。考虑其改动范围，可裁决为 `deferred-with-owner` 并附明确收敛计划，但不宜记为「已通过类型检查」。

finding 7 为证据链问题而非代码缺陷，且 validation 之后的生产增量仅 2 文件 / 38 行，实质风险有限；建议在当前 head 重新生成可复现的验收记录后即可关闭。

- Open High: 1（finding 1）
- Open Medium: 6（findings 2、3、4、5、6、7）
- Open Low: 1（finding 8）

> **（初审计数已被 corrective re-review 取代：最终 open High/Medium/Low = 0/0/0；deferred-with-owner 2 项另列，不计入 open。）**
