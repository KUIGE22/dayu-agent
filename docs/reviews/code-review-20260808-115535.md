# Aggregate Fix RT-01 Implementation — Independent Second-Lane Deep Review

- Reviewer: codex (second lane, independent)
- Date: 2026-08-08T11:55:35Z
- Base: `a0c469af` (HEAD)
- Scope: `dayu/cli/commands/research_template.py`, `tests/cli/test_research_template_command.py`, `docs/reviews/aggregate-fix-rt-exit-implementation-20260808-codex.md`
- Contract: v5.8 AGG-RT-CTRL-01 (`docs/plans/2026-08-07-cli-write-architecture-refactor.md`, lines 2646-2738)

---

## Verdict

**PASS** — open High/Medium/Low = `0/0/0`

No material issues found. Implementation is a precise, bounded fix that exactly satisfies the v5.8 contract.

---

## Scope Verification

| Dimension | Status | Evidence |
|---|---|---|
| Changed files within allowed scope | PASS | Only `research_template.py` (2 lines), `test_research_template_command.py` (+34 lines), and the implementation artifact (+59 lines) differ from HEAD. No other production, test, README, plan, or CI files touched. |
| No unintended scope creep | PASS | `git diff HEAD --stat` shows exactly the 3 files. No changes to dispatch mapping, other runners, exception boundaries, owner/DAG, or manual/rollback labels. |

---

## Dimension Reviews

### 1. Exit Code Correctness (Success/Failure)

**PASS.** Production change at line 614:

```python
# HEAD (broken): always returns 0 regardless of validation result
return 0

# Working tree (fixed):
return 0 if result.get("ok") is True else 1
```

The validator `validate_monitoring_source_map_payload` (`_research_template_core.py:359-428`) has a single return path, always returns `dict[str, object]`, and sets `"ok": not errors` (always `bool`). `result.get("ok") is True` therefore correctly returns `0` when validation succeeds and `1` when it fails.

The expression matches the v5.8 contract exactly (`return 0 if result.get("ok") is True else 1`).

**Sibling validator comparison:** Five other `_run_validate_*` functions use the same pattern. Of those, four add an `isinstance(validation, dict)` guard (e.g., `_run_validate_bundle` at line 763: `isinstance(validation, dict) and validation.get("ok") is True`). The RT-01 fix omits this guard. This is **not a defect** because (a) the contract specifies the exact expression without `isinstance`, (b) the validator is statically typed `-> dict[str, object]` and has a single dict return path, so the guard is unnecessary. The stylistic difference with sibling validators is a pre-existing design choice (the sibling validators were written with defensive `isinstance` checks for different validators; the RT-01 contract intentionally uses the simpler form matching `_run_validate_research_workbook` at line 497).

### 2. stdout Preservation

**PASS.** The `print(json.dumps(result, ensure_ascii=False, indent=2))` call is unchanged. The JSON payload is printed regardless of success/failure, exactly as before. No stdout content was added, removed, or reformatted.

### 3. Exception Behavior

**PASS.** No try/except blocks were added, removed, or modified. The existing exception propagation paths (`FileNotFoundError`, `OSError`, `ValueError` from `_load_json_object` and the validator) remain unchanged. The docstring `Raises` section accurately documents these.

### 4. Dispatch / Action Key / Owner / DAG

**PASS.** The dispatch mapping at line 1672 (`"validate-source-map": _run_validate_source_map`) is unchanged. No action keys were added or removed. Owner and DAG structure untouched.

### 5. Docstring

**PASS.** Line 602 changed from `校验结果成功输出时返回 0。` to `校验成功时返回 0，校验失败时返回 1。` — accurately reflects the new return semantics. Args and Raises sections unchanged and correct.

### 6. Type Safety

**PASS.** The expression `result.get("ok") is True` returns `int` (0 or 1), matching the function's `-> int` return annotation. No `Any`, `object`, or untyped parameters introduced. Pyright confirms 0 errors.

### 7. Tests

**PASS.**

- **Valid regression (pre-existing):** `test_run_validate_source_map_command_outputs_validation_json` at line 3265 — uses `consumer`/`consumer` inputs, asserts `result == 0` and `payload["ok"] is True`.
- **Invalid regression (new):** `test_run_validate_source_map_command_returns_nonzero_for_template_mismatch` at line 3291 — uses `financial` rules + `consumer` source-map (template mismatch per v5.8 L-PLAN-01 specification), asserts `result == 1`, `payload["ok"] is False`, `payload["errors"]` non-empty.

Both tests use the real CLI entry path `run_research_template_command(args)` → dispatch → `_run_validate_source_map`, exercising the full path including JSON loading, validation, and exit code propagation. No hand-written payloads. Test construction matches the v5.8 contract's locked `financial`/`consumer` mismatch exactly.

Test docstring follows project conventions (Chinese, Args/Returns/Raises).

### 8. Coverage

**PASS.** Implementation artifact reports `304/378 = 80.42%` for the production file, satisfying the `>=80%` threshold.

### 9. Implementation Artifact

**PASS.** `docs/reviews/aggregate-fix-rt-exit-implementation-20260808-codex.md` is a well-formed implementation record documenting scope, verification results, environment notes, and plan conformance. No factual errors found against the code diff.

---

## Observations (Non-blocking)

None.

---

## Summary

The Aggregate Fix RT-01 implementation is a precise, minimal, contract-faithful correction of a single bug: `_run_validate_source_map` previously always returned exit code 0 regardless of validation outcome. The fix correctly maps `ok=True` → exit 0 and `ok=False` → exit 1, matching the pattern used by the sibling `_run_validate_research_workbook` validator and exactly satisfying the v5.8 AGG-RT-CTRL-01 contract. The new test exercises the failure path through the real CLI dispatch with the contractually mandated `financial`/`consumer` template mismatch. No unintended changes to dispatch, exception behavior, stdout, owner/DAG, or other runners.
