# Final Focused Re-Review: v4.5 W16-CTRL-01

**Review ID**: `plan-review-20260808-005559`
**Date**: 2026-08-08
**Reviewer**: Codex (最终 focused re-review)
**Scope**: 对 v4.5 W16-CTRL-01 做最终 focused re-review。重点确认 L1 已准确修正、L2-L4 Controller NON-DEFECT 裁决有源码证据、三 TypedDict/helper/两 flush/35 tests/pyright/其他 slice 零变化仍成立、plan 与 erratum artifact 六处计数自洽。
**Baseline**:
- `docs/plans/2026-08-07-cli-write-architecture-refactor.md` (v4.5 CANDIDATE / AWAITING FINAL DUAL RE-REVIEW)
- `docs/reviews/plan-slice-4-v4.5-type-erratum-20260808-codex.md` (Controller erratum artifact)
- `docs/reviews/plan-review-20260808-004533.md` (Codex 初次 re-review, PASS / L1-L4)
- `docs/reviews/plan-review-20260808-004534.md` (MiMo 初次 re-review, PASS / LOW-01-03)
- `dayu/cli/commands/research_workbook.py` (source truth)
- `AGENTS.md` (硬约束)
**Status**: 只审查，不改 plan/code/tests，不 commit/push。

---

## 总体结论: PASS

**PASS** — v4.5 W16-CTRL-01 erratum 已完成全部修正，三 TypedDict/helper/两 flush/35 tests/pyright/其他 slice 零变化均成立。L1 已在 plan 六处 + erratum 两处统一修正为精确事实。L2-L4 与 MiMo LOW-01/02/03 经 Controller 裁决为 NON-DEFECT，源码证据确凿。零 open findings。

**Findings**: High 0 · Medium 0 · Low 0 · **旧 findings 全部 CLOSED**

---

## 1. L1 修正验证 — CLOSED ✓

### 1.1 源码精确事实

| 类别 | 函数签名数 | `dict[str, object]` 出现次数 |
|------|-----------|---------------------------|
| 非私有函数（含 public builder） | 13 | 17 |
| 私有 helper（`_load_json_object` + `_sha256_json_object`） | 2 | 2 |
| **合计** | **15** | **19** |

逐函数明细已在 004533 §1.3 列出，此处不重复。

### 1.2 六处一致性验证

| # | 位置 | 文本 | 一致 |
|---|------|------|------|
| 1 | plan header changelog (line 12) | "本模块 15 个存量函数签名共含 19 处 `dict[str, object]`（含 public builder 与 2 个私有 helper），均不在 W16 scope" | ✓ |
| 2 | plan §1.7 W16 摘要 (line 329) | 同上 | ✓ |
| 3 | plan Slice 4 实施段 (lines 764-765) | 同上 | ✓ |
| 4 | plan tail 状态行 | "本模块 15 个存量函数签名共含 19 处 `dict[str, object]`（含 public builder 与 2 个私有 helper），均不在 W16 scope" | ✓ |
| 5 | erratum artifact "替代方案裁决" (lines 98-100) | "本模块 15 个存量函数签名共含 19 处 `dict[str, object]`（含 public builder 与 2 个私有 helper），均不是本次新增" | ✓ |
| 6 | erratum artifact "Residual risk" (lines 174-176) | "本模块 15 个存量函数签名共含 19 处 `dict[str, object]`（含 public builder 与 2 个私有 helper），由未来独立 typed-workbook contract work unit 负责" | ✓ |

**六处完全一致。004533 L1 已 CLOSED。** ✓

---

## 2. L2 裁决验证 — CLOSED ✓

**004533 L2**: `current_items = []` 时序未在 plan 中显式标注。

**Controller 裁决**: REJECTED / NON-DEFECT（erratum artifact lines 112-115）。

**源码证据**:

```python
# research_workbook.py lines 86-92
for line in template_path.read_text(encoding="utf-8-sig").splitlines():
    stripped = line.strip()
    if stripped.startswith("## "):
        append_current_section()           # ← 调用点 1（被替换）
        current_title = ...
        current_category = ...
        current_items = []                 # ← line 92: 调用点之后的下一条语句，不在替换范围内
        continue
```

`current_items = []` 是调用点后的独立语句，不属于 `append_current_section()` 调用的替换。plan 说 "替换两个调用点" 自然保留调用点周围的上下文不变 — 这是 Python 代码编辑的基本语义，无需逐行复制到 plan。

**验证通过**: 裁决有源码证据支持。**L2 CLOSED。** ✓

---

## 3. L3 裁决验证 — CLOSED ✓

**004533 L3**: `_ResearchWorkbookEvidence` 与 `deepcopy(evidence_records)` 的类型分界。

**Controller 裁决**: REJECTED / NON-DEFECT（erratum artifact lines 116-120）。

**源码证据**:

```python
# research_workbook.py line 603-604
def build_research_workbook_update_preview(
    workbook_payload: dict[str, object],   # ← 公开 API 使用 dict[str, object]
    ...
    evidence_records: list[dict[str, object]] | None = None,  # ← 同上
) -> dict[str, object]:
```

```python
# line 619-621
updated = deepcopy(workbook_payload)    # updated: dict[str, object]
sections = updated.get("sections")      # sections: object | None
```

```python
# line 627-634
items = section.get("items")            # items: object | None
# ...
item = ...                              # item: dict[str, object] (after isinstance guard)
evidence = item.get("evidence")         # evidence: object | None
```

整个 `build_research_workbook_update_preview` 的数据流完全在 `dict[str, object]` 类型边界内操作。`_ResearchWorkbookEvidence` TypedDict 从未出现在此代码路径中。W16 仅精化 `build_research_workbook_payload` 内的 section **构造**路径，不触及 update 路径。

**验证通过**: 裁决有源码证据支持。**L3 CLOSED。** ✓

---

## 4. L4 裁决验证 — CLOSED ✓

**004533 L4**: plan 未显式覆盖 `from __future__ import annotations` 兼容性。

**Controller 裁决**: REJECTED / NON-DEFECT（erratum artifact lines 121-125）。

**源码证据**:

```python
# research_workbook.py line 3
from __future__ import annotations
```

兼容性验证（004533 §3.3 已执行）：
- TypedDict + `__future__` annotations 在 Python 3.11 完全兼容
- pyright 在编译期解析注解，不受 PEP 563 延迟求值影响
- 运行时 `isinstance(section, dict)` → True
- 项目已有 10+ 文件使用 TypedDict，pyright 0 errors

plan 的 pyright 门禁直接覆盖此类兼容性 — 若 TypedDict 与 `__future__` 不兼容，pyright 会直接报错。

**验证通过**: 裁决有源码证据支持。**L4 CLOSED。** ✓

---

## 5. MiMo LOW-01/02/03 裁决验证 — 全部 CLOSED ✓

Controller 对 MiMo 三项 LOW findings 的裁决（erratum artifact lines 125-135）：

| Finding | 摘要 | Controller 裁决 | 源码证据 |
|---------|------|----------------|---------|
| LOW-01 | `items` 局部变量名遮蔽（`build_research_workbook_payload` 的 `current_items` vs helper 内的 `items`） | REJECTED / NON-DEFECT — 不同作用域，无冲突 | `items` 是 helper 内部局部变量，`current_items` 是调用方局部变量，Python 词法作用域隔离 |
| LOW-02 | `from __future__ import annotations` 对 TypedDict runtime introspection 的影响 | REJECTED / NON-DEFECT — 当前无 TypedDict 运行时反射 | 模块无 `typing.get_type_hints()` 等反射调用 |
| LOW-03 | `total=True` 与未来 schema 演进 | REJECTED / NON-DEFECT — 当前所有字段必填是精确表达 | `total=True` 是 TypedDict 默认值，与当前 JSON shape 完全匹配 |

**全部三项裁决有源码或语言规范证据支持。CLOSED。** ✓

---

## 6. 核心验证项复检

| # | 验证项 | 004533 结果 | 本次复检 |
|---|--------|-----------|---------|
| 1 | 三 TypedDict 14 字段 vs JSON shape | PASS | 未变 — plan lines 710-737 不变 ✓ |
| 2 | helper exact signature 零 object/Any | PASS | 未变 — plan lines 742-749 不变 ✓ |
| 3 | sections/items 局部类型精化 | PASS | 未变 — plan lines 757-759 不变 ✓ |
| 4 | heading/EOF 两 flush ID/时序语义 | PASS | 未变 — plan lines 760-762 不变 ✓ |
| 5 | §1.7 摘要 vs Slice 4 实施段自洽 | PASS | 未变 — 仅 L1 计数修正 ✓ |
| 6 | 架构树函数名正确 | PASS | line 370 "`_append_research_workbook_section`" 不变 ✓ |
| 7 | header/status/tail 自洽 | PASS | line 1 "v4.5 CANDIDATE / AWAITING FINAL DUAL RE-REVIEW" ✓ |
| 8 | 其他 14 slices 零语义变化 | PASS | 零 cross-contamination ✓ |
| 9 | 35 tests + pyright 0 errors 基线 | PASS | 35 passed, 0 errors ✓ |
| 10 | Slice 4 段零 Any/cast/type:ignore/glue | PASS | 仅 line 767 "禁止新增" ✓ |

**全部 10 项核心验证复检通过。** ✓

---

## 7. Plan × Erratum Artifact 自洽性

| 检查项 | Plan | Erratum Artifact | 一致 |
|--------|------|-----------------|------|
| 版本状态 | v4.5 CANDIDATE / AWAITING FINAL DUAL RE-REVIEW (line 1) | AWAITING FINAL DUAL RE-REVIEW (line 8) | ✓ |
| 控制项 | W16-CTRL-01 | W16-CTRL-01 (line 7) | ✓ |
| 三 TypedDict 字段 | lines 710-737 | lines 50-66 | ✓ |
| helper exact signature | lines 742-749 | lines 70-78 | ✓ |
| 存量签名计数 | "15 个...19 处" (lines 12, 329, 764) | "15 个...19 处" (lines 98, 174) | ✓ |
| scope 边界 | public builder 存量不变 (line 763-764) | DEFERRED / OUT OF SCOPE (line 97) | ✓ |
| 替代方案裁决 | — | lines 88-100: 7 方案 REJECTED + 1 DEFERRED | ✓ |
| 初次 re-review findings | — | lines 102-138: L1 FIXED, L2-L4 + MiMo LOW 全部 REJECTED/NON-DEFECT | ✓ |
| 其余 slices 零变化 | line 12, 329 | line 153 | ✓ |
| 验证基线 | line 778: 35 passed / 0 errors | lines 160-165: 35 passed / 0 errors | ✓ |

**Plan 与 erratum artifact 完全自洽。** ✓

---

## 8. Findings 状态总表

| ID | 来源 | 摘要 | 严重度 | 状态 | 裁决依据 |
|----|------|------|--------|------|---------|
| L1 | 004533 | "17 个存量签名" 计数表述精度 | Low | **CLOSED** — FIXED | plan + erratum 六处统一修正为 "15 个签名/19 处" |
| L2 | 004533 | `current_items = []` 时序未显式标注 | Low | **CLOSED** — REJECTED/NON-DEFECT | line 92 `current_items = []` 不在替换范围内 |
| L3 | 004533 | `_ResearchWorkbookEvidence` vs `deepcopy` 类型分界 | Low | **CLOSED** — REJECTED/NON-DEFECT | update 路径全量使用 `dict[str, object]` 边界 |
| L4 | 004533 | plan 未覆盖 `__future__` annotations 兼容性 | Low | **CLOSED** — REJECTED/NON-DEFECT | pyright 门禁直接覆盖，已验证兼容 |
| LOW-01 | 004534 | `items` 局部变量名遮蔽 | Low | **CLOSED** — REJECTED/NON-DEFECT | 不同函数作用域，无冲突 |
| LOW-02 | 004534 | `__future__` annotations runtime introspection | Low | **CLOSED** — REJECTED/NON-DEFECT | 当前无 TypedDict 反射调用 |
| LOW-03 | 004534 | `total=True` 未来 schema 演进 | Low | **CLOSED** — REJECTED/NON-DEFECT | 当前所有字段必填，`total=True` 精确匹配 |

**全部 7 项旧 findings CLOSED。零 open findings。**

---

## 9. 验证命令（不变）

```bash
pytest tests/cli/test_research_template_command.py -k "research_workbook" -q
# 预期: 35 passed

pyright dayu/cli/commands/research_workbook.py tests/cli/test_research_template_command.py
# 预期: 0 errors, 0 warnings, 0 informations
```

---

## 10. 最终裁决

| 维度 | 结果 |
|------|------|
| L1 计数修正（六处自洽） | **PASS** |
| L2-L4 Controller NON-DEFECT 源码验证 | **PASS** |
| MiMo LOW-01/02/03 Controller NON-DEFECT 验证 | **PASS** |
| 三 TypedDict 14/14 字段 vs JSON shape | **PASS** |
| helper signature 零 object/Any | **PASS** |
| 两 flush ID/时序语义保持 | **PASS** |
| plan × erratum artifact 自洽 | **PASS** |
| 其他 14 slices 零变化 | **PASS** |
| 35 tests + pyright 0 errors | **PASS** |
| AGENTS.md 硬约束合规 | **PASS** |
| 旧 findings 全部 CLOSED | **PASS** |
| **总评** | **PASS** |

**v4.5 W16-CTRL-01 erratum 最终 ACCEPT。Slice 4 可以按修正后的 plan 实施。**
