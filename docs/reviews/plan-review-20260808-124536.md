# Plan Re-Review: Slice11 v5.5 — S11-CTRL-02 Bounded Replacement 复核

**审查目标**: `docs/plans/2026-08-07-cli-write-architecture-refactor.md` v5.5 §11f S11-CTRL-02
**审查基线**: `8c63d80` (clean HEAD)
**审查来源**: DeepSeek 122456 (FAIL, 1H+1M+1L)、MiMo 122457 (FAIL, 1H+2M+2L)、Controller erratum `docs/reviews/plan-slice-11-v5.5-phase-efh-erratum-20260808-codex.md`
**审查时间**: 2026-08-08 12:45:36
**审查人**: planreview (adversarial, focused re-review)
**结论**: **PASS** — 全部 8 项 findings CLOSED；plan 达到 code-generation-ready

---

## 1. 审查范围

本 re-review 逐项复核 v5.5 S11-CTRL-02 对 122456/122457 全部 findings 的闭环，
并对照 clean HEAD `8c63d80` 验证 plan contract 的精确性。每项按
"plan contract → HEAD 真源 → 判定" 三联对照。

**不审查**: Phase A/B table/adapter 内部实现、Protocol 字段计数（已被 v5.3 双路 PASS 覆盖）、
Slice 12/13 内容。

---

## 2. 全部 Findings 逐项复核

### 2.1 DeepSeek 122456 Finding 3 (HIGH) — Phase E Phantom Helper

**原发现**: `_build_challenger_run_plan_if_required` 不存在于 HEAD，plan v5.4 §11f 调用
该幻觉符号且标注 "Phase E 不变"。

**v5.5 处置 — CLOSED-BY-PLAN-TEXT**:

| 验证项 | Plan v5.5 Contract | HEAD `8c63d80` | 匹配? |
|--------|-------------------|-----------------|-------|
| Phantom 存在性 | grep count = **0** (line 2292-2294 显式禁止) | grep count = 0 | ✅ |
| 四字段 any() | `"routing_challenger_run_plan_output"`, `"_approval_request"`, `"_approval_output"`, `"_approval_input"` (lines 2202-2205) | `write.py:341-344` 逐字相同 | ✅ |
| builder 调用 | `_build_challenger_run_plan_from_args(args=args, paths_config=paths_config, write_model_override_name=(...))` (lines 2210-2213) | `write.py:349-352` keyword-only 签名一致 | ✅ |
| boundary assert | `_assert_challenger_run_output_boundaries(plan=..., workspace_dir=...)` (lines 2215-2217) | `write.py:354-356` | ✅ |
| 5 类异常 | `FileExistsError, FileNotFoundError, OSError, TypeError, ValueError` (lines 2219-2225) | `write.py:358-364` 逐类相同 | ✅ |
| 错误日志 | `f"Challenger 双跑计划无效: {exc}"` (line 2227) | `write.py:366` 逐字相同 | ✅ |
| return 2 | line 2230 | `write.py:369` | ✅ |
| `dict[str, Any] \| None` | line 2198; line 2194-2195 明确标注 "存量局部语义 1→1 保留" | `write.py:337` | ✅ |
| getattr 保留 | Phase E 四个字段全部保留 getattr (lines 2200-2201) | `write.py:339` | ✅ |
| bounded scope 声明 | "禁止新建、导入或调用任何用于封装 Phase E 的新 helper" (line 2137) | N/A (plan contract) | ✅ |
| phantom-zero 门禁 | "全仓生产源码不得定义或调用任何额外 Phase E wrapper/helper" (lines 2292-2294) | N/A (gate) | ✅ |

**判定**: **CLOSED**。Phase E fenced code 与 HEAD `write.py:337-369` 逐行一致。
幻觉符号已从 plan 完全消除（grep count 0）。Bounded replacement 边界明确禁止
Phase E helper extraction (方案 B REJECTED)。

---

### 2.2 DeepSeek 122456 Finding 4 (MEDIUM) — Phase F Guard 语义偏差

**原发现**: v5.4 plan 用 `challenger_run_plan is not None and bool(...)` 短路 guard
替代 HEAD 的 `assert challenger_run_plan is not None`，将 fail-loud 改为 fail-silent。

**v5.5 处置 — CLOSED-BY-PLAN-TEXT**:

| 验证项 | Plan v5.5 Contract | HEAD `8c63d80` | 匹配? |
|--------|-------------------|-----------------|-------|
| assert 保留 | `assert challenger_run_plan is not None` (line 2241) | `write.py:380` | ✅ |
| selector 直接属性 | `args.routing_challenger_run_approval_input` (line 2240) | `write.py:372-378` (getattr) | ⚠️ 授权差异 |
| 变量名 | `run_approval_exit_code` (line 2242) | `write.py:381` | ✅ |
| 验证调用 | `_verify_and_consume_challenger_run_approval_before_host(...)` (lines 2242-2246) | `write.py:381-384` | ✅ |
| 退出码 | `return run_approval_exit_code` (line 2248) | `write.py:387` | ✅ |
| 禁止静默 guard | "禁止把 assert 改为 challenger_run_plan is not None and ..." (lines 2251-2252) | N/A (contract) | ✅ |
| 不变量测试 | 新增 AssertionError 回归测试 (lines 2252-2254) | N/A (gate) | ✅ |

**selector 差异分析**: `routing_challenger_run_approval_input` 是 Write Protocol exact20
字段之一（§11a line 1817），因此 Slice11 将 selector 读取从 `getattr` 改为直接属性
是 bounded replacement 的授权范围（S11-CTRL-02 line 2132: "对 Write Protocol exact20
字段的既有 selector 读取改为直接属性访问"）。Plan exact20 边界表 line 2182 明确列出
Phase F 允许 `args.routing_challenger_run_approval_input` 直接访问。
语义等价：HEAD 的 `getattr(args, "routing_challenger_run_approval_input", "")` 在
Protocol structural subtyping 下与直接属性访问等价（Dayu 已声明该字段）。

**判定**: **CLOSED**。`assert` fail-loud 语义完整保留。Selector 改为直接属性是
授权的 exact20 边界内变更。

---

### 2.3 DeepSeek 122456 Finding 5 (LOW) — Phase C/D "不变" 措辞不精确

**原发现**: v5.4 标注 Phase C/D "不变" 但 selector 已改用直接属性访问。

**v5.5 处置 — CLOSED-BY-PLAN-TEXT**:

Plan line 2186: "Phase C/D 的注释与契约均为'语义不变，exact20 selector 随 Protocol 改为直接属性'"

| 验证项 | Plan v5.5 | 判定 |
|--------|-----------|------|
| Phase C 注释 | "语义不变，exact20 selector 随 Protocol 改为直接属性" | ✅ 区分语义保持与访问方式变化 |
| Phase D 注释 | 同上 | ✅ |
| exact20 表 | lines 2177-2184: Phase C `args.summary`, Phase D `args.preflight_only` 均在 Protocol 中 | ✅ |

**判定**: **CLOSED**。措辞精确区分了语义不变与访问方式授权变更。

---

### 2.4 MiMo 122457 H-01 (HIGH) — Phase E Phantom / 丢失 Inline Error Boundary

**原发现**: 与 DeepSeek 3 同源。强调 phantom helper 会丢失 inline 的
required-any、builder、boundary assert、5 类异常与 return 2。

**v5.5 处置 — CLOSED-BY-PLAN-TEXT**:

v5.5 Phase E fenced code (lines 2197-2231) 完整保留了：
1. `challenger_run_plan_required = any(...)` — 四字段 required-any ✅ (line 2199)
2. `_build_challenger_run_plan_from_args(...)` — 真实 builder ✅ (line 2210)
3. `_assert_challenger_run_output_boundaries(...)` — output-boundary assert ✅ (line 2215)
4. 5 类异常 + 精确日志 + `return 2` ✅ (lines 2219-2230)

**判定**: **CLOSED**。所有 error boundary 元素均在 v5.5 contract 中精确锁定。

---

### 2.5 MiMo 122457 M-01 (MEDIUM) — Phase F Fail-Loud Invariant 漂移

**原发现**: 与 DeepSeek 4 同源。MiMo 建议保留 HEAD `getattr`。

**v5.5 处置 — CLOSED-BY-PLAN-TEXT**:

Controller erratum (line 89-91): "Controller 采纳直接属性但保留 assert；不采纳 MiMo 建议的
blanket HEAD getattr，因为该 selector 已是 accepted Write Protocol 字段。"

Plan line 2236-2237: "selector 已进入 Write Protocol，故只允许把 selector 读取改为直接属性；
HEAD 的 assert、验证调用与退出码必须保留"

**判定**: **CLOSED**。Controller 裁决采纳直接属性 + assert 保留，拒绝 blanket getattr。
裁决理由充分：该 selector 已进入 Write Protocol exact20。

---

### 2.6 MiMo 122457 M-02 (MEDIUM) — Phase H 三组异常处理遗漏

**原发现**: v5.4 plan 省略了 Phase H 的三组 try/except：
plan rebuild 四异常、challenger-config ValueError、materialize Exception。

**v5.5 处置 — CLOSED-BY-PLAN-TEXT**:

| 异常组 | Plan Contract | HEAD 证据 | 匹配? |
|--------|--------------|-----------|-------|
| **G1: plan rebuild** | `(FileNotFoundError, OSError, TypeError, ValueError)` + `"Challenger 双跑计划复核失败: {exc}"` + `return 2` (lines 2261-2264) | `write.py:700-710` — 四异常 + 日志 + return 2 | ✅ |
| **G2a: preflight challenger-config** | `ValueError` + `str(exc)` + `return 2` (lines 2265-2267) | `write.py:581-588` — ValueError + Log.error(str(exc)) + return 2 | ✅ |
| **G2b: main challenger-config** | `ValueError` + `str(exc)` + `return 2` (lines 2265-2267) | `write.py:717-724` — ValueError + Log.error(str(exc)) + return 2 | ✅ |
| **G3: materialize** | `Exception` (保留 BLE001) + `"研究工件 materialize 失败: {type(exc).__name__}: {exc}"` + `return 2` (lines 2268-2271) | `write.py:751-758` — Exception + noqa BLE001 + 日志 + return 2 | ✅ |

Plan 正确识别了两个 challenger-config 调用点（preflight 路径 line 581 与 main 路径
line 717），contract 表述为 "preflight challenger 与 main challenger 的两个
_build_challenger_write_config 调用点" (line 2265-2266) ✅。

**判定**: **CLOSED**。三组异常边界全部精确锁定，与 HEAD 逐组一致。

---

### 2.7 MiMo 122457 L-01 (LOW) — `write_exit` vs `write_exit_code`

**原发现**: v5.4 旧示意中使用 `write_exit`，HEAD 使用 `write_exit_code`。

**v5.5 处置 — CLOSED-BY-PLAN-TEXT**:

Plan line 2273-2274: "Phase H 变量名精确保持 write_exit_code；后续判断、默认 runner
结果与 materialize 成功路径都使用该名称，不得恢复旧示意中的 write_exit。"

Bounded replacement fenced code 不再包含任何旧示意，因此 `write_exit` 已从 plan 消失。

**判定**: **CLOSED**。

---

### 2.8 MiMo 122457 L-02 (LOW) — Phase C/D/G 访问方式边界不清

**原发现**: v5.4 plan 未明确区分哪些字段允许直接属性访问、哪些必须保留 getattr。

**v5.5 处置 — CLOSED-BY-PLAN-TEXT**:

Plan lines 2175-2191 新增 **exact20 selector 直接属性边界表**：

| 阶段 | 允许的直接属性 | 在 Protocol? |
|------|--------------|-------------|
| Phase A/B predicates | 11 bool + 3 path + 2 bool | ✅ 全部 exact20 |
| Phase C | `args.summary` | ✅ |
| Phase D | `args.preflight_only` | ✅ |
| Phase F | `args.routing_challenger_run_approval_input` | ✅ |
| Phase G | `args.summary`, `args.reprice_costs` | ✅ |
| Phase H | `args.preflight_only` | ✅ |

Line 2187-2191 明确 table-out 字段（`output`、routing/proposal/config-change 路径、
`infer`、`materialize_research` 等）继续使用 HEAD `getattr`，不得扩充 Dayu 或
blanket 改写。

**判定**: **CLOSED**。边界表精确，每阶段允许的直接属性均有 Protocol 字段依据。

---

## 3. S11-CTRL-02 整体契约验证

### 3.1 Phase A/B Bounded Only ✅

Plan lines 2128-2137 明确两项且仅两项改动：
1. Phase A 14 if + Phase B 2 if → phase tables + adapters
2. exact20 selector → 直接属性

"除上述两项外...必须保持 HEAD" (line 2135) 涵盖控制流、异常边界、日志、退出码、
I/O、变量时序、table-out getattr。

Replacement fenced code (lines 2144-2166) 仅含 Phase A loop → resolve/build →
Phase B loop。**不包含** Phase C–H 的任何代码。

### 3.2 Phase E 精确保留 ✅

详见 §2.1 逐行验证。关键不变量全部锁定：
- 四字段 `any()` 动态 inventory (table-out getattr)
- `_build_challenger_run_plan_from_args` keyword-only 调用
- `_assert_challenger_run_output_boundaries` 验证
- 5 类异常 → 精确日志 → `return 2`
- `dict[str, Any] | None` 局部类型 1→1 保留

### 3.3 Phase F Fail-Loud Assert ✅

详见 §2.2。`assert challenger_run_plan is not None` 保留；
selector 改为直接属性（授权变更）。新增 AssertionError 回归测试。

### 3.4 Phase H 三组异常 + Partial-Success ✅

详见 §2.6。三组边界逐组锁定。变量名 `write_exit_code` 强制执行。

### 3.5 exact20 vs Table-Out 边界 ✅

详见 §2.8。边界表 + Dayu 不扩充声明 + blanket 改写禁止。

### 3.6 门禁完整性 ✅

| 门禁 | Plan 位置 | 状态 |
|------|----------|------|
| 16-action matrix G1/G2/G3 | line 2280-2282 | ✅ |
| Phase E 真实回归 (required/build/boundary/5 exc) | lines 2283-2284 | ✅ |
| Phase F AssertionError 回归 | line 2285 | ✅ |
| Phase H 三组异常回归 | lines 2286-2288 | ✅ |
| AST: phase table 长度 exact14/2 | line 2289 | ✅ |
| AST: adapter exact14/2, lambda runner 0 | line 2289 | ✅ |
| AST: run_write_command 顺序 | lines 2290-2291 | ✅ |
| Phantom-zero | lines 2292-2294 | ✅ |
| Phase E inline AST = git show 8c63d80 subtree | lines 2293-2294 | ✅ |
| AST: Phase F direct selector + assert | line 2295 | ✅ |
| AST: Phase H 三组异常 + write_exit_code | lines 2295-2296 | ✅ |
| AST: Phase C/D exact20; table-out getattr inventory | lines 2296-2297 | ✅ |
| _build_execution_options 路径计数 | lines 2298-2299 | ✅ |
| Arguments AST: exact1/20/21 + union equality | lines 2300-2302 | ✅ |
| pytest + pyright + Ruff F/I + coverage >=80% | lines 2303-2309 | ✅ |
| README 同步规则 | lines 2310-2312 | ✅ |
| git diff --check + import smoke + 零 Any/object/cast/glue | lines 2313-2314 | ✅ |
| Stop condition | lines 2316-2319 | ✅ |

### 3.7 Controller Ruling 一致性 ✅

方案 A (bounded Phase A/B replacement + HEAD Phase E/F/H preservation) — ACCEPTED
方案 B (新建 Phase E helper) — REJECTED-WITH-REASON

Plan contract 全线贯彻方案 A：bounded replacement (lines 2128-2137)、
Phase E 精确保留 (lines 2193-2234)、禁止新 helper (line 2137, 2292-2294)。

---

## 4. Findings 映射汇总

| 来源 | ID | 严重程度 | 简述 | v5.5 状态 | 证据 |
|------|----|---------|------|----------|------|
| DeepSeek 122456 | 3 | HIGH | Phase E phantom helper | **CLOSED** | §2.1 |
| DeepSeek 122456 | 4 | MEDIUM | Phase F guard → assert 漂移 | **CLOSED** | §2.2 |
| DeepSeek 122456 | 5 | LOW | Phase C/D 措辞不精确 | **CLOSED** | §2.3 |
| MiMo 122457 | H-01 | HIGH | Phase E phantom / inline boundary 丢失 | **CLOSED** | §2.4 |
| MiMo 122457 | M-01 | MEDIUM | Phase F fail-loud invariant 漂移 | **CLOSED** | §2.5 |
| MiMo 122457 | M-02 | MEDIUM | Phase H 三组异常遗漏 | **CLOSED** | §2.6 |
| MiMo 122457 | L-01 | LOW | write_exit vs write_exit_code | **CLOSED** | §2.7 |
| MiMo 122457 | L-02 | LOW | Phase C/D/G 访问边界不清 | **CLOSED** | §2.8 |

**合计**: 8/8 CLOSED，0 OPEN，0 DEFERRED。

---

## 5. Open Questions

| # | 问题 | 严重程度 |
|---|------|---------|
| Q1 | Phase E 中 `routing_challenger_run_approval_input` 同时出现在 Phase E getattr loop（table-out 风格，不变）和 Phase F 直接属性访问（Protocol 风格，授权变更）中。同一字段的两种访问方式共存于不同 Phase，实施 Agent 可能困惑。 | LOW — 两处均为精确 contract：Phase E 是 exact HEAD，Phase F 是授权变更。建议在 erratum 或实施期加一行注释说明 |
| Q2 | v5.5 状态仍为 CANDIDATE / AWAITING DUAL RE-REVIEW，需 MiMo 侧同步 PASS 后方可进入 accepted。 | 流程 — 非 plan 缺陷 |

---

## 6. Residual Risks

| # | 风险 | 跟踪建议 |
|---|------|---------|
| R1 | 实施 Agent 在执行 bounded replacement 时可能不小心修改 Phase E/F/H 代码 | Stop condition (lines 2316-2319) 已覆盖：若 Phase E/F/H AST 与 HEAD 不一致，Agent 必须停止。建议实施时用 `diff` 精确限定变更行范围 |
| R2 | Phase G 大量 table-out getattr（routing/proposal/config-change 路径）的精确 inventory 未逐字段列出 | Plan 以语义描述而非逐字段枚举方式定义 boundary (line 2187-2191)。实施时需对照 HEAD 逐字段确认不误改 |

---

## 7. 最终结论

**PASS** — v5.5 S11-CTRL-02 bounded replacement contract 满足 code-generation-ready 标准。

**证据链**:
1. Phantom helper `_build_challenger_run_plan_if_required` → plan 中 **0 出现**，HEAD 源码中 **0 定义**
2. Phase E fenced code → 与 HEAD `write.py:337-369` **逐行一致**
3. Phase F fenced code → `assert` **保留**，selector 改为直接属性（授权边界内）
4. Phase H contract → **三组异常全部锁定**，变量名 `write_exit_code` 强制执行
5. exact20 边界表 → 每阶段允许的直接属性 **均有 Protocol 字段依据**
6. 16 matrix / AST / order / call-count / phantom-zero / coverage 门禁 → **全部列出**
7. 8/8 prior findings → **全部 CLOSED**，0 新增 findings / OPEN / deferred

**Open H/M/L: 0/0/0**

**通过条件**: MiMo 侧同步 PASS（当前 AWAITING DUAL RE-REVIEW），Controller 将 status
改为 ACCEPTED 后 Slice 11 可恢复实施。
