# v5.4 Slice 10 Dayu Propagation Boundary — 复审（MiMo 113001）

- **评审时间**: 2026-08-08 11:30:01
- **评审模型**: MiMo
- **审查计划**: `docs/plans/2026-08-07-cli-write-architecture-refactor.md`（v5.4 CANDIDATE）
- **审查 Controller artifact**: `docs/reviews/plan-slice-10-v5.4-propagation-boundary-erratum-20260808-codex.md`
- **初审输入**: DeepSeek 111500（FAIL）、MiMo 111501（方案 B 推荐）
- **Accepted HEAD**: e329992
- **WIP 状态**: 冻结（production 6 文件未修改；4 个 pyright reportArgumentType 仍存在于当前 WIP）
- **审查范围**: S10-CTRL-06 propagation boundary erratum 复审，对照初审 findings 与真实调用图

---

## Verdict: PASS

**HML: open High=0, Medium=0, Low=0**

v5.4 S10-CTRL-06 正确解决了初审全部 3 个 open findings，传播边界精确，call-graph 闭合。Slice 10 可恢复实施。

---

## 1. 逐项验证

### 1.1 三个 snapshot-chain helper 终态精确 `argparse.Namespace` — PASS

**v5.4 S10-CTRL-06 声明**:

| 函数 | v5.4 终态 args |
|---|---|
| `_build_fresh_application_routing_snapshot` | `argparse.Namespace` |
| `_build_snapshot_for_args` | `argparse.Namespace` |
| `build_snapshot_builder` | `argparse.Namespace` |

**当前 WIP 实态**: 三个函数均声明为 `DayuCliArguments`（`_write_config_application.py:40`、`_write_snapshot_builder.py:18`、`_write_snapshot_builder.py:49`）。这是 v5.3 传播的残留，需在实施阶段按 S10-CTRL-06 回退为 `Namespace`。

**终态消费者验证**: `setup_write_config(args: argparse.Namespace, ...)` 在 `dependency_setup.py:716`，全函数体使用 `getattr`，零访问 `DayuCliArguments` 专有字段。snapshot helper 恢复 `Namespace` 与终态消费者完全对齐。

**结论**: v5.4 plan 文本精确声明了终态。WIP 需要实施变更（3 个签名回退），plan 准确描述了目标。

### 1.2 仅 config application runner 为 DayuCliArguments — PASS

**v5.4 S10-CTRL-06 声明**: `_run_write_model_configuration_application` 为 `DayuCliArguments`。

**当前 WIP 实态**: `_write_config_application.py:109` 声明 `args: DayuCliArguments`。该函数通过 `functools.partial` 将 `args` 传给 `_build_fresh_application_routing_snapshot`（恢复后接受 `Namespace`），`DayuCliArguments IS-A Namespace`，类型安全。

**结论**: config application runner 的 Dayu 签名正确保持，与 snapshot helper 的 `Namespace` 形成窄入口 → 宽基础设施的正确分层。

### 1.3 exact42 args + 1 parse return 计数 — PASS

**v5.4 传播计数**:

```
research-template entry 1 + runners 39 = 40
write entry = 1
config application runner = 1
────────────────────────────────────────
Total: 42 args + 1 parse return
```

**v5.3 历史公式** `1+39+1+2+2=45` 中的 `+2+2`（config_application 2 + snapshot_builder 2）已被 S10-CTRL-06 覆盖：config_application 的 2 个函数中 1 个保持 Dayu（runner）、1 个恢复 Namespace（snapshot helper）；snapshot_builder 的 2 个函数全部恢复 Namespace。

**MiMo 111501 措辞纠正**: 初审 §4.3 表格中 `config_application:2 - snapshot_builder:0 = 42` 的分解方式虽算术正确，但 `config_application:2` 暗示两个 Dayu 目标。v5.4 erratum §3 明确纠正为 **1 Dayu runner + 1 Namespace helper**，终态 42 结论不变。**此纠正已由 Controller 在 erratum 中完成，无需额外 action。**

### 1.4 rollback runner 与 manual exact15 保持 Namespace — PASS

**rollback runner**: `_write_config_rollback.py:27` 声明 `args: argparse.Namespace`。1 个函数，不改变。

**manual recovery**: `_write_manual_recovery.py` 定义 15 个函数（14 个 `_run_*` + 1 个 `_check_*`）。其中 14 个声明 `args: argparse.Namespace`，`_check_write_model_configuration_manual_recovery_gate` 无 `args` 参数。全部 15 个保持 `Namespace`。

**验证**: `grep -c "args: argparse.Namespace" _write_manual_recovery.py` = 14；`grep -c "def _run_\|def _check_" _write_manual_recovery.py` = 15。**exact15 确认。**

### 1.5 先前 4 个 pyright reportArgumentType 根因可关闭 — PASS

**当前 WIP 实态**: `pyright` 在 rollback:71、manual:392、481、581 报 4 个 `reportArgumentType`。根因是 snapshot helper 被 v5.3 收窄为 `DayuCliArguments`，而 4 个 caller 保持 `Namespace`。

**S10-CTRL-06 修复**: 三个 snapshot helper 恢复 `Namespace` 后，4 个 caller 的 `args: Namespace` 赋给 `build_snapshot_builder(args: Namespace)` 类型匹配，错误消除。

**验证链**:
- `_write_config_rollback.py:71` → `build_snapshot_builder(args=args)` → `args: Namespace` → `build_snapshot_builder` 接受 `Namespace` → ✓
- `_write_manual_recovery.py:392,481,581` → 同上 → ✓

**预期**: 实施后 `pyright dayu/cli/` → 0 errors。

### 1.6 v5.3 structural conformance、mapping/entry 行为、fixtures、coverage、no-cast/no-glue 未退化 — PASS

| 子项 | 状态 | 说明 |
|------|------|------|
| S10-CTRL-05 structural conformance | **不变** | `ResearchTemplateDispatchArguments(Protocol)` 1 field，`DayuCliArguments(argparse.Namespace)` 1 AnnAssign，Dayu 不继承 Protocol |
| S11-CTRL-01 structural conformance | **不变** | Slice 11 追加 20 write 字段的计划不受 S10-CTRL-06 影响 |
| S10-CTRL-03 mapping 行为 | **不变** | 39-entry `_RESEARCH_TEMPLATE_ACTION_DISPATCH` mapping 不受 snapshot helper 类型变更影响 |
| S10-CTRL-04 entry 行为 | **不变** | `run_research_template_command` 的 `setup_loglevel` + selector + `try/except` 行为不变 |
| 39 mapping matrix | **不变** | parameterized test matrix 不涉及 snapshot helper |
| Fixtures | **需调整** | 42 个 Dayu 目标 direct tests 使用 `DayuCliArguments()`；3 个 snapshot helper direct tests 使用 `argparse.Namespace()`（v5.4 erratum §5 明确禁止 blanket fixture 迁移） |
| Coverage | **按 modified file** | 预期 modified: `arguments.py`、`arg_parsing.py`、`research_template.py`、`write.py`、`_write_config_application.py`；`_write_snapshot_builder.py` 若恢复后 diff=0 则不机械要求 coverage |
| No-cast/no-glue | **不变** | S10-CTRL-06 不引入 cast、type-ignore、adapter、wrapper 或 glue |
| `git diff --check` | **不变** | plan-only erratum，production WIP 冻结 |

---

## 2. 初审 Findings/Observations 最终 CLOSED/OPEN 映射

### DeepSeek 111500 Findings

| Finding ID | 严重程度 | 初审状态 | v5.4 处理 | 最终状态 |
|---|---|---|---|---|
| S10-CALLGRAPH-01 | HIGH | OPEN | S10-CTRL-06 采纳方案 B：三个 snapshot helper 恢复 `Namespace`，四个 `reportArgumentType` 根因消除 | **CLOSED** |
| S10-LAYER-01 | MEDIUM | OPEN | S10-CTRL-06 明确 dispatch concrete type 不沿 snapshot infrastructure 反向扩散，layer boundary 在 plan text 中固化 | **CLOSED** |
| S10-COUNT-01 | LOW | OPEN | S10-CTRL-06 修正传播计数为 exact42 + 1，覆盖 v5.3 历史 45 公式 | **CLOSED** |

### MiMo 111501 Observations

| Observation | 初审状态 | v5.4 处理 | 最终状态 |
|---|---|---|---|
| 方案 B 推荐（42 结论） | 建议 | S10-CTRL-06 采纳方案 B，42 计数确认 | **CLOSED** |
| config_application 分组措辞 | 未显式标注为 finding | v5.4 erratum §3 纠正为「1 Dayu runner + 1 Namespace helper」 | **CLOSED** |

**Open 计数**: High=0, Medium=0, Low=0

---

## 3. 初审 config_application 分组措辞纠正

MiMo 111501 §4.3 传播计数表:

```
v5.3 计划: 45 = arg_parsing:1 + research_template:40 + write:1 + config_application:2 + snapshot_builder:2
方案 B 终态: 42 = arg_parsing:1 + research_template:40 + write:1 + config_application:2 - snapshot_builder:0
```

`config_application:2` 的分解：
- `_run_write_model_configuration_application` → DayuCliArguments ✓
- `_build_fresh_application_routing_snapshot` → argparse.Namespace

表中 `config_application:2 - snapshot_builder:0` 在算术上得到 42，但 `config_application:2` 暗示两个 Dayu 目标。实际上 config_application 的 2 个函数中只有 1 个是 Dayu 目标，另一个（snapshot helper）是 Namespace。正确分解应为：

```
42 = (research 40) + (write 1) + (config_app runner 1)
     其中 snapshot_builder 的 2 个 + config_app 的 1 个 snapshot helper = 3 个 Namespace 不计入
```

v5.4 erratum §3 已明确纠正此措辞。初审 42 结论本身正确，分组表述不精确但不影响最终结果。**此纠正已由 Controller 完成，复审确认 CLOSED。**

---

## 4. WIP 与 v5.4 Plan 的 Gap 分析

| 项目 | 当前 WIP | v5.4 目标 | Gap |
|------|---------|-----------|-----|
| `_build_fresh_application_routing_snapshot` args | `DayuCliArguments` | `argparse.Namespace` | 需回退 1 签名 |
| `_build_snapshot_for_args` args | `DayuCliArguments` | `argparse.Namespace` | 需回退 1 签名 |
| `build_snapshot_builder` args | `DayuCliArguments` | `argparse.Namespace` | 需回退 1 签名 |
| `_run_write_model_configuration_application` args | `DayuCliArguments` | `DayuCliArguments` | 无 gap |
| rollback runner args | `argparse.Namespace` | `argparse.Namespace` | 无 gap |
| manual recovery 15 functions args | `argparse.Namespace` | `argparse.Namespace` | 无 gap |
| pyright | 4 errors | 0 errors | 实施后消除 |

**实施工作量**: 3 个签名从 `DayuCliArguments` 回退为 `argparse.Namespace`，涉及 2 个文件（`_write_config_application.py`、`_write_snapshot_builder.py`）。需同步调整 import 语句。

---

## 5. 结论

v5.4 S10-CTRL-06 propagation boundary erratum 正确解决了初审全部 open findings：

1. **S10-CALLGRAPH-01 (HIGH)**: CLOSED — snapshot helper 恢复 `Namespace`，call-graph 闭合
2. **S10-LAYER-01 (MEDIUM)**: CLOSED — layer boundary 在 plan text 中固化
3. **S10-COUNT-01 (LOW)**: CLOSED — 计数修正为 exact42 + 1
4. **MiMo 措辞纠正**: CLOSED — Controller 在 erratum §3 中完成

**复审结论: PASS**。open High/Medium/Low = 0/0/0。v5.4 可恢复 Slice 10 implementation。
