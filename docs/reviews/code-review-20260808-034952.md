# Slice 5 Code Review — Follow-up 双路 Re-Review

- **日期**: 2026-08-08
- **基线**: `9041d7d gateflow: accept cli write architecture plan v4.7`
- **分支**: `codex/dual-model-research-mvp`
- **审查范围**: 全部未提交变更 vs accepted HEAD
- **前审**: `docs/reviews/code-review-20260808-033848.md`（DS 初审，PASS，M1/L1/L2）
- **Controller 裁决**: `docs/reviews/slice-5-code-review-adjudication-20260808-codex.md`（六项 observation 全部 rejected-with-reason）
- **实现记录**: `docs/reviews/slice-5-write-config-params-implementation-20260808-codex.md`
- **审查类型**: Follow-up re-review — 复核 Controller 裁决是否有 accepted v4.7 plan 与源码证据支持
- **最终结论**: **PASS** — High=0, Medium=0, Low=0; all findings CLOSED

---

## 1. 逐项复核：DS M1/L1/L2

### 1.1 M1 — `_challenger_requested` 双 owner → CLOSED

**初审**: Medium — 双 owner 是非直觉的 monkeypatch 语义裂痕，建议加 docstring 说明。

**Controller 裁决**: rejected-with-reason / NON-DEFECT。

**Re-review 证据核实**:

Accepted v4.7 plan 第 367–379 行 **明确、逐条定义了双 owner 契约**：

> **`_challenger_requested` 双 owner 语义（首轮 re-review clarification）**:
>
> - `dayu.cli.commands.write._challenger_requested` 是 `run_write_command` 内 3 个 global call site 的正确 monkeypatch owner；patch 该绑定必须控制真实 `run_write_command` 路径。
> - `dayu.cli.commands._write_params_validation._challenger_requested` 是真实 `_validate_research_materialization_args` 函数内部调用的正确 monkeypatch owner；patch 该绑定必须控制 validator 内部路径。
> - 禁止把所有旧 `write._challenger_requested` patch blanket 迁到 params。monkeypatch 必须按被测 call site 的函数 globals 选择 owner。
> - `tests/engine/test_cli_running_config.py` 当前约 line 5934 同时 patch `write._validate_research_materialization_args` 与 `write._challenger_requested`，服务于 `run_write_command` 路径，保持不变。

计划第 896–906 行进一步要求两条真实回归测试分别验证 write owner 与 params owner 的独立受控。实施代码中：

- `test_write_challenger_owner_controls_real_run_command` — patch `write_command_module._challenger_requested`，触发真实 `run_write_command`，验证退出码 47
- `test_params_challenger_owner_controls_real_validator` — patch `write_params_validation._challenger_requested`，调用真实 `_validate_research_materialization_args`，验证内部路径受控
- engine test line 5934 保持 patch `write_command_module._challenger_requested`（不迁到 params），与 plan 一致

双 owner **不是** bug 或偶然裂痕，**是** accepted plan 的显式契约。Controller 裁决完全由 accepted plan 证据支持。

Controller 对"不建议在生产 docstring 中写 monkeypatch 说明"的裁决合理——将测试替换机制写入生产契约会降低模块边界清晰度。

**裁定**: CLOSED (rejected-with-reason, NON-DEFECT)。无需 code/test/doc 修改。

---

### 1.2 L1 — `_resolve_write_company_name` 静默吞异常 → CLOSED

**初审**: Low — `except Exception: return ""` 吞掉所有 Exception 子类，pre-existing。

**Controller 裁决**: rejected-with-reason / PRE-EXISTING / OUT-OF-SCOPE。

**Re-review 证据核实**:

旧 `write.py` 中该函数体（已删除行）：

```python
try:
    return str(company_name_resolver(ticker) or "").strip()
except Exception:
    return ""
```

新 `_write_config_helpers.py:49-52` 中：

```python
try:
    return str(company_name_resolver(ticker) or "").strip()
except Exception:
    return ""
```

**逐字节 identical**。Accepted v4.7 plan 第 908 行明确要求：

> 8 个迁移函数保持 exact 签名、控制流、返回值、**异常与日志语义**，并补齐完整中文 Args/Returns/Raises docstring。

改变异常处理（如缩小 `except` 范围、改为 propagate）会突破"exact 异常语义"约束，产生未授权的行为变更。该函数的旧 Raises docstring 写"无"曾是不准确的，新 docstring 已如实描述"本函数会吞掉公司名称解析依赖抛出的异常并返回空字符串"。

**裁定**: CLOSED (rejected-with-reason, PRE-EXISTING / OUT-OF-SCOPE)。无需修改。

---

### 1.3 L2 — Docstring 变更与 plan "exact" 措辞 → CLOSED

**初审**: Low — plan 写 "exact 迁移" 但实际补齐了 docstring，措辞不精确。

**Controller 裁决**: rejected-with-reason / NON-DEFECT。

**Re-review 证据核实**:

Accepted v4.7 plan 第 908–909 行：

> 8 个迁移函数保持 exact 签名、控制流、返回值、异常与日志语义，**并补齐完整中文 Args/Returns/Raises docstring**。

"exact" 的修饰范围是 **签名、控制流、返回值、异常与日志语义**，不包括 docstring。"补齐 docstring" 是并列的第二要求。两者不矛盾。

实施已验证：
- Body/signature/control-flow: exact（见 §2.3 比对表）
- Docstring: 全部补齐为完整中文 Args/Returns/Raises 格式（AGENTS.md 硬约束要求）

**裁定**: CLOSED (rejected-with-reason, NON-DEFECT)。无需修改。

---

## 2. 跨审发现交叉复核

Controller 裁决文档中还引用了 MiMo 初审的三项 finding（M1: getattr, L1: missing coverage, L2: validator 规模），均被 rejected-with-reason。作为 DS re-reviewer，交叉核实：

| MiMo Finding | Controller 裁决 | 证据与判断 |
|---|---|---|
| M1: 六处 `getattr` 读取 `WriteCliConfig` | rejected-with-reason / PRE-EXISTING | `_build_write_run_config` 旧函数体中的六处 `getattr` 是 exact 迁移的一部分；替换为直接属性访问会突破"只迁移、不顺手重构"的 Slice 边界。裁决成立 |
| L1: config helper 四条未覆盖 statement | rejected-with-reason / NON-DEFECT | Plan 与项目的精确门禁是 statement coverage ≥80%，`_write_config_helpers.py` 达 87.5%，已通过。Plan 没有逐行解释所有 missing statement 的额外契约。裁决成立 |
| L2: research materialization validator 规模 | rejected-with-reason / PRE-EXISTING / OUT-OF-SCOPE | 大型 validator 是 HEAD 既有函数，Slice 5 只授权 exact 迁移，禁止顺手拆分。裁决成立 |

**无跨审矛盾**。六项 observation 裁决一致，全部 rejected-with-reason。

---

## 3. 最终结构核查（不变）

前审已通过的全部结构核查在本 re-review 中重新确认，无变化：

| 检查项 | 状态 |
|---|---|
| MODULE 单一真源（`_write_config_helpers.py:12`） | PASS |
| setup_model_name 13 patches（旧 owner 0, 新 owner 13） | PASS |
| 8 个旧定义全部删除 | PASS |
| 6 个 functional binding（config 4 + params 2）+ MODULE | PASS |
| 禁止 compat re-export（`_validate_challenger_run_plan_args`, `_validate_live_smoke_plan_args` 在 write.py 中为 0） | PASS |
| Import 无循环（两个新模块零反向 import write.py） | PASS |
| 类型安全（零 `Any`/`object`/`cast`/`type: ignore` 在新模块中） | PASS |
| Pyright 0/0/0 | PASS |
| 测试 639 passed（三文件合集） | PASS |
| 覆盖率 ≥80% × 3 files | PASS |
| Ruff F/I delta 0；full-rule code multiset delta 0 (UP035 2→1) | PASS |

---

## 4. 区分：accepted contract vs pre-existing vs Slice 引入缺陷

| 类别 | 项目 | 处置 |
|---|---|---|
| **Accepted v4.7 contract** | 双 owner 语义（`_challenger_requested` 两个独立 monkeypatch owner） | 正确实施，非缺陷 |
| **Accepted v4.7 contract** | 6 个 functional binding + MODULE，禁止两个 compat re-export | 正确实施，非缺陷 |
| **Accepted v4.7 contract** | Docstring 补齐（与 exact 行为迁移并列要求） | 正确实施，非缺陷 |
| **Pre-existing exact body** | `except Exception: return ""` 吞异常 | 非本 Slice 引入，不改 |
| **Pre-existing exact body** | `_build_write_run_config` 六处 `getattr` | 非本 Slice 引入，不改 |
| **Pre-existing exact body** | `_validate_research_materialization_args` ~1800 行 | 非本 Slice 引入，不改 |
| **Pre-existing gap** | `_validate_challenger_run_plan_args` 无独立 direct test | Plan 明确说明无旧 test，不要求新增 |
| **Slice 5 引入缺陷** | **无** | — |

---

## 5. 最终评估

| 维度 | 结论 |
|---|---|
| 结构迁移正确性 | PASS |
| 控制流等价性（8/8 exact body） | PASS |
| S5-CTRL-01..04 全部要求 | PASS |
| 6 个 functional binding identity | PASS |
| 双 owner 契约正确实施 | PASS |
| Import 无循环 / 零反向依赖 | PASS |
| 类型安全 (0 type escape) | PASS |
| Compat re-export 禁止 | PASS |
| Pyright (0/0/0) | PASS |
| 测试全部通过 | PASS |
| 覆盖率 ≥80% × 3 files | PASS |
| Ruff delta ≤ 0 | PASS |
| Controller 六项裁决均有 accepted plan 证据支持 | PASS |
| 无跨审矛盾 | PASS |
| Slice 5 引入的新缺陷 | **0** |

**Overall Verdict: PASS**

| | 初审 | Controller 裁决 | Re-review 终态 |
|---|---|---|---|
| DS M1 (双 owner) | Medium / OPEN | rejected-with-reason | **CLOSED** |
| DS L1 (异常吞) | Low / OPEN | rejected-with-reason | **CLOSED** |
| DS L2 (docstring) | Low / OPEN | rejected-with-reason | **CLOSED** |
| MiMo M1 (getattr) | — | rejected-with-reason | **CLOSED** |
| MiMo L1 (coverage) | — | rejected-with-reason | **CLOSED** |
| MiMo L2 (validator 规模) | — | rejected-with-reason | **CLOSED** |

- **High**: 0 (初审 0 → re-review 0)
- **Medium**: 0 (初审 1 → Controller 驳回 → re-review 0)
- **Low**: 0 (初审 2 → Controller 驳回 → re-review 0)
- **Open**: 0
- **CLOSED**: 6/6

**不需要任何 code、test、doc、README 或 master plan fix。**

Slice 5 可以 proceed to accepted commit。
