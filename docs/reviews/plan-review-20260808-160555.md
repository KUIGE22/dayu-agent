# Plan Re-Review — v5.7 Ruff Ratchet Erratum

- **日期**: 2026-08-08 16:05
- **HEAD**: `4c5bb1b`（v5.6 accepted）
- **受审对象**: `docs/reviews/plan-v5.7-ruff-ratchet-baseline-erratum-20260808-codex.md`（Controller erratum）及其 plan working diff
- **初审来源**: `plan-review-20260808-154611.md`（DeepSeek，baseline/gate blocker review）、`plan-review-20260808-154612.md`（MiMo，CI/configured-rule-set 独立审查）
- **审查类型**: adversarial re-review — 独立复测所有测量、验证 baseline 真实性、检验 residual 完整性、核对 header/status/tail 与 stop conditions 一致性
- **约束**: 只写本 artifact，不改 plan/code/erratum

---

## 0. 独立复测

以下所有测量使用与 erratum 完全相同的 leaf command：

```bash
uv run --no-project --with ruff==0.16.1 ruff check --no-cache \
  --output-format=json dayu/cli/commands/ dayu/services/
```

并通过 `Counter(item["code"])` 构造 multiset。

### 0.1 Architecture baseline `5821014` 验证

`git show -s --format='%H %P %s' f0a3ea1`:
```
f0a3ea1 5821014 gateflow: accept cli write architecture plan
```

`5821014` 是 first accepted plan commit `f0a3ea1` 的唯一 parent。`f0a3ea1` 的 `git diff 5821014..f0a3ea1 --stat` 仅含 6 个 docs 文件（plan + 4 reviews，合计 2709 insertions），**零 production 代码变更**。因此 `5821014` 的代码树与 `f0a3ea1` 的代码树完全等价。

CLI architecture work unit 的第一个代码变更 commit 是 `81df373`（"test: characterize cli write dispatch"），其祖先为 `f0a3ea1` → `5821014`。`5821014` 的 commit message 为 `fix: keep host cleanup off event loop`，是 work unit 开始前的最后一个 production commit。

**验证**: `5821014` 作为 architecture refactor 的代码基线是精确的。

### 0.2 Architecture ratchet 测量复现

| 指标 | Erratum 声称 | 独立复测 | 匹配 |
|---|---|---|---|
| HEAD total | `335` | `335` | ✅ |
| BASE total | `364` | `364` | ✅ |
| positive | `{}` (empty) | `{}` (empty) | ✅ |
| negative codes | `I001:11, TRY004:17, UP035:1` | `I001:11, TRY004:17, UP035:1` | ✅ |
| negative total | `29` | `29` | ✅ |

**结论**: Architecture Ruff ratchet **通过** — HEAD 相对 work-unit baseline `5821014` 无任何新增 Ruff finding（零 positive delta）。实际有 29 个 finding 被消除（I001 未排序 import 减少 11、TRY004 减少 17、UP035 减少 1）。

### 0.3 PR-level residual 测量复现

`origin/main=2115c86` 同算法、同 scope：

| 指标 | Erratum 声称 | 独立复测 | 匹配 |
|---|---|---|---|
| HEAD total | `335` | `335` | ✅ |
| origin/main total | `143` | `143` | ✅ |
| per-code positive codes | 10 个 | 10 个 | ✅ |
| per-code positive total | `216` | `216` | ✅ |

Per-code 逐项验证：

| Code | Erratum 声称 | 独立复测 (HEAD − origin/main) | 匹配 |
|---|---|---|---|
| B009 | 53 | 53−0 = 53 | ✅ |
| BLE001 | 3 | 13−10 = 3 | ✅ |
| FURB162 | 12 | 12−0 = 12 | ✅ |
| ISC004 | 1 | 1−0 = 1 | ✅ |
| RUF010 | 22 | 22−0 = 22 | ✅ |
| RUF022 | 2 | 5−3 = 2 | ✅ |
| SIM102 | 1 | 3−2 = 1 | ✅ |
| TRY004 | 118 | 135−17 = 118 | ✅ |
| UP012 | 2 | 2−0 = 2 | ✅ |
| UP035 | 2 | 20−18 = 2 | ✅ |
| **合计** | **216** | **216** | ✅ |

额外确认：origin/main 上存在但 HEAD 已消除的 codes（F811:1, F841:2）以及 count 减少的 codes（F401:15→7, F541:14→1, I001:27→27）不在 positive 集合内，与 erratum 一致。

### 0.4 `--select ALL` 与 constraints Ruff 版本

| 测量 | 结果 | 匹配 |
|---|---|---|
| HEAD `--select ALL` total | `11248` | ✅ |
| `constraints/min-py311.txt` Ruff 版本 | `ruff==0.15.11` | ✅ |

`--select ALL` 的 `11248` 与 no-select Slice-report rule set 的 `335` 数量级完全不同。`0.15.11`（CI）与 `0.16.1`（architecture gate）的 E/F 语义也不同。Erratum 将三者明确分层，不混淆。

---

## 1. 方案裁决正确性

### 1.1 current-HEAD baseline → REJECTED

深审 154611 建议以 current HEAD 作为 ratchet baseline。Erratum 拒绝此方案。

**复审核实**: 以 `4c5bb1b` 自比较，`Counter(HEAD) - Counter(HEAD)` ≡ `{}`，永真。该比较不提供任何 non-regression 证据，是 tautology。拒绝正确。

### 1.2 origin/main as architecture hard ratchet → REJECTED

MiMo 154612 建议以 `origin/main` 作为 architecture ratchet baseline。Erratum 拒绝，改为仅用作 PR-level residual baseline。

**复审核实**: `origin/main` 与 `5821014` 之间有大量非本 work unit 的 feature commits（如 dual-model research MVP）。以 `origin/main` 作为 architecture ratchet 会将无关工作的 finding 混入 architecture 比较，无法隔离 "CLI architecture refactoring 是否引入退化"。拒绝正确。

### 1.3 changed-lines 算法 → REJECTED

**复审核实**: 当前仓库无可用的 `ruff --git-diff` 或等价 changed-lines 工具。Ruff 无原生 diff-aware 模式。无受支持、可复现的实现证据。拒绝正确。

### 1.4 新增 Slice 14 → REJECTED

**复审核实**:
- TRY004 修复（`raise ValueError` → `raise TypeError`）改变异常类型，可能破坏既有 `except ValueError` 处理器 → 行为变更
- B009 修复（`getattr(args, "field")` → `args.field`）丢失 argparse 防御性语义 → 设计意图违背
- FURB162 修复改变 datetime timezone-aware/naive 行为 → 语义变更

Erratum 的拒绝理由基于 accepted contracts 的保护。拒绝正确。

### 1.5 MiMo RUFF-05 建议在当前范围直接 cleanup → REJECTED

MiMo 154612 建议 "Ruff violations = lint debt，非架构缺陷；可在当前范围内 cleanup"。Erratum 部分接受（不需要 Slice 14）但拒绝在当前范围做机械 cleanup。

**复审核实**: 机械修复的 TRY004/B009/FURB162 会改变行为语义，与 "在当前范围内 cleanup" 的简单性假设矛盾。Erratum 的拒绝理由（contract preservation）成立。拒绝正确。

---

## 2. Residual 完整性

### 2.1 PR residual 逐 code 记录是否完整

Erratum §2.3 逐 code 记录了 origin/main 的 10 个 positive codes 及精确 count。与独立复测完全一致。无遗漏 code、无淡化表述。

### 2.2 PR residual 是否被隐藏

Erratum 在以下位置显式记录了 origin/main positive 216：
- §2.3（完整 per-code 表格）
- §4（plan working diff）: master plan §9 stop conditions 明确写 "遗漏、淡化或把它误称为 CI 已覆盖均阻止 deepreview closeout"
- §4（plan working diff）: master plan §10 新增 residual 行
- §4（plan working diff）: master plan §11 新增 checklist item 23

在 plan working diff 中，stop condition 文本明确区分：
> "LOC measurement 与已记录的 origin/main PR-level Ruff residual 本身不阻断 architecture validation；但 residual 未被完整带入 aggregate deepreview 时，必须停止，不得关闭 deepreview gate"

**判定**: 未被隐藏。PR residual 216 作为 mandatory deepreview input 完整记录，与 architecture hard gate 清晰分层。

### 2.3 CI 不覆盖 production Ruff 是否被记录

Erratum §3.1 M02 裁决 `ACCEPTED AS RESIDUAL`，plan diff §10 新增 residual 行 "origin/main PR-level production Ruff residual 未被 CI required lane 覆盖"。明确记录且不扩大到 CI workflow 修改。

---

## 3. Header/Status/Tail 与 Stop Conditions 一致性

### 3.1 Header

Plan diff header: `v5.7 CANDIDATE / AWAITING DUAL RE-REVIEW` ✅

### 3.2 Status line

Erratum status: `v5.7 CANDIDATE / AWAITING DUAL RE-REVIEW` ✅

Plan diff changelog entry: `v5.6→v5.7 变更` 完整记录 AGG-RUFF-CTRL-01..04 ✅

Plan diff 新增 review source line: `v5.7 Ruff ratchet baseline review 来源: ...` ✅

### 3.3 Tail

Plan diff tail: `本 plan v5.7 CANDIDATE / AWAITING DUAL RE-REVIEW（clean HEAD 4c5bb1b ... 等待双路 plan re-review）。v5.6 ACCEPTED / DUAL PLAN RE-REVIEW PASS 与更早 accepted 版本保持为历史。` ✅

v5.6 accepted 历史保留 ✅

### 3.4 Stop Conditions

Architecture hard gate（plan diff §9）:
- `architecture Ruff ratchet 相对 5821014 的 positive delta=0` → 阻断条件明确 ✅
- 实测 `positive={}` → 当前通过 ✅

PR residual gate（plan diff §9）:
- "LOC measurement 与已记录的 origin/main PR-level Ruff residual 本身不阻断 architecture validation"
- "但 residual 未被完整带入 aggregate deepreview 时，必须停止，不得关闭 deepreview gate"
- 双层 stop condition 清晰：architecture gate 看 positive=0，deepreview closeout 看 residual 完整性 ✅

完成定义（plan diff §11 item 23）:
- 记录了 architecture gate 的精确参数（baseline `5821014`、Ruff `0.16.1`、scope、HEAD=335、BASE=364、positive={}）
- 记录了 PR residual 的 mandatory 输入要求
- 列出了被拒绝的替代方案 ✅

---

## 4. Erratum 与初审 Findings 的闭环

### 4.1 DeepSeek 154611 findings

| Finding | 严重度 | Erratum 处置 | 复审确认 |
|---|---|---|---|
| H01: plan §11 Ruff delta=0 gate 不可实现 | HIGH | ACCEPTED / CLOSED-BY-PLAN-TEXT | ✅ — baseline 修正为 5821014，architecture positive=0 实际通过 |
| M01: §9 rule-set 歧义 | MEDIUM | ACCEPTED / CLOSED-BY-PLAN-TEXT | ✅ — 固定 Ruff 0.16.1 + no-select + JSON Counter |
| M02: CI 不检查 production Ruff | MEDIUM | ACCEPTED AS RESIDUAL | ✅ — 记录为 §10 residual，不修改 CI |
| L01: origin/main 全量 450 findings 上下文 | LOW | ACCEPTED AS CONTEXT / CLOSED | ✅ — scope 保持，architecture/PR baseline 分层 |
| L02: internal/write_pipeline 预存暴露 | LOW | ACCEPTED AS CONTEXT / CLOSED | ✅ — 已在 baseline multiset 中自然覆盖 |

154611 的 5/5 findings 全部 CLOSED。无新 finding。

### 4.2 MiMo 154612 findings

| Finding | 严重度 | Erratum 处置 | 复审确认 |
|---|---|---|---|
| RUFF-01: configured rule set vs --select ALL 混淆 | M | ACCEPTED / CLOSED-BY-PLAN-TEXT | ✅ — 固定为 Slice-report no-select rule set |
| RUFF-02: CI 不执行全仓 Ruff | M | ACCEPTED AS PR RESIDUAL / CLOSED | ✅ — 不扩大 CI scope |
| RUFF-03: 192 positive 精确语义未验证 | L | CLOSED（erratum 自测替代） | ✅ — 以 216（origin/main）和 architecture positive=0 关闭 |
| RUFF-04: violations 归属阶段不确定 | L | ACCEPTED / CLOSED | ✅ — 5821014 隔离 architecture gate，origin/main 保留 PR residual |
| RUFF-05: 无需 Slice 14 | — | ACCEPTED（no Slice 14）/ REJECTED（不直接 cleanup） | ✅ — contract preservation 理由充分 |
| RUFF-06: baseline 选 origin/main | — | REJECTED-WITH-REASON | ✅ — 仅作 PR residual，不作 architecture hard ratchet |

154612 的 6/6 items 全部 CLOSED。其中 RUFF-05 的 "直接 cleanup" 子建议被 REJECTED（contract 保护），RUFF-06 的 "origin/main as architecture ratchet" 被 REJECTED（跨 work-unit 不对称）。拒绝理由成立。

---

## 5. 未覆盖项

### 5.1 architecture negative（改善）未列为正面证据

Architecture ratchet 有 29 negative（I001:11, TRY004:17, UP035:1）——即 HEAD 比 baseline 少 29 个 finding。Erratum 将此信息记录在 §2.2 但没有在 plan diff 中作为 "改善证据" 显式标注。这不影响 gate 判定（positive={} 已充分证明无退化），但若在 §11 item 23 中加一行 "negative 29表明本work unit实际改善了3类finding" 会让完成定义更完整。

**严重度**: LOW — 不影响 gate 逻辑，属于 completeness polish。

### 5.2 origin/main 的 F811/F841 消除与 F401/F541 减少未提及

origin/main 比较中 HEAD 消除了 F811 (1→0)、F841 (2→0)，并显著减少了 F401 (15→7)、F541 (14→1)。这些是本 work unit 的正面贡献，但 erratum 未提及。

**严重度**: LOW — residual 聚焦 positive delta，不要求列出改善。但正面证据可能对未来 PR review 有用。

---

## 6. Final Verdict

**PASS**。Erratum 与 plan diff 的 architecture Ruff ratchet gate 设计正确、可复现、已在独立复测中通过。

### Open High

**无**。Architecture baseline `5821014` 精确、algorithm 自包含可复现、HEAD=335 BASE=364 positive={} architecture ratchet PASS。初审 154611 的 HIGH 已通过 baseline 修正闭环。

### Open Medium

**无**。Rule-set 歧义已由固定 Ruff `0.16.1` + no-select 解决。CI/PR residual 的双层 gate 设计清晰。初审 154611/154612 的全部 Medium findings 已 CLOSED。

### Open Low

| ID | 描述 | 处置 |
|---|---|---|
| **L01** | Architecture negative=29（改善）未在 §11 完成定义中显式记录 | ACCEPTED — 建议在 item 23 追加 "negative=I001:11/TRY004:17/UP035:1 表明本 work unit 实际改善了 3 类 finding"。不影响 gate 逻辑。 |
| **L02** | origin/main 比较中 F811/F841 消除与 F401/F541 减少未记录 | ACCEPTED AS CONTEXT — 正面证据，非 residual 必需。可在 deepreview 中作为 "本 work unit 的 lint 质量改善" 提及。 |

---

## 7. 验证结果

- **Baseline 5821014 真实性**: PASS — 是 f0a3ea1 唯一 parent，零 production diff，work unit 前最后一个 commit
- **Ruff 0.16.1 算法可复现性**: PASS — 独立复测，全部 335/364/positive={}/negative=29 精确匹配
- **origin/main positive=216 完整性**: PASS — 10 codes 逐项验证，无遗漏、无淡化、无隐藏
- **--select ALL=11248 区分**: PASS — 与 no-select rule set 明确分层
- **constraints 0.15.11 E/F 区分**: PASS — CI 环境与 architecture gate 环境明确区分
- **Header/status/tail 一致性**: PASS — v5.7 CANDIDATE 全文一致，v5.6 历史保留
- **Stop conditions**: PASS — architecture positive=0 阻断明确，PR residual mandatory deepreview 阻断明确
- **初审闭环**: PASS — 154611:5/5 CLOSED, 154612:6/6 CLOSED; 拒绝理由成立

---

*本 review 只写 `docs/reviews/plan-review-20260808-160555.md`。不改 plan/code/erratum。verdict PASS / open H/M/L = 0/0/2。*
