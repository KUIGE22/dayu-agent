# Corrective Re-Review — v5.6 Aggregate Gate Plan-Fix

- **日期**: 2026-08-08
- **基线**: `2a4bafb`
- **分支**: `codex/dual-model-research-mvp`
- **审查范围**: Controller `plan-v5.6-aggregate-gate-erratum-20260808-codex.md` §2.3 裁决 + master plan working-tree diff + DeepSeek `152220` + MiMo `152221`
- **审查类型**: corrective re-review — 修正 `152221` 的 M-01/L-01 计量证据错误

---

## Verdict: **PASS**

**Open findings**: High 0 / Medium 0 / Low 0

---

## 1. M-01 CLOSED: 验证命令已自包含于 plan §9

### 152221 原始 finding

> Controller artifact §9 验证命令未自包含，需跨文件组装。

### 修正

Controller artifact §2.3 明确指出：

> 本轮受审 source 是 master plan，§9 已内联完整 AST 脚本以及 pytest、pyright、diff 和 machine gates，并逐项列出 owner inventory、DAG、zero-compat 与 Ruff delta 要求；Controller artifact 已明确指向该唯一计划真源，无需重复整块命令。

Working-tree diff 确认 plan §9 新增了完整的 `Aggregate pre-deepreview 结构门禁（v5.6）` 块，包含：

1. **完整 AST 验证脚本**（~70 行 Python）：`write.py` FunctionDef exact18 + adapter exact16 + AnnAssign exact2 + phase table entries 14/2；`research_template.py` FunctionDef exact41 + selector present + runners exact39 + ClassDef0 + dispatch 39-key + `__all__` 55-entry
2. **pytest 命令**：`test_write_cli_dispatch.py` + `test_research_template_command.py` + `test_write_service.py`
3. **pyright ratchet**：`utils.ci_pr_pyright --base ... --head ...`
4. **whitespace gate**：`git diff --check`
5. **machine gates**：`validate_handoff_docs` + `codex_review_gate` + `dual_model_pipeline_check`
6. **文本补充要求**：owner FunctionDef `22/26/12/11/12`、functional binding `6/18/6/9/6`、DAG、zero-compat、Ruff delta=0

Controller artifact 作为 erratum 记录，正确指向 plan 作为唯一验证真源，无需在 artifact 中重复命令块。

**裁决**: M-01 **REJECTED-WITH-REASON / CLOSED / NON-DEFECT**。

---

## 2. L-01 CLOSED: Plan 文件已在 working-tree 更新

### 152221 原始 finding

> Plan 文件尚未更新（预期状态，等待 re-review PASS 后合入）。依据：`git diff HEAD~3..HEAD` 为空。

### 修正

152221 使用 `git diff HEAD~3..HEAD` 检查已提交 commits 之间的差异，该命令只比较 commit `dda9cb4`(Slice 12) 与 `2a4bafb`(Slice 13)——两者均未修改 plan 文件，因此输出为空。

正确命令为 `git diff HEAD -- docs/plans/2026-08-07-cli-write-architecture-refactor.md`，检查 working-tree 相对 HEAD 的差异。该命令输出 **非零**，确认 plan 文件已在 working-tree 中被修改：

| 变更位置 | 内容 |
|---|---|
| Header | `v5.5 ACCEPTED` → `v5.6 CANDIDATE / AWAITING DUAL RE-REVIEW` |
| HEAD | `8c63d80` → `2a4bafb` |
| Provenance | 新增 v5.6 aggregate gate review 来源 |
| Changelog | 新增 `v5.5→v5.6 变更` 段（AGG-CTRL-01..03） |
| §2.1 | `FunctionDef exact40` → `aggregate terminal FunctionDef exact41` |
| §8 | LOC 硬门槛改为 informational measurement 说明 |
| §9 | 新增 ~97 行 aggregate pre-deepreview 结构门禁完整命令块 |
| §11 item 2 | LOC gate 替换为精确结构闭合 |
| §11 item 15 | 拆分 Slice 9 历史 exact40 与 Slice 10 后 terminal exact41 |
| Status | 更新为 `v5.6 CANDIDATE / AWAITING DUAL RE-REVIEW` |
| Tail | 同步更新 |

**裁决**: L-01 **REJECTED-WITH-REASON / CLOSED / MEASUREMENT-ERROR**。Plan 文件已在 working-tree 中完整更新，152221 的计量命令选择错误。

---

## 3. 核心 v5.6 erratum 验证

### 3.1 结构门禁替代 LOC — 充分

Plan §9 的 AST 脚本精确验证：
- `write.py`: FunctionDef 18（adapter 16 + materialize 1 + entry 1）、AnnAssign 2（14+2 entries）
- `research_template.py`: FunctionDef 41（entry 1 + selector 1 + runners 39）、dispatch 39-key、`__all__` 55-entry、ClassDef 0

辅以 pytest、pyright ratchet、whitespace gate、machine gates 与文本要求（owner/DAG/zero-compat/Ruff delta）。LOC 仅作 informational，不触发 stop condition。

**充分性结论**：结构门禁在防函数膨胀、防兼容泄漏、验证 DAG 完整性三个维度严格强于 LOC gate，且不随 docstring 密度漂移。

### 3.2 历史 123 保持 — 正确

Plan diff §11 item 15：

> Slice 9 当时主模块 FunctionDef exact40（entry1 + runners39）且 `83+40=123` 的历史闭合保持不变

时序正确：Slice 9 规划时 `_resolve_research_template_action` 不存在，123 是该时间截面的精确计数。

### 3.3 Terminal 41 — 正确

Plan diff §2.1、§9a、§11 item 2/15 全部统一为 `entry1 + selector1 + runners39 = exact41`。Controller artifact §2.3 确认 DeepSeek 152220 与 MiMo re-review 均验证通过。

### 3.4 无新增 Slice/代码 — 正确

Controller artifact §4 明确拒绝 Slice 14。Working-tree diff 仅修改 plan 文本，零 production/test/README 变更。

### 3.5 Header/Status/Tail 一致性 — 正确

三处统一为 `v5.6 CANDIDATE / AWAITING DUAL RE-REVIEW`，v5.5 accepted 历史完整保留。

---

## 4. DeepSeek 152220 复核

DeepSeek re-review 结论：**PASS / open H/M/L = 0/0/0**。初审 AGG-01..05 全部闭环，未产生新 finding。与本轮 corrective 结论一致。

---

## 5. Open Findings Summary

| ID | 严重度 | 描述 | 处置 |
|---|---|---|---|
| — | — | 无 | — |

**Controller open H/M/L: 0/0/0**。

---

## 6. 152221 计量错误自省

| 原始 finding | 错误类型 | 根因 |
|---|---|---|
| M-01（验证命令未自包含） | NON-DEFECT | 未读取 working-tree plan diff，未发现 §9 已内联完整命令块 |
| L-01（plan 文件未更新） | MEASUREMENT-ERROR | 使用 `git diff HEAD~3..HEAD`（committed diff）而非 `git diff HEAD -- <path>`（working-tree diff） |

两处错误均为审查方法论缺陷：应优先检查 working-tree 状态而非仅检查已提交 commits。

---

## 7. 结论

v5.6 aggregate gate plan-fix **PASS**，open H/M/L = **0/0/0**。

DeepSeek 152220 PASS/open0 + 本轮 MiMo corrective PASS/open0 构成双路 re-review，Controller 可将 v5.6 状态推进至 ACCEPTED。
