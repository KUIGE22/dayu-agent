# Code Review

## Scope

- Mode: current changes
- Branch: `codex/dual-model-research-mvp`
- Base: `d2801c6 gateflow: accept cli write architecture slice 11`
- Output file: `docs/reviews/code-review-20260808-140135.md`
- Included scope:
  - `dayu/services/write_service.py` — unstaged modify
  - `dayu/services/_write_report.py` — untracked new
  - `tests/application/test_write_service.py` — unstaged modify
  - `docs/reviews/slice-12-write-report-implementation-20260808-codex.md` — implementation artifact (read-only truth reference)
- Excluded scope:
  - HEAD has zero commits beyond `d2801c6`；all changes are working-tree unstaged
  - `AGENTS/v5.5 Slice12/C4/§4.3` plan documents not found on disk；implementation artifact used as accepted design truth proxy
- Parallel review coverage: 无

## Verdict

**PASS** — 无 material correctness/state/contract/exit-code 缺陷。一项 Low severity cosmetic finding（门禁函数 warning 中英文混用）。

## Findings

### 1-未修复-低-`_validate_print_report_gate_conditions` warning 语言不一致
- **入口/函数**: `WriteService.print_report` → `_validate_print_report_gate_conditions`
- **文件(行号)**: `dayu/services/_write_report.py:108,111`（中文 `[警告]`）vs `:117-194`（英文 `[warning]`）
- **输入场景**: 任何触发门禁失败的用户输入（如缺少 `routing_history_root`、输入输出互斥等）
- **实际分支**: 行 107-112 走中文 warning 路径；行 113-195 走英文 warning 路径
- **预期行为**: 全部 warning 统一使用中文 `[警告]`（项目指令：一律用中文）
- **实际行为**: 前两个校验（proposal 互斥、缺少 routing_history_root）输出中文 `[警告]`；后续八个校验（promotion/config/approval/preflight）输出英文 `[warning]`
- **直接证据**:
  - 行 108: `print("  [警告] Challenger 提案操作需要提供模型路由历史目录")`
  - 行 111: `print("  [警告] Challenger 提案输入与输出不能同时使用")`
  - 行 117-120: `print("  [warning] Challenger promotion proposal input and output cannot be used together")`
  - 行 130-134: `print("  [warning] configuration change request input and output cannot be used together")`
  - 行 142-146, 152-155, 160-164, 174-177, 185-189, 191-194: 全部以 `[warning]` 开头 + 英文正文
- **影响**: 用户可见输出语言不统一；`[警告]` vs `[warning]` 前缀不一致导致脚本 grep 解析不可靠
- **建议改法和验证点**: 将行 113-195 的所有 `[warning]` 替换为 `[警告]`，英文正文替换为中文；或反向统一为英文。建议统一为中文以符合 CLAUDE.md 语言约定。验证：grep `[warning]` `_write_report.py` 结果为 0
- **修复风险（低）**: 纯文本替换，不影响行为、退出码、控制流
- **严重程度（低）**: 不改变程序正确性，但影响用户一致性体验和可解析性

## 逐项审查 PASS 清单

| # | 检查项 | 结果 | 证据 |
|---|--------|------|------|
| 1 | public exact15 signature/默认值 | PASS | `write_service.py:399-416`；`output_dir` positional + 14 keyword-only，`model_catalog=None`，`overwrite_routing_proposal=False`，其余默认 `None`；与 implementation artifact §Pre-edit 审计 exact15 一致 |
| 2 | step1 inline 仅 exit2 | PASS | `write_service.py:445-447`；`exit_code = print_write_report(...)` 后仅 `if exit_code == 2: return exit_code`；其他非零 exit_code 继续执行，最终在行 518 `return exit_code` 传播 |
| 3 | gate step3 早退 | PASS | `write_service.py:448-463`；`_validate_print_report_gate_conditions` 返回 `int \| None`，非 None 时立即 `return gate_exit_code`，阻断后续 step4–9 |
| 4 | repricing step4 warning continue | PASS | `_write_report.py:199-239`；`_print_repriced_comparison` 返回 `None`，内部 catch `(FileNotFoundError, OSError, TypeError, ValueError)` 打印 warning 后 fallback 或设 `resolved_comparison=None`；调用方 `write_service.py:465` 不捕获返回值，控制流不受影响 |
| 5 | promotion/config request/config approval/health-proposal-preflight 顺序与 0/2/4 传播 | PASS | `write_service.py:467-517`：promotion export → promotion verify → config request export → (conditional config request verify) → config approval → health/proposal/preflight。每个 helper 返回 int，非零立即 `return`；0=继续、2=I/O fail、4=policy block |
| 6 | helper6 vs helper7 issuance/non-issuance 不重复 verify/I/O | PASS | `write_service.py:485-496` 在 `not config_change_approval_issuance` 时才调用 helper6；issuance=true 时 helper7 内聚加载+验证（`_write_report.py:458-466`）；issuance=false 时 helper7 仅处理 `challenger_config_change_approval_input` 验证（`_write_report.py:537-564`）。两路径互斥，不重复加载或验证 |
| 7 | model_catalog object 只在 accepted exact3 | PASS | `write_service.py:403` 签名 `Mapping[str, Mapping[str, object]] \| None`；`_write_report.py:201` `_print_repriced_comparison` 签名；`_write_report.py:570` `_handle_health_trend_and_proposal_and_preflight` 签名。精确 3 处，与 implementation artifact §类型与 owner 边界 一致 |
| 8 | 8 helper 签名/DAG/public exposure | PASS | `_write_report.py` exact8 FunctionDef、0 ClassDef；全部 `_` 前缀模块级私有；DAG 无环，`print_report` 内顺序调用；`write_service.py:20-29` 显式按名 import，`__all__` 行 580-585 不暴露任何 helper |
| 9 | moved monkeypatch 真实 owner | PASS | 测试 diff 中所有 moved-owner patch target 从 `dayu.services.write_service.xxx` 迁移到 `dayu.services._write_report.xxx`；step1 `print_write_report` 和 `run_write_pipeline` 的 patch 继续留在 `dayu.services.write_service`（真实 owner 未变）；implementation artifact 确认 "AST 审计旧 moved-owner patch target=0" |
| 10 | 失败/边界测试是否真实 | PASS | 新增 `test_print_report_gate_failure_matrix_runs_after_step1`（6 类 gate 失败，每类验证 step1 先执行、退出码=2）；新增 `test_print_report_migrated_failure_boundaries_propagate_exit_codes`（promotion policy block→4、I/O fail→2、config request policy block→4、I/O fail→2）。所有断言基于真实 `WriteService.print_report` 调用和返回值，非 mock 出口码桩 |

## Open Questions

- `AGENTS/v5.5 Slice12/C4/§4.3` 文件未在仓库中找到。implementation artifact 声明其为 accepted plan 真源；本次审查以 implementation artifact 作为 design truth proxy。若 artifact 与原始 plan 存在偏差，需由 Gateflow controller 裁决。

## Residual Risk

- `_write_report.py` 仍有 43 条 uncovered statement（implementation artifact 报告 coverage 81.86%），主要是 except tuple 中的等价异常成员及内部不变量防御分支。代表性 policy/I/O/status 路径已覆盖，与 artifact 分类一致：current-slice non-blocking covered-by-equivalent-path。
- `write_service.py` 仍有 23 条 uncovered statement，位于本 Slice 未改的其它 service 路径（preflight/run）；修改后 `print_report` 编排与失败路径均已覆盖。
- 无 deferred、未分类或需要外部 owner 的风险。
