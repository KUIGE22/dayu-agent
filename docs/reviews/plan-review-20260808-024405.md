# Plan Review: v4.7 CANDIDATE Master Plan + Controller — Focused Follow-up Adversarial Re-Review

**审查日期**: 2026-08-08 02:44 UTC
**审查范围**: `codex/dual-model-research-mvp` 分支 `217dd2c` 工作树
**被审查 artifact**:
- Master plan `docs/plans/2026-08-07-cli-write-architecture-refactor.md` (v4.7 CANDIDATE, 1843 lines)
- Controller `docs/reviews/plan-slice-5-v4.7-private-binding-erratum-20260808-codex.md`
**基线审查**: `docs/reviews/plan-review-20260808-023020.md` (本次审查的前置 DeepSeek focused review)
**审查类型**: Adversarial plan re-review — 跨 artifact 一致性、factual claim 验证、stale language 检测、open H/M/L

---

## 1. 审查动机

前置 DeepSeek focused review（plan-review-20260808-023020）确认了 v4.6 Slice5 的两个 compat re-export 违规事实并提出 v4.7 erratum。Controller 据此发布了 v4.7 private-binding erratum 并更新了 master plan。本 follow-up 审查对更新后的两个 artifact 做 adversarial re-review，逐项验证：

1. 所有 stale v4.6 "8-write-binding" 表述已修正
2. 精确定义数：两个模块 4+4=8、write functional bindings 6、MODULE 独立
3. 两个 validator compat re-export 被禁止且 verification commands 可执行
4. engine live-smoke import 迁移与 6 identity assertions 精确
5. setup13/MODULE/dual owner/line5934/Slice8 exact13/gates 不变

---

## 2. 逐项 Factual Claim 验证

### 2.1 Stale v4.6 "8-write-binding" 清理

| 位置 | 文本 | 判定 |
|---|---|---|
| Master plan line 14 (v4.6→v4.7 changelog) | "write.py 仅正常顶层 import/rebind 6 个真正功能 private symbol" | ✅ 正确 |
| Master plan line 347-358 (monkeypatch contract) | "write.py 只正常顶层 import/rebind 下列 6 个真正功能 private symbol..." + 逐项列出 | ✅ 正确 |
| Master plan line 843 (Slice 5 section) | "S5-CTRL-01..04（v4.7 exact scope；v4.6 accepted history retained）" | ✅ 正确，明确标注 v4.7 收窄 |
| Master plan line 863-869 (write.py 变更) | "从 _write_params_validation 正常顶层 import _challenger_requested 与 _validate_research_materialization_args；不 import/re-export 两个 validator" | ✅ 正确 |
| Controller line 47-56 | "write.py 的真实功能 private binding 是 config 4 个加 params 2 个，共 6 个" | ✅ 正确 |
| Controller line 112-127 (exact implementation contract) | 逐条列出 6 保留 + 2 禁止 + engine import 迁移 + identity 6 | ✅ 正确 |
| Ready-for-Review item 18 (line 1793-1798) | "write.py 只保留 6 个 functional private binding... 两个新模块仍各 exact 4、合计 exact 8 个定义" | ✅ 正确 |
| Footer (line 1842) | "write.py 仅正常 import/rebind config 4 个 + _challenger_requested + _validate_research_materialization_args 共 6 个" | ✅ 正确 |

**结论**: 无残留 stale v4.6 "8-write-binding" 语言。全文 "6 functional bindings + MODULE separate + 4+4=8 module defs" 三重区分一致。

### 2.2 精确定义计数验证

**源码验证**（本审查实时执行）:

```text
# write.py 中 8 个函数的 def 全部为零
rg "^def (_resolve_write_model_override_name|...|_validate_research_materialization_args)" write.py
→ (零输出)

# _write_config_helpers.py 精确 4 个 def
rg "^def " _write_config_helpers.py
→ _resolve_write_model_override_name
→ _resolve_write_company_name
→ _build_write_run_config
→ _log_write_preflight_result
(4 functions + MODULE constant)

# _write_params_validation.py 精确 4 个 def
rg "^def " _write_params_validation.py
→ _challenger_requested
→ _validate_challenger_run_plan_args
→ _validate_live_smoke_plan_args
→ _validate_research_materialization_args
(4 functions)
```

| 契约 | Claim | 源码 | 判定 |
|---|---|---|---|
| `_write_config_helpers.py` exact 4 函数 | v4.7 master plan | 4 `^def` | ✅ |
| `_write_params_validation.py` exact 4 函数 | v4.7 master plan | 4 `^def` | ✅ |
| 两模块合计 8 个定义 | Controller + master plan | 4+4=8 | ✅ |
| `write.py` 6 functional bindings | 真实 6 个 import + caller | config4 + params2 | ✅ |
| `MODULE` 不计入 private function | 仅 `_write_config_helpers.py` 定义 | `rg '^MODULE = "APP.WRITE"'` → 1 处 | ✅ |

### 2.3 两个 Validator Compat Re-export 与 Lint 门禁

| 验证项 | 命令/证据 | 结果 |
|---|---|---|
| write.py 零 `_validate_challenger_run_plan_args` 出现 | `rg` in write.py → 仅 import alias（line 22），零 call site | ✅ 确认（实施后应为 0） |
| write.py 零 `_validate_live_smoke_plan_args` 出现 | `rg` in write.py → 仅 import alias（line 23），零 call site | ✅ 确认（实施后应为 0） |
| PLC0414 × 2 | `ruff check --select PLC0414 write.py` → 2 errors | ✅ 确认存在 |
| 删除后 PLC0414/F401 → 0 | plan verification command line 931-932 | ✅ 逻辑正确 |
| Master plan 禁止 compat re-export 语言 | line 360-365, line 878-879 | ✅ 明确 |
| Controller 禁止 compat re-export 语言 | line 63-71 (v4.7-F01) | ✅ 明确 |

### 2.4 Engine Live-Smoke Import 与 6 Identity Assertions

**当前源码事实**:

```python
# tests/engine/test_cli_running_config.py:24-29
from dayu.cli.commands.write import (
    _needs_auto_research_bootstrap,
    _validate_live_smoke_plan_args,       # ← 需迁移
    _validate_research_materialization_args,
    run_write_command,
)
```

| 验证项 | Plan 要求 | 判定 |
|---|---|---|
| engine test `_validate_live_smoke_plan_args` import | 从 `_write_params_validation` 获取 | ✅ verification command line 933-934 精准确认 |
| dispatch identity 锁定数 | 6（非 8） | ✅ line 894-895 明确 |
| `_validate_challenger_run_plan_args` 零旧 direct test | 无 test 需迁移 | ✅ 已确认（仅 `_write_params_validation.py` 内部使用） |
| `_challenger_requested` 双 owner 回归 | 两条独立测试（write path + validator path） | ✅ line 896-903 精确描述 |

**注意**: 当前 dispatch 测试 (`test_write_cli_dispatch.py:608-615`) 断言了 **8** 个 identity binding（含两个即将删除的 validator）。实施后须缩减为 **6**。Controller line 123-125 和 master plan line 894-895 均明确要求此变更。✅ 一致。

### 2.5 不变门禁验证

| 门禁项 | Claim | 当前源码事实 | 判定 |
|---|---|---|---|
| 旧 `write.setup_model_name` patch → 0 | Controller line 131-132 | `rg "dayu\.cli\.commands\.write\.setup_model_name" tests/` → **0** | ✅ 已达成 |
| 新 `_write_config_helpers.setup_model_name` → exact 13 | Controller line 132 | 实施中待验证；plan 要求 13 | ⚠️ 依赖实施后 `rg` 确认 |
| `MODULE = "APP.WRITE"` 仅在 `_write_config_helpers.py` | Controller line 133 | `rg '^MODULE = "APP.WRITE"' dayu/cli/commands` → 仅 helpers 1 处 | ✅ 已达成 |
| `_challenger_requested` 双 owner | Controller line 134 | write.py 保留 import（line 21）+ params_validation 定义 | ✅ 已达成 |
| engine line 5934 patch 不变 | Controller line 135 | write owner `_challenger_requested` + `_validate_research_materialization_args` 均保留 | ✅ 不变 |
| Slice 8 剩余 Challenger 组 exact 13 | Controller line 136 | Master plan line 980-994 列出 exact 13 | ✅ |
| Coverage ≥ 80% per file | Controller line 137 | 待实施后验证 | ⚠️ 依赖实施 |
| pyright 零新增 error | Controller line 138 | 删除 dead import 不引入类型错误 | ✅ 预期通过 |
| Ruff F/I delta vs HEAD = 0 | Controller line 139 | 删除两个 dead import 消除 PLC0414×2，其他不变 | ✅ 预期通过 |

---

## 3. Adversarial Findings

### F-A1: Prior DeepSeek Review 计数措辞错误 — CONFIRMED / Controller 已纠正

**位置**: `plan-review-20260808-023020.md` §5.4 不变契约表

**描述**: 该表将 `_write_params_validation.py` 描述为 "exact 8 个符号"（`_challenger_requested`, `_validate_challenger_run_plan_args`, `_validate_live_smoke_plan_args`, `_validate_research_materialization_args` + 4 个内部 detail）。正确表述应为 "exact 4 个函数"（该模块只有 4 个 `^def`）。

**Controller 处置**: Controller line 97-110 已识别并纠正此错误，明确 "该计数措辞不作为 Controller 事实依据"，准确契约是 4+4=8。

**严重度**: LOW — 不影响 v4.7 erratum 核心逻辑（删除两个 compat re-export 的决策与计数措辞无关），且 Controller 已记录纠正。

**建议**: 不修改外部 review artifact（Controller 已声明不修改）；本 finding 关闭。

### F-A2: Prior Review §5.3 中关于 test_write_challenger_owner_controls_real_run_command 的表述 — 轻微歧义，无功能影响

**位置**: `plan-review-20260808-023020.md` line 217

**描述**: "同时删除 `test_write_challenger_owner_controls_real_run_command` 依赖于 `_challenger_requested` 在 `write.py` 顶层存在的要求" 措辞可能被误读为需要修改该测试。实际上该测试 monkeypatch 的 `write_command_module._challenger_requested` 属于 6 个保留 functional binding 之一，**完全不受影响**。行末 "不受影响" 的结论正确，但前句措辞有歧义。

**严重度**: LOW — 结论正确，实施无误导风险（`_challenger_requested` 明确在保留列表中）。

**建议**: 不修改；本 finding 记录为措辞澄清。

### F-A3: setup_model_name exact 13 计数无法在 plan 阶段验证 — INFO

**描述**: Master plan line 881-884 要求旧 patch 目标为 0、新目标为 13。当前 `rg "dayu\.cli\.commands\.write\.setup_model_name" tests/` 返回 0（旧目标已为 0），但新目标计数需在 tests 完成 monkeypatch 迁移后才能通过 `rg` 验证。plan 中 13 的数字来自 v4.6 Controller 的精确计数，当前无法独立重现验证。

**严重度**: INFO — 来自 v4.6 已审计的数字，v4.7 不改变此数字，风险极低。

### F-A4: Master plan "8 个迁移函数" vs "6 个 functional binding" 语言边界 — VERIFIED CORRECT

**验证**: Master plan line 907-908 说 "8 个迁移函数保持 exact 签名"——这指两个新模块合计 8 个定义，正确。同段 line 874-877 说 "6 个 functional private binding"——指 write.py 的 import，正确。两处数字不同但指代不同对象（模块定义 vs write.py import），不存在矛盾。

**判定**: ✅ 无 inconsistency。

---

## 4. 跨 Artifact 一致性矩阵

| 维度 | Master Plan | Controller | Prior Review (023020) | 源码 | 一致性 |
|---|---|---|---|---|---|
| `_write_params_validation.py` 定义数 | 4 | 4 | ~~8~~ (已纠正) | 4 `^def` | ⚠️ Prior review 有误，Controller 已纠正 |
| `_write_config_helpers.py` 定义数 | 4 | 4 | 4 | 4 `^def` | ✅ |
| `write.py` functional bindings | 6 | 6 | 6 | 6 imports | ✅ |
| MODULE 计入 private function? | 不计入 | 不计入 | 不计入 | N/A (常量) | ✅ |
| 禁止 compat re-export | ✅ | ✅ | ✅ | 待实施删除 | ✅ |
| engine test import 迁移 | `_write_params_validation` | `_write_params_validation` | `_write_params_validation` | 待实施 | ✅ |
| dispatch identity 数量 | 6 | 6 | 6 | 待实施缩减 | ✅ |
| setup_model_name patch | 0→13 | 0→13 | 不变 | 旧:0 | ✅ |
| MODULE 单一真源 | `_write_config_helpers` | `_write_config_helpers` | `_write_config_helpers` | 仅 helpers | ✅ |
| 双 owner `_challenger_requested` | write + params | write + params | write + params | write + params | ✅ |
| engine line 5934 | 不变 | 不变 | 不变 | 不变 | ✅ |
| Slice 8 exact 13 | 13 listed | 13 | N/A (out of scope) | N/A (plan) | ✅ |

---

## 5. Open H/M/L 与 Closure

| Finding | 严重度 | 状态 | 处置 |
|---|---|---|---|
| **F-A1**: Prior review `_write_params_validation.py` "exact 8 符号" 计数措辞错误 | **LOW** | **CLOSED** | Controller 已识别并纠正；不影响 erratum 核心逻辑 |
| **F-A2**: Prior review §5.3 line 217 歧义措辞 | **LOW** | **CLOSED** | 结论正确；实施无误导风险；不修改 |
| **F-A3**: setup_model_name exact 13 无法在 plan 阶段独立验证 | **INFO** | **OPEN→实施门禁** | 实施后 `rg` 验证；当前无证据质疑 v4.6 审计数字 |
| **F-A4**: "8 个迁移函数" vs "6 个 binding" 疑似矛盾 | **INFO** | **CLOSED** | 指代对象不同，无 inconsistency |
| Master plan 残留 stale v4.6 "8-write-binding" 语言 | N/A | **CLOSED** | 全文搜索确认 0 残留 |
| Controller 与 master plan 核心契约不一致 | N/A | **CLOSED** | 跨 12 维度一致性矩阵全部 ✅ |

**Open High**: 0
**Open Medium**: 0
**Open Low**: 0（F-A1/A2 已关闭；F-A3 为实施门禁 INFO）

---

## 6. 最终裁决

| 维度 | 判定 |
|---|---|
| **v4.6→v4.7 stale language 清理** | **PASS** — 全文 0 残留 "8-write-binding" |
| **精确定义数一致性** | **PASS** — 4+4=8 modules / 6 write bindings / MODULE separate 三重区分在全 plan + Controller + 源码间一致 |
| **Compat re-export 禁止** | **PASS** — plan/Controller/verification commands 三重锁定 |
| **Engine import + identity 6** | **PASS** — 迁移路径与精确断言数明确 |
| **不变门禁** | **PASS** — setup13/MODULE/dual owner/line5934/Slice8 exact13 全部确认 |
| **Prior review 计数措辞错误** | **CORRECTED** — Controller 明确纠正，不影响核心逻辑 |
| **Adversarial completeness** | **PASS** — 4 findings (2 LOW closed, 2 INFO) 无 HIGH/MEDIUM |

**v4.7 CANDIDATE plan 已就绪**，可进入 DUAL PLAN RE-REVIEW PASS 并推进 implementation。

---

*审查依据: AGENTS.md · CLAUDE.md · 当前工作树源码 (write.py, _write_config_helpers.py, _write_params_validation.py, test_cli_running_config.py, test_write_cli_dispatch.py) · ruff PLC0414 · rg 全仓 caller/binding 搜索 · plan-review-20260808-023020.md · plan-slice-5-v4.7-private-binding-erratum-20260808-codex.md · 2026-08-07-cli-write-architecture-refactor.md*
