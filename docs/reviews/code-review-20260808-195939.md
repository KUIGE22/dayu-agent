# Aggregate Fix RT-01 Code Review

- **Review 类型**：deepreview（当前改动，review-only）
- **日期**：2026-08-08 19:59:39 CST
- **Output file**：`docs/reviews/code-review-20260808-195939.md`
- **Review 基线**：`a0c469a`（HEAD，v5.8 ACCEPTED）
- **改动范围**：
  - `dayu/cli/commands/research_template.py`
  - `tests/cli/test_research_template_command.py`
- **参考契约**：`docs/plans/2026-08-07-cli-write-architecture-refactor.md` v5.8 `AGG-RT-CTRL-01`
- **实现记录参考**：`docs/reviews/aggregate-fix-rt-exit-implementation-20260808-codex.md`
- **Reviewer**：Codex（Claude Code deepreview gate）

## 审查结论

**Verdict**：PASS。无 material findings。

**Open Findings**：High=0，Medium=0，Low=0。

## 审查维度

### 1. Correctness — 退出码语义

**改动**：

```python
# Before (a0c469a)
return 0

# After (working tree)
return 0 if result.get("ok") is True else 1
```

**分析**：

- `validate_monitoring_source_map_payload` 返回 `dict[str, object]`，其中 `ok` 键由 `not errors` 赋值（`_research_template_core.py:422`），恒为 `bool` 类型。`is True` identity check 对此完全正确。
- `result.get("ok")` 在 `ok` 缺失时返回 `None`，`None is True` 为 `False`，安全回退到退出码 `1`，不会误报成功。`validate_monitoring_source_map_payload` 的返回结构始终包含 `ok`，该 fallback 是防御性的，无功能影响。
- 该修改不改变 JSON stdout 内容、异常传播、dispatch key 或任何其他 `_run_validate_source_map` 调用链上的语义。

**证据**：

- 快速验证确认 `financial` rules + `consumer` source-map 的 template mismatch 正确产生 `{"ok": False, "errors": ["template mismatch: rules='financial' source_map='consumer'"], ...}`。
- Focused 测试 `3 passed, 213 deselected`（`tests/cli/test_research_template_command.py -k "validate_source_map"`）。

**结论**：无缺陷。

### 2. Stability — 异常与边界

**分析**：

- 异常传播路径未变：`_run_validate_source_map` 未新增 `try/except`；所有异常仍由 `run_research_template_command:137` 的 `except (FileNotFoundError, FileExistsError, ValueError)` 捕获并统一返回 `1`。
- `os.EX_OK` / `os.EX_DATAERR` / `os.EX_USAGE` 等 POSIX 退出码体系未被引入 — 返回值 `0`/`1` 保持与 `run_research_template_command` docstring "未知 action 或已知输入、文件异常返回 1" 的简单约定一致。这不是 regression，而是 v5.8 计划明确接受的设计选择。
- 无新增依赖、无新增 I/O、无新增可变状态。

**结论**：无稳定性退化。

### 3. Maintainability — 代码质量

**分析**：

- **Docstring**：`_run_validate_source_map` 的 `Returns` 从"校验结果成功输出时返回 0"更新为"校验成功时返回 0，校验失败时返回 1"，精确反映新的退出码语义。`Args`、`Raises` 无需改变，仍准确描述实际行为。
- **类型**：`-> int` 签名不变，`result.get("ok")` 返回 `object` 但 `is True` 对任意类型安全，无类型错误引入。
- **一致性**：同类 validate runner（`_run_validate_research_workbook`、`_run_validate_bundle`、`_run_validate_monitoring_plan`、`_run_validate_workbook_report`、`_run_validate_scheduler_manifest`）已全部采用条件退出码模式。`_run_validate_source_map` 是唯一有 bug 的 runner；修复后整体一致。
- **无 scope creep**：AST diff 确认只有 `_run_validate_source_map` 的 `Return` 与 docstring 变化。未改变 dispatch mapping、action key、JSON stdout、异常传播，未新增 wrapper、未新增 Slice 14。

**结论**：可维护性改善。

### 4. Test Quality — 回归覆盖

**新增测试**：`test_run_validate_source_map_command_returns_nonzero_for_template_mismatch`

**分析**：

- **真实入口路径**：通过 `run_research_template_command` → dispatch → `_run_validate_source_map` 完整链路，非单元 mock。
- **契约精确性**：
  - `write_monitoring_rules_payload("financial", ...)` + `write_monitoring_source_map_payload("consumer", ...)` 构造 template mismatch，精确匹配计划指定的 `financial`/`consumer` 组合。
  - 断言 `result == 1`（退出码）。
  - 断言 `payload["ok"] is False`（stdout JSON）。
  - 断言 `payload["errors"]` 非空。
- **对称性**：已有 `test_run_validate_source_map_command_outputs_validation_json` 覆盖 `consumer`/`consumer` 成功路径（退出码 0、`ok is True`），新增测试覆盖 failure 路径。两个分支均通过真实 dispatch 覆盖，满足分支覆盖率要求。
- **文档**：新测试包含完整中文 docstring（Args/Raises/Returns），符合项目规范。
- **隔离性**：使用 `tmp_path` 独立文件，无跨测试污染。

**现有测试保持不变**：成功路径回归测试未修改，确保行为未退化。

**结论**：测试覆盖充分，符合计划要求。

### 5. Plan Fidelity — AGG-RT-CTRL-01 逐项对照

| 计划要求 | 实现状态 |
|---|---|
| 只改 `_run_validate_source_map` 的 Return | ✅ `return 0 if result.get("ok") is True else 1` |
| 同步中文 docstring Returns | ✅ |
| JSON stdout 不变 | ✅ `print(json.dumps(result, ensure_ascii=False, indent=2))` 未改 |
| dispatch mapping/action key/owner/DAG 不变 | ✅ |
| 异常集合与传播不变 | ✅ |
| 测试用 `financial` rules + `consumer` source-map | ✅ |
| 测试经真实 `run_research_template_command` | ✅ |
| 断言返回 1、`ok is False`、errors 非空 | ✅ |
| valid 回归用 consumer/consumer、断言返回 0、`ok is True` | ✅ 已有测试保留 |
| 不新增 Slice 14、不重构其他 runner | ✅ AST diff 确认 |
| Production 字节变化后重跑 full suite | ✅ 实现记录已确认 |
| 不修改 README | ✅ 无需修改 |

**结论**：完全符合 v5.8 计划契约。

### 6. Coverage — 门槛检查

实现记录报告：目标生产文件 `304/378 = 80.423280%`，满足 `>=80%` 门槛。新增 failure 路径测试覆盖了此前未覆盖的 `result.get("ok") is True` 的 `False` 分支。

**结论**：覆盖率满足项目要求。

### 7. Adversarial Failure Pass

**攻击面扫描**：

| 攻击向量 | 分析 | 风险 |
|---|---|---|
| `result` 无 `ok` 键 | `.get("ok")` 返回 `None`，`is True` → `False`，返回 1（安全） | 无 |
| `result["ok"]` 为非布尔真值（如 `1`、`"yes"`） | `is True` identity check 拒止，返回 1（安全） | 无 |
| `validate_monitoring_source_map_payload` 抛出非预期异常 | 仍被 `run_research_template_command` 的 `except ValueError` 或顶层 handler 捕获 | 无（未改变） |
| `capsys` 捕获 stdout 后残留影响后续测试 | `capsys` 是 function-scoped fixture，每个测试独立 | 无 |
| `tmp_path` 文件泄漏 | pytest 自动清理 | 无 |
| `print()` 写入已关闭的 fd | 不适用；CLI 入口正常流程 | 无 |

**结论**：无可利用或退化路径。

### 8. CLAUDE.md 项目指令检查

| 指令 | 状态 |
|---|---|
| 函数完整中文 docstring | ✅ `_run_validate_source_map` docstring 已更新；新测试函数有完整中文 docstring |
| 禁止 `object`/`Any`/无类型签名 | ✅ 签名 `-> int` 未变 |
| 禁止 magic 数字/字符串 | ✅ `0`/`1` 是 POSIX 退出码约定，非 magic |
| 测试 >= 80% 覆盖率 | ✅ |
| 每次修改后补齐/更新测试 | ✅ 新增 failure 路径测试 |

**结论**：满足项目编码约束。

## 补充说明

- 本 review 仅审查工作树中 `git diff HEAD` 的两个代码/测试文件与对应的计划契约；不审查 92-commit 全系列、不审查 `origin/main`、不审查已删除/未跟踪文件。
- 实现记录 `docs/reviews/aggregate-fix-rt-exit-implementation-20260808-codex.md` 中报告的全部门禁（full suite、pyright、Ruff、AST delta、machine gates）与本次代码层面的审查结论一致，未发现矛盾。
- 其他 5 个 validate runner（`_run_validate_research_workbook`、`_run_validate_bundle`、`_run_validate_monitoring_plan`、`_run_validate_workbook_report`、`_run_validate_scheduler_manifest`）已正确实现条件退出码，确认 `_run_validate_source_map` 是唯一受影响者且修复完整。无 scope creep risk。

---

*本 artifact 由 Codex deepreview gate 产出。状态：FINAL。不进入 commit/PR gate。*
