# Re-Review: Slice 4 W16 — research_workbook.py helper 提取

- **日期**: 2026-08-08
- **Gate**: dual code re-review
- **Work unit**: CLI Write 架构重构
- **Slice**: Slice 4 / W16
- **基线**: `86538d2 gateflow: accept cli write architecture plan v4.5`
- **分支**: `codex/dual-model-research-mvp`
- **审查人**: MiMo (deepreview)
- **审查类型**: focused re-review（初审 findings 闭环 + 契约复证）
- **Verdict**: **PASS**

---

## 1. 初审输入

| 审查 | 文件 | Verdict | Findings |
|---|---|---|---|
| DeepSeek | `code-review-20260808-011443.md` | PASS | LOW-01, LOW-02 |
| MiMo | `code-review-20260808-011444.md` | PASS | O-1, O-2 (observations) |

Fix artifact: `slice-4-research-workbook-section-fix-20260808-codex.md`
Updated implementation artifact: `slice-4-research-workbook-section-implementation-20260808-codex.md`

---

## 2. 初审 Findings 闭环状态

### DeepSeek LOW-01 — Raises 文案 — **CLOSED**

**原 finding**: `_append_research_workbook_section` 的 `Raises: 无。` 表述过于绝对。

**Controller 裁决**: ACCEPTED / FIXED。改为 `本函数不显式抛出异常。`

**独立验证**:

```
grep -A 2 "Raises:" dayu/cli/commands/research_workbook.py
→ "Raises:"
→ "    本函数不显式抛出异常。"
```

当前 `git diff` 确认 line 87-88 的 Raises 文案为 `本函数不显式抛出异常。`。无行为变化，仅 docstring 文案。**CLOSED**

### DeepSeek LOW-02 — 模块 docstring 语言不一致 — **CLOSED / OUT OF SCOPE**

**原 finding**: 模块 docstring 改为中文但其余 15+ 函数保持英文。

**Controller 裁决**: REJECTED / NON-DEFECT / OUT OF SCOPE。AGENTS 要求本次新增/修改函数使用中文 docstring，其余为 W16 未修改的存量代码，不扩大治理范围。

**复证**: diff 仅修改 `_append_research_workbook_section`（新增）和 `build_research_workbook_payload`（修改）的 docstring。其余函数零触及。**CLOSED**

### MiMo O-1 — public builder 返回类型 `dict[str, object]` — **CLOSED / OUT OF SCOPE**

**原 observation**: `build_research_workbook_payload` 返回类型仍为 `dict[str, object]`，可考虑 TypedDict 化。

**Controller 裁决**: REJECTED / OUT OF SCOPE。public builder TypedDict 化会改变 accepted plan 明确保留的存量 public contract。

**复证**: `build_research_workbook_payload(...) -> dict[str, object]` 签名在 diff 中未变。**CLOSED**

### MiMo O-2 — Raises 段可省略 — **CLOSED / STYLE OBSERVATION**

**原 observation**: `-> None` 函数的 Raises 段可省略。

**Controller 裁决**: REJECTED / STYLE OBSERVATION。AGENTS 要求完整异常说明，因此不省略 Raises 段。

**复证**: Raises 段保留，文案已按 LOW-01 改为更精确的表述。**CLOSED**

---

## 3. 契约复证

### 3.1 三 TypedDict 字段/required 性

从当前 diff 确认：

| TypedDict | 字段 | required | 状态 |
|---|---|---|---|
| `_ResearchWorkbookEvidence` | `source`, `reference`, `finding` (all `str`) | 全部 | ✅ |
| `_ResearchWorkbookItem` | `item_id`, `prompt`, `status`, `response`, `evidence`, `analyst_notes`, `evidence_required` | 全部 | ✅ |
| `_ResearchWorkbookSection` | `section_id`, `title`, `category`, `items` | 全部 | ✅ |

无 `NotRequired`、`total=False` 或 `Optional`。**PASS**

### 3.2 Helper exact signature

当前 diff line 67-74:

```python
def _append_research_workbook_section(
    sections: list[_ResearchWorkbookSection],
    *,
    normalized: str,
    current_title: str,
    current_category: str,
    current_items: list[str],
) -> None:
```

与 plan W16-CTRL-01 逐字匹配。**PASS**

### 3.3 Nested 删除与两个调用

- `append_current_section` nested 定义: 已删除（diff `-` 行确认）
- heading flush: diff `+` 行确认调用 `_append_research_workbook_section`，传入 5 个参数
- EOF flush: diff `+` 行确认同上
- `current_items = []`: 在 heading flush 后由 caller 执行（line 164）

**PASS**

### 3.4 Heading reset / EOF / early return

- heading flush 先于 `current_title`/`current_category` 重置
- EOF flush 在 `for` 循环之后
- early return: `if not current_category or not current_items: return`

**PASS**

### 3.5 哈希材料和 ID 稳定性

从 diff 确认哈希材料字符串与原实现完全一致：

- section: `f"{normalized}\0{section_index}\0{current_category}\0{current_title}"` → `[:10]`
- item: `f"{normalized}\0{section_index}\0{item_index}\0{current_category}\0{current_title}\0{prompt}"` → `[:12]`
- 前缀: `section-` / `item-`
- 位置索引: `len(sections)` / `enumerate`

**PASS**

### 3.6 JSON shape / public API

- `build_research_workbook_payload(...) -> dict[str, object]`: 未变
- payload 结构键: `schema_version`, `workbook_type`, `template`, `sections` 等均未变
- `__all__`: 未变，helper 不在其中

**PASS**

### 3.7 存量 15 函数 19 处 `dict[str, object]` 不变

新增 helper 返回 `-> None`，不引入 `dict[str, object]`。diff 未触及任何存量函数签名。

**PASS**

### 3.8 中文 docstring

- 模块 docstring: `"""提供研究手册生成、证据、进度报告与状态管理工作流。"""` — 中文
- `_append_research_workbook_section`: Args 5 项 + Returns + Raises（`本函数不显式抛出异常。`）— 中文
- `build_research_workbook_payload`: Args 3 项 + Returns + Raises 3 项 — 中文

**PASS**

### 3.9 无 Any/object/cast/ignore/glue

```
grep -n "cast\|Any\|type: ignore\|getattr\|hasattr" research_workbook.py → 无结果
```

**PASS**

### 3.10 pyright

```
pyright dayu/cli/commands/research_workbook.py → 0 errors, 0 warnings, 0 informations
```

**PASS**

### 3.11 测试

```
pytest tests/cli/test_research_template_command.py -q → 212 passed
```

**PASS**

### 3.12 覆盖率

沿用实现门禁已验证的精确数据（仅 docstring 文案变化，可执行语句零变化）：

```
branch coverage: 220 / 284 = 85.79%（>= 80%）
statement coverage: 553 / 617 = 89.63%
```

**PASS**

### 3.13 Ruff delta

```
HEAD → 5 × TRY004
current → 5 × TRY004
delta → 0
```

**PASS**

### 3.14 Scope

diff 严格限制在 `dayu/cli/commands/research_workbook.py`。未修改 tests、README、plan 或其他生产代码。**PASS**

---

## 4. 修正点独立验证

修正内容仅一处：`_append_research_workbook_section` docstring 的 Raises 文案从 `无。` 改为 `本函数不显式抛出异常。`

- 生产控制流: 零变化
- 可执行语句: 零变化
- 测试 corpus: 零变化
- pyright 结果: 零变化
- Ruff 结果: 零变化
- 覆盖率: 零变化

修正为纯文档层变更，不影响任何行为或类型检查。**PASS**

---

## 5. Findings

### Open findings: 0

| # | 严重度 | 状态 | 描述 |
|---|---|---|---|
| DeepSeek LOW-01 | Low | **CLOSED** | Raises 文案已改为 `本函数不显式抛出异常。` |
| DeepSeek LOW-02 | Low | **CLOSED** | 模块 docstring 语言不一致，out of scope |
| MiMo O-1 | Observation | **CLOSED** | public builder 返回类型，out of scope |
| MiMo O-2 | Observation | **CLOSED** | Raises 段风格，已采用更精确表述 |

**High: 0, Medium: 0, Low: 0, Open: 0**

---

## 6. 最终判定

| 维度 | 判定 |
|---|---|
| 初审 findings 闭环 | PASS（4/4 CLOSED） |
| TypedDict / helper / flush / ID 契约 | PASS |
| JSON shape / public API | PASS |
| 类型检查 / 测试 / 覆盖率 / Ruff | PASS |
| Scope 控制 | PASS |
| **总体** | **PASS** |

可进入下一 gate（commit → push → draft PR）。
