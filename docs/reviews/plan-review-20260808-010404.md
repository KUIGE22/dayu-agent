# Corrective Re-Review: v4.5 W16-CTRL-01 — 存量签名计数纠正

**Review ID**: `plan-review-20260808-010404`
**Date**: 2026-08-08
**Reviewer**: MiMo (corrective focused re-review)
**Scope**: 纠正 005600 LOW-01 的计数方法错误，用 AST 精确复核存量 `dict[str, object]` 签名计数。
**Baseline**:
- `docs/reviews/plan-review-20260808-005600.md`（MiMo 前序 final re-review，含错误的 LOW-01）
- `docs/plans/2026-08-07-cli-write-architecture-refactor.md` (v4.5)
- `docs/reviews/plan-slice-4-v4.5-type-erratum-20260808-codex.md` (Controller erratum)
- `dayu/cli/commands/research_workbook.py`
**Status**: **PASS** — 零 HIGH，零 MEDIUM，零 LOW。005600 LOW-01 标记为 CLOSED (reviewer measurement error)。

---

## 1. 005600 LOW-01 错误分析

### 1.1 错误方法

005600 使用的脚本方法：按 `def` 到下一个 `def` 的文本区间计数 `dict[str, object]` 出现次数。这把**函数体内的局部变量类型注解**也归入了 enclosing function 的签名计数。

### 1.2 被错误计入的 4 行

| 行号 | 代码 | 实际归属 | 005600 错误归类 |
|------|------|----------|----------------|
| 45 | `sections: list[dict[str, object]] = []` | `build_research_workbook_payload` **函数体内**局部变量 | 误计为该函数签名 occurrence |
| 431 | `metadata: dict[str, object] = {}` | `inspect_research_workbook_report` **函数体内**局部变量 | 同上 |
| 623 | `matches: list[dict[str, object]] = []` | `build_research_workbook_update_preview` **函数体内**局部变量 | 同上 |
| 777 | `current_validation: dict[str, object]` | `build_research_workbook_rollback_preview` **函数体内**局部变量 | 同上 |

这 4 行是函数体内的局部变量赋值语句，**不是函数签名的一部分**。函数签名仅包含参数注解（`args`/`posonlyargs`/`kwonlyargs`/`vararg`/`kwarg` 的 `annotation`）和返回值注解（`returns`）。

### 1.3 额外误差

005600 还声称 line 138 含 2 处但仅被计为 1 处。AST 验证确认 line 138 确实含 2 处（`arg:payload` + `return`），Controller 的 17 处（非私有）已正确包含这两处。

---

## 2. AST 精确复核

使用 `ast.parse()` 遍历 `module.body` 顶层 `FunctionDef`，仅检查 `args.*.annotation` 和 `returns`：

```python
import ast
tree = ast.parse(source)
for node in tree.body:
    if isinstance(node, ast.FunctionDef):
        # check node.args.args[*].annotation
        # check node.returns
        # → only these are "function signature"
```

### 2.1 完整结果

| # | 函数 | 性质 | 签名 occurrences | 位置 |
|---|------|------|-----------------|------|
| 1 | `build_research_workbook_payload` | public | 1 | return@L40 |
| 2 | `validate_research_workbook_payload` | public | 2 | arg:payload@L138, return@L138 |
| 3 | `build_research_workbook_report` | public | 1 | arg:workbook_payload@L299 |
| 4 | `inspect_research_workbook_report` | public | 1 | return@L424 |
| 5 | `discover_research_workbook_reports` | public | 1 | return@L507 |
| 6 | `build_research_workbook_report_status_snapshot` | public | 1 | return@L527 |
| 7 | `build_research_workbook_update_preview` | public | 3 | arg:workbook_payload@L604, arg:evidence_records@L610, return@L611 |
| 8 | `write_research_workbook_update` | public | 2 | arg:evidence_records@L699, return@L700 |
| 9 | `build_research_workbook_rollback_preview` | public | 1 | return@L741 |
| 10 | `write_research_workbook_rollback` | public | 1 | return@L815 |
| 11 | `inspect_research_workbook` | public | 1 | return@L879 |
| 12 | `discover_research_workbooks` | public | 1 | return@L923 |
| 13 | `build_research_workbook_status_snapshot` | public | 1 | return@L938 |
| 14 | `_load_json_object` | private | 1 | return@L1017 |
| 15 | `_sha256_json_object` | private | 1 | arg:payload@L1076 |

**合计**: 15 函数，19 处（13 public 函数 / 17 处 + 2 private 函数 / 2 处）

### 2.2 与 erratum 对照

| 维度 | Erratum 声明 | AST 验证结果 | 匹配 |
|------|-------------|-------------|------|
| 函数总数 | 15 | 15 | ✓ |
| 非私有函数数 | 13 | 13 | ✓ |
| 私有 helper 数 | 2 | 2 | ✓ |
| 非私有 occurrences | 17 | 17 | ✓ |
| 私有 occurrences | 2 | 2 | ✓ |
| 总 occurrences | 19 | 19 | ✓ |

**Erratum 的 "15 函数 / 19 处（13 非私有/17 + 2 私有/2）" 完全正确。**

---

## 3. 全文件 `dict[str, object]` 总数（补充说明）

全文件 `line.count('dict[str, object]')` 共 23 处。其中：
- **19 处**在函数签名（args + returns）
- **4 处**在函数体内的局部变量注解（line 45, 431, 623, 777）

这 4 处局部变量注解不属于函数签名，不应计入"函数签名共含 X 处"的统计。005600 的错误在于将这 4 处也计入了签名级 occurrence。

---

## 4. 前序 Findings 状态更新

| Finding | 来源 | 状态 | 理由 |
|---------|------|------|------|
| 005600 LOW-01 (erratum "19 处" 不精确) | 005600 | **CLOSED (reviewer measurement error)** | AST 验证确认 erratum 的 19 处完全正确。005600 的脚本方法（按 def 区间计数）把函数体局部变量注解误归入签名，导致 23 处的错误计数。 |
| 004534 MiMo LOW-01 (items 遮蔽) | 004534 | **CLOSED** | Controller NON-DEFECT |
| 004534 MiMo LOW-02 (future annotations) | 004534 | **CLOSED** | Controller NON-DEFECT |
| 004534 MiMo LOW-03 (total=True) | 004534 | **CLOSED** | Controller NON-DEFECT |
| 004533 DeepSeek L1 (计数精度) | 004533 | **CLOSED** | Controller ACCEPTED/FIXED，AST 确认修正后的 19 处正确 |
| 004533 DeepSeek L2-L4 | 004533 | **CLOSED** | Controller NON-DEFECT |

---

## 5. 最终裁决

| 维度 | 结果 |
|------|------|
| Erratum "15 函数 / 19 处" 计数 | **PASS** — AST 验证完全一致 |
| 三 TypedDict 字段完整性 | **PASS** — 前序 review 已确认 |
| Helper exact signature | **PASS** — 前序 review 已确认 |
| Heading/EOF flush 语义 | **PASS** — 前序 review 已确认 |
| 35 tests + pyright | **PASS** — 前序 review 已确认 |
| 其他 slices 零变化 | **PASS** — 前序 review 已确认 |
| Plan 自洽 | **PASS** — 前序 review 已确认 |
| **总评** | **PASS** |

**Open findings: HIGH 0 / MEDIUM 0 / LOW 0**

**v4.5 W16-CTRL-01 erratum 可以 ACCEPT。Slice 4 可以按 plan 实施。**
